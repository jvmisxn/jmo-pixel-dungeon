# Backlog

## High Priority

- Harden save/load contracts for `Hero`, `Level`, `Mob`, and related runtime state.
- Identify which systems should be moved toward cleaner core-vs-presentation boundaries.

## Medium Priority

- Decide whether to split framework-level code from Shattered-PD-specific content into separate top-level modules or folders.
- Add a small smoke-test workflow for launch, floor transition, combat, and save/load.

## Low Priority

- Consolidate overlapping large logs if they stop providing distinct value.
- Add a compact “recent sessions” index if the change log grows too large.

## System Audit Findings

Filed by the system-audit loop (`docs/memory/system-audit/`). Tag `[audit:<id>]`.

- [P1][audit:S01] Autosave on floor transition + app-close/pause; today a crash/kill/tab-close loses the whole run (save only fires from manual Save & Quit).
- [P1][audit:S01] Atomic save write + `.bak` rotation; current write does `store_var` straight over the sole file — mid-write crash corrupts it unrecoverably.
- [P2][audit:S01] Delete ~200 lines of dead, out-of-sync duplicate serialization in `save_manager.gd:309-537` (encodes a wrong contract; corruption trap if ever wired up).
- [P2][audit:S01] Add save migration path (`_migrate(save, from_version)`); `SAVE_VERSION` currently only rejects newer saves, older ones silently default changed fields.
- [P3][audit:S01] Verify `RegularLevel.serialize()` drop of room list is intentional (room-scoped spawns/shop detection) or persist it.
- [P1][audit:S02] Mage starts with placeholder `worn_shortsword` "staff", no wand/spell (`hero.gd:124-137`); SPD Mage starts with a Staff of Magic Missile — core class identity/upgrade target missing.
- [P1][audit:S02] Class passives are empty `pass` stubs while perks advertise them: Warrior faster regen + Rogue stealth (`hero.gd:97-103`). Implement or stop advertising.
- [P2][audit:S02] `earn_xp()` doesn't cap `xp` at `MAX_HERO_LEVEL` (`hero.gd:772-776`); xp grows unbounded past cap and is serialized/shown. Clamp at max level.
- [P2][audit:S02] Champion "second weapon in ring slot" perk (`subclass_abilities.gd:85-92`) is unwired — `Belongings` has no dual-wield slot or attack path.
- [P2][audit:S02] Many talents beyond intuition/meal set are inert "groundwork slot" no-ops (`talent_data.gd`); points spend for zero effect — mark inert ones so picker UI doesn't sell dead upgrades.
- [P2][audit:S02] Add headless round-trip tests for `Belongings` (equip→serialize→deserialize identity, quickslot rebind, stack merge/remove_quantity) — highest data-loss surface in the hero system.
- [P2][audit:S02] `total_armor()` doc claims "armor and rings" but only sums armor (`belongings.gd:205-210`); fix the doc (rings act via buffs in SPD, not flat armor).
- [P3][audit:S02] `_projectile_collision_pos()` allocates a full-map bool array per throw (`hero.gd:486-497`); reuse a scratch `PackedByteArray` to cut per-shot GC churn.
- [P3][audit:S02] Duplicate local-focus-hero expression in `_do_move`/`_on_death` (`hero.gd:348,1077`); extract `_is_local_focus()` helper. (Truncated file.)
- [P3][audit:S02] `belongings.serialize()` writes unused `backpack_count` key (`belongings.gd:350`); dead field. (Truncated file → no auto-edit.)
- [P3][audit:S02] `BattemagePower` class/`buff_id` misspelled "Battemage" (`battlemage_power.gd:1,7`, ref `subclass_abilities.gd:155`); rename needs a save migration since `buff_id` is serialized — not mechanically safe.
- [P3][audit:S02] Backpack items lacking `serialize()` are silently dropped on save (`belongings.gd:354-357`); add an assert to surface silent data loss.
- [P1][audit:S03] `Char` has no combat-state serializer — `Actor.serialize()` saves only actor_id/pos/active (`actor.gd:53-70`); hp/hp_max/ht/str/shielding/stats/flags rely on each subclass remembering them. Add `serialize_char()`/`deserialize_char()` (buffs already handled) and super-call it.
- [P1][audit:S03] `damage_roll()` uses uniform `randi_range` (`char.gd:70`); SPD `Char.damageRoll()` is `Random.NormalIntRange` (bell). Port damage is too swingy — apply the two-roll average already used by `dr_roll()`.
- [P1][audit:S03] Guaranteed hits fire only for invisible attackers (`char.gd:153`); SPD makes any attack a can't-miss surprise when the target doesn't see the attacker (sleeping/unaware/stealth) + Assassin +50%. Thread a `surprise` flag into `attack()`/`hit()`.
- [P2][audit:S03] Non-`Buff` nodes attach via `add_buff` script-path key (`char.gd:378`) but `has_buff`/`get_buff`/`remove_buff_by_id` only match `Buff` (`char.gd:418-435`) — such buffs become un-findable/un-removable. Require `Buff` or handle the path key in lookups.
- [P2][audit:S03] `_innate_immunities()` allocates a new Array every `is_immune()` call in the hot attack/take_damage path (`char.gd:551-558`); return a cached/`const` array.
- [P2][audit:S03] Buff lookups are O(n) string scans, ~12 per hit; add a `buff_id -> Array[Buff]` index updated in add/remove for O(1) `has_buff`/`get_buff`.
- [P2][audit:S03] No headless combat tests — add coverage for damage roll, `hit()` 1e6 eva/acc bounds (`char.gd:157-160`), armor averaging, shield→HP ordering, Doom×1.67/Vulnerable×1.33 order.
- [P2][audit:S03] Resistances/immunities skipped for `Char`-sourced attack damage (`char.gd:273-275`); SPD applies `resist(src.getClass())` to attacks too — confirm no physically-typed attackers need it or route attack damage through `resist()`.
- [P3][audit:S03] `die()` prevention (`char.gd:347`) can leave `is_alive=true` at `hp<=0` if a subclass prevents without restoring HP → re-enters `die()` on next hit; add a guard/assert.
- [P3][audit:S03] `distance_to`/`is_adjacent` duplicated verbatim in `Actor` (`actor.gd:77-96`) and `Char` (`char.gd:585-594`); Blob uses the base copies so keep them, but Char's could `super()`-delegate to cut drift.
- [P2][audit:S04] TurnManager persists no scheduling state — `clear_actors()` wipes cooldowns on save and actors re-register at 0 on load (`turn_manager.gd:331`, `save_manager.gd:86`); slowed/hasted/just-acted mobs reset timing. Serialize `{actor_id: cooldown}` and restore before `process_until_hero()`.
- [P2][audit:S04] Cached `speed` goes stale silently — cooldown math divides by `entry["speed"]` set at register time, refreshed only via manual `refresh_speed()` (`turn_manager.gd:112-156`); a speed buff without the paired refresh uses wrong timing with no error. Re-query `get_speed()` live in `spend_energy` (SPD recomputes per action).
- [P2][audit:S04] Dead async mob-pacing layer — `MOB_ACTION_DELAY` is `const 0.0` so the only `await` (`turn_manager.gd:321`) is unreachable; `_process_mobs_async` runs synchronously and `processing_mobs` never blocks. Restore per-mob pacing or delete the coroutine/cache/gate.
- [P2][audit:S04] Add headless scheduler tests: cooldown rebasing (`:208`), speed→cooldown division (`:151`), `round_completed` once per party round (`:264`), `remove_actor` cleans `_round_hero_ids_pending` (`:122`), freed-actor skip (`:218`).
- [P3][audit:S04] `_is_time_frozen_for_nonhero()` walks GameManager→hero→belongings→artifact every non-hero turn even with no hourglass (`turn_manager.gd:52-64, 235`); resolve the frozen flag once per hero action and cache it.
- [P3][audit:S04] All TurnManager lookups are O(n) scans over `_actors` (`:108-178`), several per action; add a `node → entry` Dictionary index for O(1).
- [P3][audit:S04] `process_until_hero()` bails silently after 500 iterations (`:343`); with no hero registered it burns 500 mob turns on load — log a warning on cap-hit.
- [P3][audit:S04] Header docstring claims buffs register as actors (`turn_manager.gd:6`) but `Buff extends Node` and never registers; fix the comment (truncated file → no auto-edit).
- [P1][audit:S05] Swarm split duplicates HP instead of halving — `_split()` sets `child.hp = hp` (full copy) leaving parent HP unchanged (`sewer/swarm.gd:43-45`); SPD divides current HP between the two flies, so total swarm HP ~doubles per split and over-grants XP. Split HP: `child.hp = hp/2; hp -= child.hp` (guard hp>=2), sync child ht/hp_max.
- [P2][audit:S05] `_find_visible_heroes()` returns unsorted and callers always take `heroes[0]` (`mob.gd:139,154,461`); on multi-hero levels the target isn't the nearest. Route wake/notice through the existing distance-based `_find_nearest_char()` (`mob.gd:268`).
- [P2][audit:S05] Spinner's `_act_hunting` override drops shared AI guards — never consults `should_flee()`/Amok/Terror from base (`caves/spinner.gd`, base `mob.gd:170`); an amok/terrified spinner still hunts. Delegate the move/attack tail to `super._act_hunting()`.
- [P2][audit:S05] `create_boss` fallback returns a statless bare `Mob.new()` for unknown depth (`mob_factory.gd:170`); return `null` to match `create_mob`'s warn+null contract (`:137-138`).
- [P2][audit:S05] Halls spawn table ignores depth — `_halls_table(_depth)` is flat, unlike every other region (`mob_factory.gd:79`); add a depth ramp for parity.
- [P3][audit:S05] `first_summon` is inert in Necromancer — tracked/serialized/set but never read; docstring promises a faster first summon that never happens (`prison/necromancer.gd:16,127,162,171`). Wire it into summon timing or drop the field (serialized → needs judgment, not auto-fixed).
- [P3][audit:S05] Goo `damage_roll()` mutates state (`pumped_up = 0`) as a side effect and the ranged branch re-zeroes it (`bosses/goo.gd`); move pump consumption to the attack path so the roll is pure/testable.
- [P3][audit:S05] `did_visible_action`/`last_visible_action` bookkeeping is hand-duplicated across many subclasses; centralize via the base `on_move`/`on_attack_hit` hooks (`mob.gd:391,406`).
- [P3][audit:S05] No tests for the AI state machine or `MobFactory` weighted selection (`mob_factory.gd:141`) / detection rolls (`mob.gd:142,157`) — all pure functions, seed the RNG.
- [P1][audit:S06] Fury 1.5× applied twice → effective 2.25× melee — `Char.damage_roll()` iterates `modify_damage()` (`char.gd:71-73`, `fury.gd:11`) AND `Char.attack()` re-multiplies by name (`char.gd:107-108`). Drive damage-mod from one path only.
- [P1][audit:S06] `get_speed()` never calls polymorphic `modify_speed()` — hard-codes Cripple/Stamina/Adrenaline/Haste by name (`char.gd:531-543`), so Sleep(0.0)/Dread/MonkFlurry/FreerunnerMomentum speed effects are dead and Adrenaline/Haste carry redundant no-op hooks. Iterate the hook, then drop the name checks.
- [P1][audit:S06] Doom is invented — 30-turn countdown that kills via `take_damage(target.hp)` (`doom.gd:16-23`); SPD Doom is a permanent +50% damage-taken amplifier that never timer-kills. Replace timer with a damage-taken multiplier hook.
- [P1][audit:S06] Gladiator Combo fully inert — `add_hit()`/`finisher_multiplier()` never called outside buffs dir (`combo.gd`); buff accumulates nothing, no finisher fires. Wire into the Gladiator attack path.
- [P1][audit:S06] Frozen freeze-potion loop mis-scoped — `if freezable.size() > 0` block sits inside the `for item` loop (`frozen.gd:36-45`), freezing multiple potions and mutating the backpack mid-iteration; SPD freezes one potion/mystery-meat. Dedent the block out of the loop.
- [P2][audit:S06] charm.gd/terror.gd lose `source_id` on save/load — both `serialize()` write it but neither overrides `deserialize()` (`charm.gd:23`, `terror.gd:23`); charmer/terror source reverts to -1 on reload. Add matching deserialize.
- [P2][audit:S06] Barkskin & ArcaneArmor ignore `interval` — `level -= 1` every turn regardless (`barkskin.gd:31-40`, `arcane_armor.gd:36`); Warden/high-interval variants decay too fast. Gate decay on interval.
- [P2][audit:S06] Barrier shielding coupling fragile — writes directly into `target.shielding` and `on_turn` overwrites it (`barrier.gd:36-47`), clobbering other shield sources; exposes `shielding()` but `Char.total_shielding()` looks for `get_shielding()` (`char.gd:496-501`) so Barrier isn't summed. Unify shield API.
- [P2][audit:S06] HerbalArmor double mechanic — flat +5 via `modify_armor` AND a 15-pt absorb pool (`herbal_armor.gd:18-24`); SPD Earthroot Armor is absorb-only.
- [P2][audit:S06] `ht` vs `hp_max` duality (`char.gd:20-21`) — regen/fury/well_fed use `ht`, berserker_rage/soul_mark use `hp_max`; kept equal in hero.gd but desyncs if any effective-max-HP modifier is added. Consolidate to one max-HP concept.
- [P2][audit:S06] vertigo.gd:26-32 random-neighbor offsets do raw ±1/diagonal index math with no row-edge check → column-edge char can be shoved to a wrapped adjacent-row cell. Add row-boundary guard.
- [P2][audit:S06] Weakness carries `STR_PENALTY` + doc'd STR/accuracy penalty but implements no `modify_*` hooks (`weakness.gd`) — inert unless external code reads the const. Wire the penalty or document the external owner.
- [P2][audit:S06] Buff lookups are O(n) string scans (`has_buff`/`get_buff`/`remove_buff_by_id`, `char.gd:424-435`), many per swing; add a `Dictionary[buff_id → buff]` cache like SPD's typed map.
- [P2][audit:S06] No `ParalysisResist` buff — paralysis break uses inline `randi_range` per hit (`paralysis.gd:27-33`) instead of SPD's accumulating slow-decay resist. Add the resist buff.
- [P3][audit:S06] Poison damage off-by-one vs SPD — base `act()` decrements `time_left` before `on_turn()` reads it (`poison.gd:33`, `buff.gd:63-65`); tick uses `left-1`.
- [P3][audit:S06] MonkFlurry.description() builds `parts[]` then returns a fixed generic string ignoring it (`monk_flurry.gd:64-72`).
- [P3][audit:S06] fire_imbue/frost_imbue document Burning/Chill immunity that is not implemented (`fire_imbue.gd:6`, `frost_imbue.gd:6`).
- [P3][audit:S06] Imbue procs unwired — `FireImbue.proc()`/`FrostImbue.proc()` never called; `FireImbue.proc` also calls non-existent `Burning.reignite()` (`fire_imbue.gd:27`) → latent crash if wired. Fix the reignite call and wire procs into the attack path.
- [P3][audit:S06] `Burning.burn_increment` declared+serialized but never read (`burning.gd:10,82,89`) — dead state carried in every save. (Persistence-adjacent; left for approval rather than auto-fixed.)
- [P3][audit:S06] barkskin/arcane_armor `delay()` is a no-op `pass` stub whose docstring promises a cooldown adjustment.
- [P3][audit:S06] Dead no-op `MonkFlurry.on_turn()` override — AUTO-FIXED this run (removed on `audit/autofix`, PR #1).
- [P3][audit:S06] No buff unit tests (poison/bleed/corrosion decay curves, barrier depletion, hunger thresholds, serialize round-trips) — pure logic, cheap to cover.
- [P1][audit:S07] `mob_spawn_positions` under-spawns mobs — `remaining` decremented on failed placements too (`regular_level.gd:411`, no-op `pass` :410); empty-`std_rooms` fallback returns one pos not `count` (:378-382); "25% second mob same room" (:359) never implemented. Loop on successful placements.
- [P1][audit:S07] No periodic mob respawn (SPD `RegularLevel.respawner`) — mobs spawn once in `_build`; killed mobs never replaced → floors deplete, diverges from SPD balance. Add a rooms-weighted respawn timer.
- [P2][audit:S07] Mimic spawn discards its item — `regular_level.gd:115-122` throws away the rolled `item` instead of giving it to the mimic (also no `scale_to_depth`) → silent item loss, mimic drops nothing.
- [P2][audit:S07] `rooms` not serialized (`level.gd:717/777`) — empty after load; blocks respawn/room-based post-load features; SPD persists room bounds/types.
- [P2][audit:S07] `is_visible_from`/`has_los` (`level.gd:591-609`) allocate a full LEN `Array[bool]` via `ShadowCaster.cast_fov` per point LOS query (5 sites incl. mob targeting); route through existing `Ballistica` (O(distance)).
- [P2][audit:S07] O(n) linear scans on hot paths — `mob_at`/`find_char_at`/`heaps_at`/`pickup_item` (`level.gd:478-516`) scan all mobs/heaps per call; add a `pos->mob`/`pos->heap` index.
- [P3][audit:S07] `update_fov` runs 3–4 full-grid (LEN) passes per hero move (`level.gd:355-369`) — fusable if FOV ever profiles hot.
- [P3][audit:S07] `unlock_exit` (`level.gd:624-631`) opens every LOCKED_DOOR on the level rather than the exit-gating door.
- [P3][audit:S07] `static var _recent_specials` (`regular_level.gd:505`) is process-global, not per-run/not reset on new game → special-room-variety tracking bleeds across games; move to per-dungeon state.
- [P3][audit:S07] Grid constants duplicated: `level.gd:10-12` hardcodes `W/H/LEN` vs `ConstantsData.WIDTH/LENGTH` in `regular_level.gd` (equal today) — derive from ConstantsData.
- [P3][audit:S07] DARK feeling is visual-only (fog dim, `game_scene.gd:439`) — never reduces view distance; low-confidence fidelity note, verify vs SPD before acting.
- [P3][audit:S07] Several `pos / W` int divisions lack `@warning_ignore("integer_division")` (`level.gd:298-299,381-382,405-406`) — cosmetic warnings.
- [P1][audit:S08] Door/`connected` system is dead on regular levels: `place_adjacent` always leaves a 2-tile gap (`builder.gd:44`) so `find_door_pos` never finds a shared wall → `connect_adjacent` always fails → `Room.connected` stays empty → no DOOR/LOCKED_DOOR ever painted (vault/armory locked doors bypassed, shop/garden doors missing). Fix: allow wall-sharing adjacency or place doors at tunnel mouths in `_carve_tunnels`. Verify in-engine.
- [P2][audit:S08] `_carve_tunnels` `pair_key = min(id)*100000+max(id)` (`standard_painter.gd:49`) overflows int64 for RefCounted instance IDs (high-bit set) → possible key collision → silently skipped tunnel / disconnected floor (masked by 15× regen + traversability check). Key on `Vector2i`/string instead.
- [P2][audit:S08] `_place_connection_rooms` only tries adjacency to room `a`, shares one `conn_idx`, and force-marks conn↔b as neighbors regardless of geometry (`loop_builder.gd:216-235`) → floating connection rooms / long L-tunnels.
- [P2][audit:S08] `_generate_patch` runs full-map (WIDTH*HEIGHT) cellular automata, re-allocating the array `smoothness` (4-6)× per water level, then samples only room interiors (`standard_painter.gd:203-233`); restrict automaton to room cells.
- [P2][audit:S08] Dead code `_is_valid_doorframe`/`_has_adjacent_door` (`standard_painter.gd:75,94`, zero call sites; `_is_solid` only reachable via them) — wire into the door fix or delete. File is in TRUNCATED_FILES.txt (no auto-edit).
- [P3][audit:S08] `random_door_pos()`/`on_border()` (`room.gd:79,67`) have zero call sites — orphaned hooks of the unwired door system; keep for the P1 fix rather than delete.
- [P3][audit:S08] Only `LoopBuilder` exists — add SPD-style alternate builders (Line/FigureEight/Regular) behind a per-depth selector for layout variety.
- [P3][audit:S08] `_water_params` applies ~30% water fill to every Sewer/Caves/City floor regardless of feeling (`standard_painter.gd:173-189`); confirm vs SPD region defaults, may over-water non-water floors.
- [P1][audit:S09] HallsLevel has no `_create_random_trap()` override (`halls_level.gd` ends line 47) → depth 21-24 falls back to `RegularLevel._create_random_trap` tier-1 pool (WornDart/Poison/Fire/Alarm/Teleport) instead of the lethal Halls pool (Grim/Disintegration/Rockfall/Pitfall/Distortion/Storm). Every other region overrides this; add the Halls override. (halls_level.gd in TRUNCATED_FILES.txt — no auto-edit.)
- [P2][audit:S09] Boss/Last `_build()` never validate traversability and always `return true` — RegularLevel checks entrance→exit path + factory retries 6×, boss arenas get neither. A magic-number slip ships an unreachable boss/entrance and softlocks with no fallback. Add an entrance→boss-pos path assert before returning.
- [P2][audit:S09] HallsBossLevel & LastLevel ring the walkable arena with CHASM adjacent to floor (`halls_boss_level.gd:24,36`, `last_level.gd:31`); Halls/Yog knockback can shove hero (or mob) into the ring → falls to next depth mid-fight, breaking the encounter. Move the seal below the chasm or make the ring non-fall décor.
- [P2][audit:S09] Framework extraction: add a shared `BossArenaLevel` base — all 5 boss `_build()`s duplicate carve-arena/entrance/exit/LOCKED_DOOR-seal/spawn-boss scaffold; extract helpers and give the traversability-validation fix one home.
- [P2][audit:S09] HallsLevel view-distance reduction is an unimplemented TODO (`halls_level.gd:9-10`); SPD Halls does `viewDistance=min(26-depth,viewDistance)`. Needs a per-level `view_distance` field on Level.
- [P2][audit:S09] Boss floors only seal the down-exit via terrain LOCKED_DOOR; nothing blocks ascending the entrance to escape a live boss (SPD seals both stairs). `goo.gd:12 floor_sealed` is dead (written/read nowhere). Implement a real bidirectional seal.
- [P3][audit:S09] `unlock_exit()` scans all LEN cells and opens *every* LOCKED_DOOR (`level.gd:627`) — harmless on boss floors but latent coupling; unlock a tracked door list instead.
- [P3][audit:S09] Region `_roll_feeling()` mutates `num_standard_rooms += 2` in place for LARGE (`caves_level.gd:17`, `city_level.gd:15`, `halls_level.gd:21`) — safe today (fresh instance per gen attempt) but a footgun on regeneration.
- [P3][audit:S09] Boss arenas hardcode bounds (4..27, radii 11/5) assuming 32-wide map, untied to `ConstantsData.WIDTH/HEIGHT` — derive/assert against constants to prevent silent out-of-bounds writes if map size changes.
- [P3][audit:S09] No tests for `LevelFactory.create_for_depth` depth→class mapping or boss-death→`unlock_exit` seal flow; both are cheap to cover.
- [P1][audit:S10] `disarming_trap.gd:44-47` calls `drop_item(weapon, drop_pos)` with args swapped vs canonical `drop_item(pos, item)` (10 other callers) → disarmed weapon becomes an uncollectable heap keyed by an object = permanent item loss. Fix: `drop_item(drop_pos, weapon)`.
- [P1][audit:S10] `paralytic_trap.gd:14-15` paralysis effect is `pass`; `Paralysis` buff class exists but is never applied — trap only logs a message. (TRUNCATED file — manual fix.)
- [P1][audit:S10] `fire_trap.gd:14-16` burning buff is `pass`; only raw damage + ember spread run. Apply `Burning` like `blazing_trap.gd:15-18` does. (TRUNCATED file — manual fix.)
- [P2][audit:S10] Trap generation pools incomplete — 7 implemented classes referenced by no `_create_random_trap`: ParalyticTrap, FrostTrap, BlazingTrap, FlockTrap, CursingTrap, DisarmingTrap, PitfallTrap. Wire into region trap tiers (per SPD) or delete dead classes.
- [P2][audit:S10] `disarming_trap.gd:18-21` weapon detection dead on hero (no `Hero.get_weapon()`/`weapon`; weapons live on `belongings.get_equipped_weapon()`) → always "nothing to disarm."
- [P2][audit:S10] `cursing_trap.gd:17` equipment path dead (no `Hero.get_equipped_items()`) → always falls back to `Hex`.
- [P2][audit:S10] `pitfall_trap.gd:31-39` doesn't descend a depth; proxies with same-level teleport (TODO). SPD PitfallTrap forces chasm depth-drop with fall damage.
- [P3][audit:S10] Trap neighbor loops (`adj = pos + dir`, bounded only by `0<=adj<LEN`) wrap rows at grid edges; add a `cell_valid`/adjacency guard instead of relying on the wall border.
- [P3][audit:S10] `storm_trap.gd:34-39` re-fetches `mob_at(cell)` for water amplification when the value is already in scope — redundant O(mobs) scan per water cell.
- [P3][audit:S10] `explosive_trap.gd:24-41` double-hits a mob triggerer (via `mob_at(pos)` and the direct `triggerer` damage).
- [P3][audit:S10] `disarming_trap.gd:46` `elif level.has_method("add_heap")` references a non-existent Level method — permanently dead branch.
- [P3][audit:S10] `grim_trap.gd:32-39` bespoke "≤50% HP → lethal" rule deviates from SPD GrimTrap damage model; no unit coverage for trap activate/one-shot/serialize round-trip.
- [P1][audit:S11] Chasms are instant-death: `chasm.gd` is entirely unreferenced; the live inline path in `hero.gd:662-666` does `take_damage(hp_max, null)` → guaranteed kill. SPD drops the hero to the next depth (pit room) with HP-scaled fall damage + bleed, and levitation crosses safely. Reachable via boss-arena knockback (S09). Fix: real fall (check `Levitation`, then descend + scaled damage); revive or delete `chasm.gd` as one source of truth. (TRUNCATED file — manual fix.)
- [P1][audit:S11] Locked doors require the wrong key: `door.gd:25` opens `LOCKED_DOOR` only with `has_key("golden")`. SPD iron (locked) doors open with IRON keys; golden keys are for golden chests. `hero.has_key()` even has iron-key depth logic (`hero.gd:730`) that nothing ever invokes → iron keys are useless. Fix: `LOCKED_DOOR` → `has_key/use_key("iron")`. (TRUNCATED file — manual fix.)
- [P2][audit:S11] `chasm.gd.can_cross` checks `has_buff("levitation")` (lowercase) but buff id is `"Levitation"` (`levitation.gd:8`) → always false. Fold into the S11 chasm rework.
- [P2][audit:S11] Door-open logic triplicated: `door.gd.open`, `hero.gd:337-342` (auto-open on walk), `mob.gd:401-404` (mob steps on door) each hand-roll set-OPEN_DOOR + emit `door_opened` + `record_stat`. Route all three through a shared `DoorFeature.force_open(level,pos)` so door rules live in one place.
- [P2][audit:S11] `door.gd.open` SECRET_DOOR reveal returns `false` and the interact caller (`hero.gd:630`) ignores it → ambiguous turn accounting on reveal. Align with `_do_search` semantics.
- [P2][audit:S11] Chasm descent has no PitRoom landing (tied to P1): SPD lands the faller at a random point of the next level. The dead `chasm.gd.find_safe_landing` only scans 8-adjacent then a fully-random cell; a real impl should hand off to the floor-transition coordinator with a "fell" flag.
- [P3][audit:S11] `door.gd.close` is dead code (no caller; SPD has no manual hero door-close). Wire to a mechanic or drop. (TRUNCATED file — manual.)
- [P3][audit:S11] No unit coverage for door key-gating or chasm fall — both are pure-ish (level + opener) and good first framework-extraction test targets once corrected.
- [P1][audit:S12] `item.gd:154` base `duplicate_item()` returns `Item.new()`, so `split()` on any stackable WITHOUT its own override (missile_weapon, seed_item, bomb, stone, food, spell, dewdrop, torch) downgrades the split-off half to a generic Item — loses throw/use behavior. Hit by bandit/thief theft (`bandit.gd:59`, `thief.gd:48`), recycle (`spell.gd:373`, `game_scene.gd:1622`). Fix: base `duplicate_item()` → `Generator.create_item(item_id)` + `_copy_base_properties`; overrides then only add their extra fields.
- [P2][audit:S12] `item.gd:287` `serialize()` omits `bones` and `kept_though_lost_invent` (ankh "kept through lost inventory" flag); factory reconstruct does not restore them → mid-run save silently clears both. Add to serialize/deserialize.
- [P2][audit:S12] Item identification has two sources of truth: stored `identified` bool vs computed `is_identified()` (`item.gd:184`). Can desync; serialize persists the stored one. Collapse to derived (persist level_known/cursed_known only).
- [P2][audit:S12] `generator.gd:16` `CATEGORY_WEIGHTS` includes `GOLD:16` (highest weight) → ~30% of `random_item()` drops are gold; SPD's Generator category table never yields gold (level gen places it). Remove GOLD (and fallback-only MISC) from the category table. (TRUNCATED file — manual.)
- [P2][audit:S12] `generator.gd:183` `_generated_artifacts` static uniqueness set isn't serialized → mid-run save/load can regenerate an already-found artifact as a duplicate; also depends on `reset_artifacts()` at run start. Persist it (or key uniqueness off inventory/catalog). (TRUNCATED file — manual.)
- [P3][audit:S12] `generator.gd:495` `random_wand()` manual `+n`/curse fallback branch is dead (`Wand.random()` always exists); `_weighted_category`/`_weighted_random_from_table` re-sum constant weight tables each call. (TRUNCATED file.)
- [P3][audit:S12] No unit coverage for the item contract's pure paths: split/merge_stack/can_stack_with and serialize→create_item→deserialize round-trip — cleanest first framework-extraction test targets.
- [P1][audit:S13] `SpiritBow.deserialize()` is `pass` (`spirit_bow.gd:143`) — discards the whole saved payload, so the Huntress's bow loses its upgrade level/augment/enchant on every load (belongings restores via `Generator.create_item` + `deserialize`, `belongings.gd:387`). Fix: call `super.deserialize(data)` / drop the no-op override.
- [P1][audit:S13] All same-tier weapons are mechanically identical except reach: `delay_factor` defaults 1.0 and is never set in any factory branch (uniform attack speed), and damage is purely `tier`-based via `Weapon.get_damage_range()`. SPD differentiates each weapon within a tier by DLY/damage/ACC/STR. Fix: set per-weapon `delay_factor` (+ min/max) in `MeleeWeapon.create`/`MissileWeapon.create`.
- [P1][audit:S13] Random armor never spawns with glyphs: `Armor.random()` stubs both glyph branches (`armor.gd:371,373-374`), so loot armor is always glyph-less; glyphs are only reachable via Scroll-of-Enchantment `inscribe()`. Fix: apply `ArmorGlyph.random()` on the good roll.
- [P2][audit:S13] Curse weapon-enchant procs are never dispatched: `WeaponEnchantment.proc()` (`:32`) matches only the 13 non-curse ids, so `_annoying_proc`/`_sacrificial_proc`/`_displacing_proc` are dead; `random_curse()` also has no callers, so curse enchants are never generated. Wire both. (TRUNCATED file — manual.)
- [P2][audit:S13] FlowGlyph & EntanglementGlyph apply inert buffs: they build a raw `Buff.new()` with `buff_id="Haste"`/`"Rooted"` by hand (`armor_glyph.gd:463-466,482-485`) instead of `Haste.new()`/`Rooted.new()` (as SwiftnessGlyph does), so Flow's haste and Entanglement's root do nothing. (TRUNCATED file — manual.)
- [P2][audit:S13] `has_curse_glyph()` always false: `armor.gd:299` reads `glyph.get("is_curse")` but `ArmorGlyph` has no `is_curse` property → curse-glyph upgrade-removal branch (`armor.gd:240`) never fires. Add `is_curse` to `ArmorGlyph` (default false) with curse glyphs.
- [P2][audit:S13] Simplified enchant behaviors vs SPD: `_shocking_proc` has no chain-lightning, `_kinetic_proc` doesn't store overkill damage, `_grim_proc` uses a bespoke <20%-HP curve, missiles reuse the melee damage formula. Restore for parity. (TRUNCATED file — manual.)
- [P3][audit:S13] Weapon displayed STR req goes stale after upgrades: `str_requirement` cached at `create()` (lvl 0), printed by `get_stats_text()` (`weapon.gd:238`); Weapon has no `_update_str_requirement()` on upgrade like Armor does. Display-only (combat uses live `get_str_requirement()`). (TRUNCATED file — manual.)
- [P3][audit:S13] `_blazing_proc` `ClassDB.class_exists/instantiate("Burning")` (`weapon_enchantment.gd:220-222`) can never succeed (`Burning` is a GDScript class_name, not a native class) → dead branch; use `Burning.new()`. (TRUNCATED file — manual.)
- [P3][audit:S13] No curse armor glyphs exist (SPD: Anti-Entropy, Corrosion, Displacement, Metabolism, Multiplicity, Stench, Overgrowth, Bulk); add so cursed armor has negative glyphs.
- [P3][audit:S13] No unit coverage for weapon/armor pure math (`get_damage_range`/`dr_max`/`dr_min`/`get_str_requirement`/augment factors) or enchant/glyph serialize→create→deserialize round-trip — clean framework-extraction test targets.
- [DONE][audit:S13] Removed two no-op overrides: `MeleeWeapon.value()` (delegated verbatim to `super.value()`) and `MissileWeapon.get_damage_range()` (fetched `super` result unchanged + misleading comment). Auto-fixed on `audit/autofix` (behavior-neutral: inherited methods are identical).
- [P0][audit:S14] Wands never recharge over time: `Wand.recharge()` (`wand.gd:50`) has no per-turn caller (grep of `.recharge(` = only scroll/armor_glyph/spell instantaneous procs), no Charger buff, and nothing in turn_manager/hero/belongings ticks equipped wands. `Recharging` buff's `recharge_rate()` (`recharging.gd:17`) has zero callers → Recharging is dead. A wand keeps its 2 starting charges forever except for rare consumable refills. Fix: add a per-hero-turn Charger buff on equip that calls `recharge(1, hero)` × Recharging/battlemage multipliers. (TRUNCATED file — manual.)
- [P1][audit:S14] WandOfFrost always paralyzes: `wand.gd:425-433` adds `Cripple` then checks `has_buff("Cripple")` on the same target — always true because it was just added — so `Paralysis` fires on every frost hit instead of only when already-chilled. Fix: capture `was_chilled` before adding Cripple. (TRUNCATED file — manual.)
- [P1][audit:S14] Wands become un-identifiable by use after 5 zaps: `_use_for_identification` (`wand.gd:105`) decrements both `_uses_left_to_id` (10) and `_available_uses_to_id` (5) and early-returns once available ≤ 0; available is never refilled, so `_uses_left_to_id` stalls at 5 and `identify()` never fires. SPD refills availableUsesToID on level-up/descend. Fix: wire a refill hook or drop the availability gate. (TRUNCATED file — manual.)
- [P1][audit:S14] WandOfCorruption makes an enemy, not an ally: on_zap (`wand.gd:1006-1012`) sets `alignment = "ally"` (string, but `ConstantsData.Alignment` is an int enum) and applies `Amok` — which "attacks the nearest character regardless of allegiance" (`amok.gd:3`), so the corrupted mob attacks the hero. Purpose-built `CorruptionBuff` (`corruption_buff.gd`) already exists and flips alignment properly. Fix: use `CorruptionBuff.new()`. Caveat: `mob.gd`/`char.gd` have no `alignment` var or `set_alignment()`, so ally-alignment may be inert (cross-ref S05/S21). (TRUNCATED file — manual.)
- [P1][audit:S14] WandOfDisintegration bypasses the damage pipeline: `wand.gd:571-574` mutates `target_char.hp` directly and calls `die()` manually instead of `take_damage()`, skipping shielding, Invulnerable/immunity/resist, ArcaneArmor, on-damage buff wakeups, and consistent death handling (all in `char.gd:242+`). Fix: route through `take_damage`; add a magic/ignore-DR flag if armor-ignore is required. (TRUNCATED file — manual.)
- [P1][audit:S14] WandOfWarding is fully inert: on_zap (`wand.gd:853-881`) only tracks `_sentry_positions` and prints messages — no Sentry actor is spawned and nothing zaps enemies. Fix: spawn an actual ward/Sentry mob that attacks in range. (TRUNCATED file — manual.)
- [P2][audit:S14] Wand damage uses uniform `randi_range` (`wand.gd:166`); SPD uses NormalIntRange (bell curve). No shared helper exists (char.gd:229-234 hand-rolls triangular inline). Fix: extract `Balance.normal_int_range(min,max)` and use across wands/char-DR/weapons. (TRUNCATED file — manual.)
- [P2][audit:S14] Chain lightning re-rolls damage per arc with `0.7^n` falloff (`wand.gd:486,492`); SPD lightning deals full damage to all affected, no falloff. `_find_nearest_char` (`wand.gd:508`) also does an 81-cell `find_char_at` grid scan per arc. Fix: match SPD damage + iterate the mob list. (TRUNCATED file — manual.)
- [P2][audit:S14] `_make_open_passable()` allocates a fresh `LENGTH`-size bool array every disintegration/fallback zap (`wand.gd:132,560`); Frost/Corrosion use `Cripple`/`Poison` stand-ins applied instantly instead of a real Chill buff / spreading corrosion gas Blob. Cache the passable array; wire dedicated buffs/blob for parity (see S20). (TRUNCATED file — manual.)
- [P3][audit:S14] No unit coverage for the wand recharge formula (`BASE + SCALING*scale^missing`), the `random()` upgrade distribution (66.7/26.7/6.7), or the frost already-chilled gate — clean framework-extraction test targets.
- [P1][audit:S15] Thrown potions never shatter: `hero._do_throw_item` (`hero.gd:414`) handles Bomb/Seed/MissileWeapon/SpiritBow then falls to a generic ranged path — a Potion matches none, so all 13 `shatter()` overrides in potion.gd are dead code (`.shatter(` only appears as internal `super` calls). Fix: add a shatterable-potion branch that calls `shatter(collision_pos, level)`, consumes one, and identifies on visible effect.
- [P1][audit:S15] `duplicate_item()` returns a base `Potion`/`Scroll`, so `Item.split()` (`item.gd:145`) yields inert items — base `drink()`/`read_scroll()` are `pass`. Splitting a stack (partial throw/transfer/bones) produces a named-but-effectless potion/scroll. Fix: route split through `Potion.create(item_id)`/`Scroll.create(item_id)` then copy fields. (scroll.gd TRUNCATED — manual.)
- [P1][audit:S15] Offensive potions (ToxicGas/ParalyticGas/LiquidFlame/Frost) stamp a one-shot 3x3 fixed-duration buff/terrain change (`potion.gd:444,487,541,629`) instead of seeding a spreading, persistent Blob as SPD does. Weaker + non-persistent. Build on pending S20 Blobs, then seed Blobs from these potions.
- [P2][audit:S15] `ScrollLullaby` fakes Sleep by relabeling a `Paralysis` (`scroll.gd:519`) despite a real `SleepBuff` class (`buffs/sleep_buff.gd`, used by drowsy.gd). Wrong wake/tick semantics. Fix: use SleepBuff. (TRUNCATED — manual.)
- [P2][audit:S15] `ScrollTransmutation` fallback just identifies the first eligible item instead of transmuting it (`scroll.gd:760`) — misleading no-op when no HUD/WndTransmute is available. (TRUNCATED — manual.)
- [P2][audit:S15] `PotionMastery.drink` auto-selects `subclasses[0]` (`potion.gd:837`) with a "UI later" TODO — hero silently loses the subclass choice. Couples to S02 subclass gaps.
- [P3][audit:S15] `PotionExperience.drink` uses `xp_to_next - xp` (`potion.gd:712`); SPD grants a fixed level-scaled amount. Minor fidelity.
- [P3][audit:S15] Extract a shared `_chars_in_3x3(spos, lvl)` helper; the four AoE potion helpers duplicate the same DIRS_8 + find_char_at loop.
- [P1][audit:S16] Equipped rings/artifacts go inert after save/load: `belongings.deserialize` raw-assigns slots to "avoid gameplay effects" (`belongings.gd:391-402`), so `on_equip`→`_apply_passive` (`ring.gd:40-64`) never runs on load; passive Buffs also don't survive `_serialize_buffs` (inner-class `resource_path` empty → dropped, `char.gd:466`) and their `ring` backref isn't persisted. Every ring effect is dead until manual re-equip. Fix: re-run `_apply_passive`/real equip path on load + persist ring↔buff link.
- [P1][audit:S16] Ring of Might corrupts hero stats across reload: `MightBuff._apply_bonus` writes base `str_val`/`hp_max`/`ht` directly (`ring.gd:472-495`); inflated stats serialize, buff is gone on load (nothing subtracts it), and re-equipping re-adds on top → cumulative STR/HP drift. Fix: apply STR/HT via modifier hooks, never mutate base fields.
- [P1][audit:S16] Rings of Furor and Haste are inert: both effects are in `modify_speed` (`ring.gd:404-432`) but `Char.get_speed` only checks named buffs and never folds in `modify_speed` (`char.gd:531-543`) — same dead-hook class as S06. Fix: fold `modify_speed` from `_buffs` into `get_speed`; also split Furor→attack-speed / Haste→move-speed (currently identical).
- [P1][audit:S16] Cape of Thorns fully inert: charge-gain/50% absorb/reflect live in `on_hero_damaged` (`artifact.gd:371-388`) which the damage pipeline never calls (`Char.take_damage` uses only herbal/barrier/shield, `char.gd:207-316`; scene `on_hero_damaged` is unrelated visual feedback). Never charges, absorbs, or retaliates. Fix: invoke the artifact hook from `take_damage` (artifact.gd TRUNCATED — wire from caller side).
- [P1][audit:S16] Ring of Wealth fully inert: `WealthBuff` has no stat hook and no loot/gold code reads its `bonus()` (nothing in generator.gd/heap.gd/game_manager/levels queries `RingOfWealth`/`WealthBuff`) (`ring.gd:554-565`). Gold-multiplier + rare-drop effect is dead. Fix: query equipped Wealth ring `bonus()` in gold/loot rolls.
- [P2][audit:S16] Ring of Force unarmed path dead: `force_damage_roll(hero_str)` (`ring.gd:369-377`) has no caller; unarmed attacks don't route through it, so Force only adds its flat armed `modify_damage`. Wire into the unarmed damage branch for SPD parity.
- [P2][audit:S16] Sharpshooting boosts all accuracy/damage (not missile-only, `ring.gd:512-524`) and Elements only resists `Buff`-sourced damage via heal-back rather than true elemental DR (`ring.gd:323-333`). Tighten toward SPD.
- [P2][audit:S16] Ring buffs recompute `pow()` on every combat query (`ring.gd:281-307,404-432`); cache the multiplier when `bonus()` changes (equip/upgrade) instead of per-attack.
- [P3][audit:S16] Cursed ring `value()` returns 0: `75*(level+1)` with cursed `level=-1` (`ring.gd:107,115`). Clamp level to ≥0 in `value()`.
- [P3][audit:S16] No tests for ring bonus curves / artifact charge+leveling; artifact hooks (`feed_seed`/`feed_food`/`on_craft`/`on_search`) are duck-typed across UI/level — candidate for a typed artifact interface.
- [P1][audit:S17] Runestones never target a cell: `Stone.execute`→`_apply_effect` (`stone.gd:31,40`) acts only on `hero.pos`/8-neighbours, and `hero.gd:422,434` wires only `Bomb`/`SeedItem` into the throw/targeting pipeline. SPD stones are thrown at a chosen cell (Blink=targeted teleport, here a random 4-tile jump `stone.gd:287`; Blast/Shock/Fear/Sleep/Disarm target a cell). Fix: route Stone through `EventBus.enter_targeting` like `seed_item.gd:60-68` and pass the picked cell into `_apply_effect`.
- [P1][audit:S17] `split()` downgrades every consumable to an inert base `Item`: none of seed/food/stone/spell/bomb override `duplicate_item()` (only gold/scroll/potion/armor do), so `Item.split` (`item.gd:145-157`) drops `plant_type`/`stone_type`/`spell_type`/`bomb_type` and food's hunger/heal/random_effect. Reachable via partial drop / thief-steal (same root cause as S12/S15). Fix: per-type `duplicate_item()` that rebuilds via its factory and copies the subtype field.
- [P2][audit:S17] Frost Bomb (`bomb.gd:121`, TRUNCATED) and Deepened-Sleep Stone (`stone.gd:342`) apply `Paralysis` though `buffs/frozen.gd` and `sleep_buff.gd` exist. Paralysis never breaks on damage, so SPD sleep-wakes-on-attack and frozen semantics are both wrong. Fix: swap to `SleepBuff`/`Frozen` (stone side editable; bomb manual).
- [P2][audit:S17] Stone of Disarming doesn't disarm — applies `Weakness` (`stone.gd:350-368`, comment admits it) instead of knocking the target's weapon to a nearby cell for several turns. Implement weapon-drop-and-lock or rename to match.
- [P2][audit:S17] Spell of Wild Energy grants a non-SPD 30% free `upgrade()` of a random item (`spell.gd:104-111`) — exploitable; SPD's Wild Energy recharges wands + emits damaging sparks. Fix: drop the random upgrade, add spark AoE.
- [P3][audit:S17] Mystery-meat effect table is a flat 4-way `randi_range(0,3)` (`food.gd:81`); SPD weights effects differently (can also no-op). Low-value balance parity.
- [P3][audit:S17] `_cast_summon_elemental` does `load(res://...summoned_elemental.gd)` on every cast (`spell.gd:304`); use `const`/`preload`.
- [P3][audit:S17] Four near-identical `_consume_one` copies (`seed_item.gd:135`, `food.gd:151`, `bomb.gd:216`, `stone.gd:427`, `spell.gd:406`); extract one `Item`/`Consumable` helper.
- [P1][audit:S18] Bags are non-functional containers: `add_to_bag`/`can_hold`/`remove_from_bag`/`find_item` have zero callers repo-wide (only `bag.gd`), belongings has no `bags` array, and pickup never routes items into a bag. A generated bag (`generator.gd:311`) takes an ordinary backpack slot and holds nothing. Fix: add `bags` to belongings + auto-route matching pickups via `can_hold`, expanding effective inventory (SPD parity).
- [P1][audit:S18] `Bag.deserialize()` drops all bag contents: `serialize()` writes `data["items"]` (`bag.gd:112-116`) but `deserialize()` never reads it (`bag.gd:124`) and belongings only rebuilds top-level backpack/slots. Latent data-loss the moment bags go live. Fix: rebuild `items` via `Generator.create_item(id)` + per-item `deserialize`.
- [P1][audit:S18] Iron keys are dead: guards drop them (`guard.gd:15`) and `has_key("iron")`/`use_key("iron")` implement the SPD depth-match (`hero.gd:722,734`), but nothing calls them — LOCKED_DOOR unlocks with `"golden"` (`door.gd:25`, cross-ref S11). Fix: switch the LOCKED_DOOR branch to iron.
- [P1][audit:S18] Skeleton keys are dead: `has_key("skeleton")`/`use_key("skeleton")` have zero callers; the boss seal opens on death (`unlock_exit`, S09) not via the boss-dropped key. Fix: gate the down-exit on `has_key("skeleton")` or remove the type.
- [P2][audit:S18] `magical_holster` breaks its own promise ("wands and missile weapons"): `accepted_category` is only `WAND` and `can_hold` has no missile branch (`bag.gd:160-164`). Add a missile-weapon predicate to `can_hold`.
- [P3][audit:S18] `has_key`/`use_key` duplicate the same backpack scan + iron depth-match (`hero.gd:722-745`); extract a `_find_key(type)` helper. Also stray FUSE leftover `src/items/bags/.fuse_hidden0000001100000005` in the source dir — safe to delete.
- [P1][audit:S19] Plants apply one-shot buffs/terrain instead of SPD's spreading Blobs: Firebloom→Fire, Sorrowmoss→ToxicGas, Stormvine→StormCloud, Icecap→Freezing. The Blob system exists (`src/actors/blobs/fire_blob.gd`, `toxic_gas.gd`, …) but `firebloom.gd`/`sorrowmoss.gd`/`stormvine.gd`/`icecap.gd` `_do_effect` only buff the trigger char + paint terrain, so effects don't linger or spread. Couples S20; wire plant activation into blob seeding once S20 lands.
- [P1][audit:S19] Wrong buff types: `icecap.gd:36` and `dreamfoil.gd:35` apply generic `Paralysis` where `buffs/frozen.gd` (Frozen — shatters frozen potions, breaks on damage) and `buffs/sleep_buff.gd` (magical sleep, wakes on attack) exist. Same family as S17 Frost-bomb/Deepened-Sleep. Fix: swap to `Frozen`/`SleepBuff`.
- [P2][audit:S19] Grid edge-wrap in the two AoE plants: `firebloom.gd:23-28` and `icecap.gd:23-26` iterate `DIRS_8` as `pos + dir` guarded only by `0<=adj<LEN`; at column 0/WIDTH-1 the E/W/diagonal offsets wrap to the opposite edge row, so a near-wall freeze/burn can hit a cell across the map. Gate each `adj` with the existing column-safe `level.adjacent(pos, adj)` (`level.gd:634`).
- [P2][audit:S19] Dead duplicate `src/plants/seed.gd` (class `Seed`, 146 lines) — orphaned by `SeedItem` (`src/items/seeds/seed_item.gd`); unreferenced by class name, uid (`uid://dkcy06klubpej`) and path (repo-wide grep). Also carried a latent broken format string (`seed.gd:111-113`, `%` binds to the wrong literal → "not all arguments converted"). AUTO-FIXED this run: deleted `seed.gd`+`.uid` and the orphaned `.fuse_hidden…` artifact on `audit/autofix`.
- [P2][audit:S19] Dreamfoil cure IDs unverified: `dreamfoil.gd:6` cures by string IDs (`Blindness/Terror/Charm/Amok/Vertigo`) via `has_buff(id)`/`remove_buff_by_id(id)`; if any don't match the buff's runtime `id` (S06 registry) the cure silently no-ops.
- [P3][audit:S19] Missing SPD plant features: Rotberry stubbed to a quest flag (`rotberry.gd`) with no Wandmaker bush regrowth, no `trample`/`wither` states, Blindweed lacks SPD's short root; and no tests around `SeedItem._create_plant` or `_do_effect` buff application.
- [P1][audit:S20] Entire blob simulation is dead — `Blob.act()` (spread→apply_effects→decay, `blob.gd:32-45`) is never called. TurnManager registers only mobs (`level.gd:548,562`); blobs live in `level.blobs` and are used solely for rendering + smoke LOS. No `level.tick_blobs()` / per-turn hook. Every gas (bomb fire, fetid-rat toxic, spinner web) sits frozen at its seed cell forever — never poisons/paralyzes/burns/roots, never spreads, never decays. Root cause of S15/S19 one-shot-buff workarounds. Fix: drive blobs each hero round (mirror `tick_pending_bombs`, `level.gd:946`) or register as turn actors.
- [P1][audit:S20] `fetid_rat.gd:26-28` toxic-gas drop crashes on arg mismatch: `blob.seed(death_level, death_pos, 6)` calls `Blob.seed(cell, amount)` with 3 args (passes level as cell) and `add_blob(blob)` calls `add_blob(blob, cell)` with 1 arg — both runtime arg-count errors → fetid death throws, no gas spawns. Fix: `blob.seed(death_pos, 6)` + `add_blob(blob, death_pos)`.
- [P1][audit:S20] Blob layer under-wired vs SPD: only FireBlob (bomb), ToxicGas (fetid, broken), WebBlob (spinner) are seeded. `ConfusionGas`/`ParalyticGas`/`WaterOfHealth` have ZERO seeders (grep-confirmed) = dead classes; potions never call `add_blob`. Missing SPD blobs: Freezing/CausticGas/StenchGas/StormCloud/Regrowth/SmokeScreen. Once ticking works, wire gas potions/plants/traps to seed real blobs.
- [P2][audit:S20] Blob persistence bypasses its own contract: `Blob.serialize()` (`blob.gd:100`) and factory `_create_entity_by_type("blob")` (`save_manager.gd:496`) are never called; `level.serialize`/`_serialize_blobs` just `.duplicate(true)` the `{blob:<live Node>,pos}` dict and `_deserialize_blobs` re-appends raw (`save_manager.gd:434,443`). Round-trips only via `store_var(full_objects)` on a Node subclass — fragile; no `Blob.deserialize`. Fix: serialize via `blob.serialize()` + rebuild through factory.
- [P2][audit:S20] `Blob._spread` mutates `active_cells` while iterating it (`blob.gd:51-62`) — appends touched neighbors into the array being iterated, causing single-turn cascade and unstable follow-on `_apply_effects`/`_decay`. Build the new active set separately and swap at end.
- [P2][audit:S20] `smoke_screen` blob referenced in LOS (`level.gd:259,275`) but no SmokeScreen blob class exists → dead LOS-blocking branch until the class is added. Also `add_blob` (`level.gd:923-929`) hard-codes `seed(cell, 1)`, ignoring caller-intended density.
- [P3][audit:S20] No blob unit tests — spread/decay/active-cell lifecycle is pure and grid-based, ideal for headless tests once `act()` is driven.
- [P1][audit:S21] Blacksmith quest uncompletable: `blacksmith.gd:22,74` requires 15 `dark_gold_ore`, but repo-wide grep finds `dark_gold_ore` ONLY in `blacksmith.gd` — no mob/loot table/generator/terrain drops it (the `quest_handler.gd:126` "drops from bats" comment is unbacked). Hero can never deliver → quest + reforge reward unreachable. Fix: add a `dark_gold_ore` drop (bats, or Caves mining terrain + Pickaxe per SPD) or change the gate.
- [P2][audit:S21] Ghost double-subscribes to `mob_defeated`: `ghost.gd:48-50` self-connects `on_mob_defeated` AND `QuestHandler._register_npc` (`quest_handler.gd:143`) routes the static `_on_mob_defeated` to every NPC's `on_mob_defeated` → ghost handler fires twice/kill (double `quest_updated` emit on the completing kill; idempotent only by guards). Imp/Wandmaker use routing only. Fix: drop the self-connect, rely on QuestHandler routing.
- [P2][audit:S21] Ghost `reward_enchanted` is rolled+saved but never applied: `ghost.gd:155` rolls 20% enchant, persists it (`:351,363`), yet `_generate_rewards` never enchants the weapon/armor. Inert field vs SPD's enchanted-reward chance. Fix: apply enchantment when true, or delete the dead roll.
- [P2][audit:S21] Ghost/Imp/Wandmaker regenerate rewards + re-pick targets in `_init` on every instantiation incl. load, then `deserialize` overwrites them — wasted allocation and, worse, advances the global RNG stream on load (perturbs later same-frame gen). Fix: guard generation behind "not deserializing" or generate lazily on first interaction.
- [P2][audit:S21] Quest depth/spawn logic duplicated: `is_quest_depth` (`quest_handler.gd:43-56`) and `spawn_quest_npc` (`:72-97`) carry parallel depth-range + `_quest_available` tables, called back-to-back (`loading_scene.gd:274-275`). Divergence risk (gate passes but spawn fails or vice-versa). Fix: single source — call `spawn_quest_npc` (null when none) and drop the separate gate.
- [P3][audit:S21] NPC placement is room-agnostic random: `_find_spawn_pos`/`_find_quest_spawn_pos` scan up to 100/80 random cells with per-cell `has_method` probes; SPD places NPCs in a room away from entrance. Can land in corridors. Fix: room-based candidate list.
- [P3][audit:S21] Shopkeeper stocks a duplicate healing potion: `shopkeeper.gd:71` always adds Potion of Healing, then Caves/City/Halls branches add an identical second (`:91,96,101`). Harmless listing noise. (shopkeeper.gd truncated — backlog only.)
- [P2][audit:S21] Blacksmith is a stub vs SPD (parity add): no Pickaxe/mining, no CRYSTAL/GNOLL variants or quest boss, no favor system, single reforge only. Revisit after the ore-source fix.
- [P3][audit:S21] Wandmaker quest items half-implemented: only `seed_of_rotberry` becomes a real `SeedItem`; `corpse_dust`/`elemental_embers` are generic MISC items (`wandmaker.gd:270-284`) with no dedicated quest rooms (MassGrave/RitualSite).
- [P3][audit:S21] No NPC/quest tests: dialogue-state transitions, `QuestHandler` availability gating, and serialize/deserialize round-trips are pure and headless-testable. AUTO-FIXED this run: removed write-only dead field `_last_interacting_hero_actor_id` in `npc.gd` (never read/serialized), verified with gdparse, on `audit/autofix` (PR #1).
- [P2][audit:S22] `Pathfinder`'s entire hand-written A* is dead code. Runtime pathing uses Godot `AStar2D` (`level.gd:130,659`); the only live `Pathfinder` symbol repo-wide is `get_neighbors` (`level.gd:653`). `find_path/find_step/build_distance_map/distance/manhattan` + all helpers have zero callers → duplicated corner-cut logic (`pathfinder.gd:194` vs `level.gd:157`) that can silently drift. Delete the dead A* or make `level.find_path` delegate to it. NOT auto-fixed — `pathfinder.gd` is in TRUNCATED_FILES.txt.
- [P2][audit:S22] `Pathfinder.build_distance_map` is a queue-relaxation (SPFA) flood with non-uniform costs (1.0/√2), not SPD's uniform-cost integer BFS `buildDistanceMap`. Currently uncalled so latent, but diverges from SPD distance semantics if ever wired up. Backlog (truncated file).
- [P2][audit:S22] No unit tests for the geometry core (`Pathfinder`/`Ballistica`/`ShadowCaster`) — all pure static flat-array functions, ideal for GUT: path length, Ballistica collision index vs wall, FOV radius/symmetry, diagonal corner-cut rejection. Much of combat/wand/AI rides on these.
- [P3][audit:S22] `Ballistica.IGNORE_SOFT_SOLID` is effectively inert — soft-solid (DOOR/WEB) only resolves when a terrain array is passed (`ballistica.gd:113-116`); only `wand.gd:129` passes it, artifact/hero LOS casts don't. Revisit when `avoid[]` lands.
- [P3][audit:S22] `auto_walk_coordinator.get_visible_mob_positions` keys mobs by cell position, not identity (`:73`) → a known mob merely moving reads as a "new mob" and cancels auto-walk (`:34-37`). Over-cautious but safe. Also 3 separate `if scene._current_level:` guards (`:38,43,48`) could collapse — behavioral-neutral refactor, needs judgment.
- [P3][audit:S22] `shadow_caster.gd:58` `origin / width` lacks the `@warning_ignore("integer_division")` annotation used elsewhere (`level.gd:136`, `pathfinder.gd:126`). Cosmetic consistency. AUTO-FIXED this run: wrapped the over-length `cast_fov` signature at `shadow_caster.gd:42` (gdlint max-line-length failure), formatting-only, verified with gdparse + gdlint, on `audit/autofix` (PR #1).
- [P1][audit:S23] ~185 lines of dead, unreachable, already-drifted duplicate input code in `game_scene.gd`: all four wrappers delegate then `return`, leaving stale bodies — `_handle_cell_click` (`:1162` return, dead 1165–1218), `_handle_key_input` (`:1221`, dead 1223–1309), `_move_direction` (`:1312`, dead ~1315–1336), `_attack_adjacent_enemy` (`:1339`, dead ~1342–1359). Proven drift: coordinator merged `KEY_SPACE`/`KEY_PERIOD` (input_coordinator.gd:106) but dead copy still splits them (game_scene.gd:1279–1284). Delete dead bodies. NOT auto-fixed — `game_scene.gd` is in TRUNCATED_FILES.txt (needs careful hand-edit).
- [P1][audit:S23] Targeting mode does not block non-ESC keyboard input (`input_coordinator.gd:53-56`): when `_targeting_active` only `KEY_ESCAPE` is intercepted; other keys fall through, so a movement key submits a real `move` action while targeting stays armed (and `KEY_I`/quickslots fire mid-target). SPD's `CellSelector` swallows the turn until cell/cancel. Fix: while targeting, handle only ESC and consume the rest. Behavioral → backlog.
- [P2][audit:S23] `TargetingCoordinator.resolve` fires `callback.call(cell)` without a bounds check (`targeting_coordinator.gd:40,61`): the FOV gate is guarded by `cell >= 0 and cell < visible.size()`, so an out-of-range cell skips visibility and still resolves → possible downstream crash. Add `ConstantsData.is_valid_pos(cell)` guard.
- [P2][audit:S23] No auto-aim / last-target memory (SPD `QuickSlotButton` pre-aims nearest enemy + remembers last target, re-tap fires — SPD #1727/#291/#1703). Our targeting needs an explicit click every activation with no default. Add `autoAim(item,range)` feeding `TargetingCoordinator.enter`.
- [P2][audit:S23] No unit tests for input dispatch or the targeting range+FOV gate — pure static logic with a mockable `scene`; would have caught the SPACE/PERIOD drift above.
- [P3][audit:S23] `resolve` attack-animation detection is a fragile heuristic (`targeting_coordinator.gd:51-56`): only `spirit_bow`/`proc`/`explode`/`zap` play the cast anim; thrown potions (`shatter`) get none. Prefer an explicit `wants_cast_animation()` item contract.
- [P3][audit:S23] `resolve` recomputes the acting hero twice (`_get_input_hero()` at `targeting_coordinator.gd:30` and `:57`) in one frame — reuse the first.
- [P3][audit:S23] `attack_adjacent_enemy` (`input_coordinator.gd:166-174`) attacks the first `DIRS_8` enemy and skips any char exposing `interact`; no priority (lowest-HP/last-target) and charmed/allied mobs (no `interact`) are still attacked.
- [P1][audit:S24] `handle_descend` bypasses the `MAX_DEPTH` (26) cap — `floor_transition_coordinator.gd:23-25` inlines `_cache_current_level`/`depth += 1`/`_on_depth_changed` instead of `GameManager.descend()` (game_manager.gd:146-155), whose `if depth >= MAX_DEPTH: return -1` guard it drops. From the deepest floor, depth advances to 27+. Delegate to `GameManager.descend()`/`ascend()` and bail on `-1`, or add the cap/seal check here. Behavioral → backlog.
- [P1][audit:S24] Depth-mutation logic duplicated/drifting — `handle_descend`/`handle_ascend` (`:23-25`,`:54-56`) hand-copy GameManager's `_cache_current_level → depth ± 1 → _on_depth_changed` (game_manager.gd:146-166) minus its bounds checks; root cause of the cap bug. Consolidate on the GameManager API.
- [P2][audit:S24] `handle_ascend` plays the wrong SFX — `floor_transition_coordinator.gd:52` calls `AudioManager.play_sfx("descend")` on ascent; no `"ascend"` cue is registered anywhere (grep). Register an ascend cue, then fix the literal. Behavioral (missing asset).
- [P2][audit:S24] Floor-change hook reaches only the equipped artifact — `notify_party_floor_change:85-89` calls `on_floor_change()` on `belongings.get_equipped_artifact()` only (sole impl `artifact.gd:742`); SPD `Item.onLevelChange()` fires on all belongings + buffs. Add a generic `on_floor_change()` Item/Buff contract and iterate belongings + active buffs.
- [P2][audit:S24] No surface exit at depth 1 — `handle_ascend:44-48` hard-blocks floor-1 ascent though `surface_scene.gd` exists; SPD floor-1 up-stairs return to the surface village. Route depth-1 ascent to the surface scene.
- [P2][audit:S24] No transition tests — floor/run transitions have zero coverage (depth caps, party-gate, cause-of-death parsing at `run_transition_coordinator.gd:80-88`). Add unit tests with a stub scene/GameManager.
- [P3][audit:S24] Latent null-deref — `handle_descend:15` / `handle_ascend:41` dereference `scene._current_level.exit_pos`/`.entrance` after a guard (`:10`,`:36`) that short-circuits when `_current_level` is null, letting a null level fall through. Add an explicit null check.
- [P1][audit:S25] Full FOV+visibility rebuild on every mob action — `on_mob_action` (`scene_feedback_coordinator.gd:41-47`) runs `update_fov` + `fog_of_war.update_visibility()` + full `_update_entity_visibility()` (rebuilds hero identifiers + blob overlays + scans every mob/item/plant/bomb sprite) for *each* acting mob. N mobs/turn = N full repaints; SPD observes FOV once per hero turn (`Dungeon.observe()`/`afterObserve`). Coalesce to one refresh per turn via a dirty flag, and/or gate on the acting mob being visible/near the hero. Behavioral (perf).
- [P2][audit:S25] This-turn sprites flash through fog — `refresh_after_turn` (`scene_feedback_coordinator.gd:16` then `20-22`) applies `_update_entity_visibility()` *before* refreshing item/plant/bomb sprites, so newly spawned sprites (added `visible=true`) in fogged cells render un-gated for one turn. Refresh sprites first then update visibility, or set initial `visible` from `level.visible/visited[pos]` at spawn (`scene_visual_coordinator.gd:66-71/100-103/129-134`). Behavioral.
- [P2][audit:S25] Stale item icon when a heap's top item changes — `refresh_item_sprites` (`scene_visual_coordinator.gd:62-65`) keys by cell `pos` only and skips any pos already tracked, so the old icon persists when a heap's top `item` changes. Cache item identity per pos and re-`setup_from_item` on change. Behavioral.
- [P2][audit:S25] Redundant double blob-overlay rebuild — refresh paths call `_refresh_blob_overlays()` then `_update_entity_visibility()`, which calls `refresh_blob_overlays` again (`scene_visual_coordinator.gd:211`); e.g. `game_scene.gd:634` then `:640`. Blob cells recomputed and re-pushed twice. Drop the standalone call when entity-visibility follows. Behavioral (perf).
- [P2][audit:S25] Dead inline block behind extraction — `_update_entity_visibility()` delegates then `return`s (`game_scene.gd:1113`); lines `1114-1160` (~47) are the old unreachable implementation (same as S23). Delete once verified. game_scene.gd is in TRUNCATED_FILES.txt → no auto-edit; manual/gated only.
- [P2][audit:S25] Feedback layer is untestable + untested — all three coordinators are static funcs over an opaque `scene: Variant` with duck-typed `scene._*`/`has_method` access; zero coverage. Extract a typed feedback target (or pass concrete typed nodes) so behavior can be unit-tested and the Variant guards drop.
- [P3][audit:S25] Missing `is_instance_valid` guard — `update_entity_visibility` item/plant/bomb loops (`scene_visual_coordinator.gd:199-210`) write `sprite.visible` without the validity guard used for mobs (`:187`); a freed sprite lingering in a dict would error. Defensive.
- [P3][audit:S25] Hard autoload coupling — coordinators reach `GameManager`/`AudioManager`/`NetworkManager`/`OnlineEventCodec`/`ConstantsData` directly; inject for framework extraction.
- [P3][audit:S25] Duplicate status-feedback paths — `on_hero_damaged_detailed` shows buff VFX directly (`scene_feedback_coordinator.gd:91-92`) while `on_status_effect_applied` also shows feedback + emits a world event (`:101-106`); same buff-damage event can double-fire VFX / diverge netcode. Unify through one path.
- [P1][audit:S26] `descend()`/`ascend()` dead; live path lost the MAX_DEPTH cap — no caller invokes `GameManager.descend()`/`.ascend()` (`game_manager.gd:146,158`); `floor_transition_coordinator.gd:23-25,54-56` inlines the body (`_cache_current_level`+`depth ± 1`+`_on_depth_changed`) **without** `descend()`'s `if depth >= MAX_DEPTH: return -1` guard → descend past floor 26 (same root as S24-P1). Route the coordinator through `descend()`/`ascend()` (restores cap, dedups) or delete the dead methods after moving the cap into the live path. game_manager.gd in TRUNCATED_FILES.txt → gated/manual.
- [P2][audit:S26] `quest_flags` not persisted — `save_manager.gd:145-176` omits it, so it survives neither save nor `_cleanup_previous_run`. Latent now (only `rotberry.gd:16` writes; `get_quest_flag` unused) but becomes P1 the moment a flag gates content (Blacksmith/Imp quests). Add `quest_flags` to the save dict. Behavioral (save coverage).
- [P2][audit:S26] `spend_gold()` dead; spending open-codes `gold -=` with inconsistent signals — `wnd_shop.gd:199` emits nothing; `shopkeeper.gd:204-207` emits `gold_collected(-price)` on buy but only `gold_changed` on sell (`:254-255`). HUD listens to `gold_collected` (`hud.gd:245`), not `gold_changed` → gold display can go stale after a shop transaction. Route every spend through `spend_gold()` (already guards underflow + emits) and have it emit `gold_collected(-amount, gold)` to mirror `add_gold()`. Behavioral.
- [P2][audit:S26] No GameManager run-state (de)serializer despite the "Save/Load" header (`game_manager.gd:483`) + class-doc claim (`:2-5`); SaveManager hand-copies 11 fields (`save_manager.gd:145-176`), so adding a field means editing two files in lockstep — how `quest_flags` was missed. Add `GameManager.serialize_run_state()`/`apply_run_state()` as the single chokepoint. Coupling/framework-extraction.
- [P3][audit:S26] Dead wrappers — `current_region()`, `current_region_name()`, `get_quest_flag()` have zero external callers (callers use `ConstantsData.region_for_depth()` directly). Drop once confirmed. game_manager.gd truncated → gated/manual.
- [P3][audit:S26] No lifecycle-math tests — MAX_DEPTH cap, `compute_final_score()`, gold add/spend symmetry, `cycle_local_hero_focus` party rotation are pure state functions with zero coverage; natural first tests for a framework-extraction pass.
- [P1][audit:S27] Boss HP-bar feature is dead — subscribed but never emitted. `hud.gd:249-251` connects `boss_fight_started`/`boss_damaged`/`boss_defeated` with working handlers (`hud.gd:403-415`) and instantiates `boss_hp_bar.gd`, but a repo-wide grep finds **zero** emitters of the three signals (`event_bus.gd:137,140,143`). Boss bar never appears/updates/hides. Emit `boss_fight_started(name,hp)` on boss alert, `boss_damaged(hp,max)` in boss `take_damage`, `boss_defeated` on boss death (SPD `BossHealthBar` behavior). Ties S35/S09. Behavioral.
- [P2][audit:S27] `cancel_targeting` connected but never emitted — `game_scene.gd:927` connects `_on_cancel_targeting`; nothing emits `cancel_targeting` (`event_bus.gd:129`). Decoupled cancel path inert (cancel works only via direct ESC branch, S23). Emit it from the cancel site or drop the dead subscriber. Behavioral.
- [P2][audit:S27] `quest_updated` broadcast 11× but no consumer — emitted from `ghost.gd:219/236/329`, `blacksmith.gd:88/194`, `imp.gd:152/245`, `wandmaker.gd:137/220`, `rotberry.gd:26`, `wnd_quest_reward.gd:164`; no connect anywhere (`event_bus.gd:107`). Add a quest-journal/badge/message-log consumer or the emissions are dead overhead. Ties S21. Behavioral.
- [P2][audit:S27] `mob_died(mob)` emitted 5× but no consumer; loot/effects run off `mob_defeated` — `mob.gd:439`, `piranha.gd:114`, `mimic.gd:123`, `bee.gd:93`, `animated_statue.gd:130` emit `mob_died.emit(self)` (declared "for loot/effects", `event_bus.gd:38`) but the 6 real consumers all listen to `mob_defeated(pos,name,id)`. Two parallel death signals, one dead. Wire `mob_died` where an object handle is needed or drop it. Behavioral.
- [P2][audit:S27] Always-true `has_signal()` guards before emit — `shopkeeper.gd:54/166`, `blacksmith.gd:144`, `ghost.gd:317`, `imp.gd:233`, `wandmaker.gd:204`, `game_scene.gd:926`, `online_event_codec.gd:527` wrap emits in `if EventBus.has_signal("...")`. EventBus is a static autoload → always true; a dict lookup per emit for zero safety. Emit directly. Cleanup/perf.
- [P3][audit:S27] Redundant `X`+`X_detailed` double-dispatch on hero move/damage/death — each site fires both variants (e.g. `hero.gd:347-350`); both have live consumers, so 2 emissions + 2 fan-outs per event. As port goes MP-aware, prefer `_detailed` and let single-hero consumers filter, then retire the plain trio.
- [P3][audit:S27] `game_saved`/`game_loaded` emitted (`save_manager.gd:54/125`) but unconsumed; `npc_face_hero` emitted (`shopkeeper.gd:55`) but unconsumed — free hooks (save toast / autosave indicator / NPC facing) currently no-ops.
- [P3][audit:S27] No bus-contract test — a headless test asserting every declared signal has ≥1 emitter AND ≥1 consumer would have caught all four dead-boss/cancel signals mechanically. High-leverage framework hook.
- [P1][audit:S28] SceneManager never updates Godot's `current_scene`, so TurnManager reads the wrong node and `on_mob_action` never fires. `_do_transition` (scene_manager.gd:45-53) uses `root.add_child()` directly, never `set_current_scene`/`change_scene_to_*` → `tree.current_scene` stays pinned to MainScene for the whole run. `turn_manager.gd:286` caches that as `_cached_game_scene`, so the `has_method("on_mob_action")` guard (turn_manager.gd:318) is false and the loop's only caller of `on_mob_action` (game_scene.gd:518) never runs → visible mob moves/attacks during the mob phase get no per-action FOV/sprite refresh. Fix: `set_current_scene(new_scene)` in `_finalize_transition`, or point TurnManager at `SceneManager.current_scene`/`root.get_node_or_null("GameScene")`. Behavioral; verify in-engine.
- [P2][audit:S28] MainScene leaks — never freed. `_ready` (scene_manager.gd:13-17) captures `tree.current_scene` during autoload init when it is still null, so the first `_do_transition` skips the `queue_free` (line 48) and MainScene lingers as an inert root child all session. Lazily capture `current_scene` on first transition. Behavioral.
- [P2][audit:S28] No transition re-entrancy guard — `_do_transition` `queue_free`s the old scene and sets `current_scene = new_scene` before the deferred `add_child` runs; two `go_to`s in one frame free a not-yet-parented node then add a freed one. Add an in-flight guard. Behavioral.
- [P2][audit:S28] `scene_changed` signal (scene_manager.gd:8,58) has zero consumers repo-wide — wire intended listeners (TurnManager cache reset, per-screen audio) or drop it. Cleanup.
- [P2][audit:S28] No SceneManager tests despite it being the single transition chokepoint — headless test: go_to frees prior, tracks current_scene, applies meta before `_ready`. Framework hook.
- [P3][audit:S28] `go_to` has no null guard on `scene_script` — `scene_script.new()` crashes on a null `load()`; every caller guards individually. Fold into the API. Robustness.
- [P3][audit:S28] Two parameter conventions — meta dict everywhere vs `go_to_node` + direct `_pending_level`/`_pending_region` props for game_scene (loading_scene.gd:404-408, game_scene.gd:151,206). Unify on meta for framework extraction. Consistency.
- [P1][audit:S29] Badge total undercounts by 3 → count can read e.g. `26/23`. `_ALL_BADGE_IDS` (badges.gd:413-441, 23 entries) omits `strength_15`, `first_death`, `death_by_goo`, all unlockable via `unlock()` (badges.gd:343,301,306). `get_unlocked_count()` = `_unlocked.size()` (badges.gd:112) so unlocked exceeds total in wnd_badges.gd:196-197 and player_profile.gd:228 (which even lists `strength_15` in its grid, wnd_badges.gd:222). Add the 3 IDs, or derive total from the union of unlockable IDs. Behavioral.
- [P2][audit:S29] `ItemCatalog._load` assigns an untyped `get_var()` dict into typed `Dictionary[String, bool]` (item_catalog.gd:110) — unlike sibling catalogs that coerce (discovery_catalog.gd:126-131, badges.gd:381-387). On Godot 4.4+ can raise a type-mismatch runtime error on load and wipe global identification. Add a `_coerce_string_bool_dict`. Save/load safety.
- [P2][audit:S29] `champion_win` is redundant with `all_classes_won` — both unlocked under the identical all-five-classes condition (badges.gd:258-260), near-identical descriptions (badges.gd:182-193 vs 227-228). Inflates the catalog and diverges from SPD (Champion = subclass/challenge win). Repurpose or remove one. Behavioral/fidelity.
- [P2][audit:S29] `death_by_goo` false-positives on ANY depth-5 death — `_on_hero_died` unlocks it whenever `GameManager.depth == 5` regardless of killer (badges.gd:303-306; comment admits the heuristic). Key off the actual damage source/boss identity. Behavioral/fidelity.
- [P2][audit:S29] DiscoveryCatalog writes the whole catalog to disk on every record — `_record` calls `_save()` unconditionally (discovery_catalog.gd:87) and `_on_mob_revealed` (:59-61) fires per revealed mob as FOV updates → several `store_var` writes/turn. Add a dirty flag + save on run-save/quit or debounce. Perf.
- [P2][audit:S29] No round-trip tests for catalogs/profile — nothing asserts unlocked ≤ total, catalog serialize/deserialize, or appearance determinism-per-seed; a headless test would have caught the denominator bug. Framework hook.
- [P3][audit:S29] PlayerProfile double-saves per kill — `_on_mob_defeated` calls `_refresh_unlocks()` (which saves when changed) then unconditionally `_save_profile()` again (player_profile.gd:372-373) → up to 2 ConfigFile writes/kill. Perf.
- [P3][audit:S29] Per-proc object allocation for a name lookup — discovery_catalog.gd:72-80 builds a fresh WeaponEnchantment/ArmorGlyph on every proc just to read its display name; use a static id→name map. Perf.
- [P3][audit:S29] Appearance pools silently truncate if content outgrows them — item_appearance.gd guards with `mini()` (:55,62,69); currently balanced (14/14 potions, 14/14 scrolls, 11/11 rings) but a 15th potion/ring gets an empty appearance with no warning. Add a `push_warning` when ids > pool. Robustness.
- [P3][audit:S29] Unreferenced public getters — `Badges.get_unlock_info` (:103), `Badges.get_all_unlocked` (:96), `ItemCatalog.is_item_known` (:58) have zero repo-wide callers. Likely intended journal/extension API — wire into the Journal window or drop. Left in backlog (read as framework hooks, not auto-deleted).
- [P1][audit:S30] MessageLog is never reset or persisted between runs — `_entries` lives on the autoload for the whole process; `clear()` has no external caller (message_log.gd:55 only) and SaveManager never serializes the log. New-game/load carries the prior run's scrollback in, and `current_turn` (set from `TurnManager._round_count`, turn_manager.gd:269) can jump backward → out-of-order "--- Turn N ---" headers in game_log_display.gd. Fix: `MessageLog.clear()` + reset `current_turn=0` on new-game and load; optionally serialize last ~20 entries. (message_log.gd in TRUNCATED_FILES.txt — no autofix.) Behavioral.
- [P2][audit:S30] Boss-finale music path fully dead — `play_region_music(..., is_boss_finale)` + the caves/city/halls `boss_finale` tracks (audio_manager.gd:281-284, :61/:69/:77) are never triggered; sole caller game_scene.gd:493 always passes plain `is_boss=true`. SPD swaps to the finale track for boss phase 2 (DM-300/Dwarf King/Yog). Wire a boss-phase signal → pass `is_boss_finale=true`. Fidelity/addition.
- [P2][audit:S30] Procedural SFX fallback is entirely silent — all ~34 `_gen_*` helpers (audio_manager.gd:503-609) return `_gen_silent(0.05)` despite the header comment promising procedural tones; if `assets/spd/sounds/` is missing the game runs mute, and ~130 lines only alias silence. Implement real synthesis or collapse to one silent stub + fix the comment. Cleanup/robustness.
- [P3][audit:S30] `set_music_muted(false)` restarts the track from position 0 — unmute calls `_music_player.play()` after mute stopped it (audio_manager.gd:328-335); track jumps to the top and crossfade state isn't restored. Track playback position across mute. Behavioral.
- [P3][audit:S30] `_get_free_sfx_player()` always steals channel 0 when the pool is saturated (audio_manager.gd:364-369) — repeated same-channel cutoff under 8+ simultaneous SFX; use round-robin/oldest index. Perf/polish.
- [P3][audit:S30] SaveManager applies missing `music_volume` as `1.0` (save_manager.gd:642) but DEFAULT_SETTINGS + AudioManager default are `0.5` (save_manager.gd:608, audio_manager.gd:15) — louder-than-default music when the key is absent. Align the fallback. (In save_manager, adjacent.) Consistency.
- [P3][audit:S30] No tests for `_weighted_random_pick` (distribution) or MessageLog prune/`get_recent` boundary — both pure, easily testable once a GDScript harness lands. Framework hook.
- [P1][audit:S31] Client re-sanitizes the host's authoritative run config — `_client_receive_online_run_start` calls `_sanitize_run_config(config)` again (network_manager.gd:671), rebuilding party/player_infos from the client's own `lobby_players` instead of trusting the host. Stale/reordered lobby snapshot at run-start → different class-to-slot mapping = whole-run desync. Clients should consume host config verbatim. Correctness.
- [P1][audit:S31] Per-turn full-state snapshot broadcast — `OnlineSyncCoordinator.sync_snapshot` reliably RPCs the entire `level.serialize()` (full map + all mobs + heaps + items) + every hero every turn boundary (online_snapshot_utils.gd:4-17, online_sync_coordinator.gd:4-9). No delta tracking; large reliable payloads can head-of-line stall ENet. Diff via the existing `capture_*_snapshot_state` helpers; full snapshot only on transition/late-join. Optimization.
- [P2][audit:S31] `set_local_ready` wipes the host's profile icon — network_manager.gd:317 is the only `_upsert_lobby_player` call omitting `local_profile_icon_id`, so a host ready-toggle resets `profile_icon_id` to "warrior" and re-broadcasts it. Add the missing argument (matches :194/:295/:308). Behavioral (host-only cosmetic). Correctness.
- [P2][audit:S31] No tests on the pure codec layer — `OnlineActionCodec`/`OnlineEventCodec`/`OnlineSnapshotUtils` are static/RefCounted with no engine deps; add round-trip tests (make_item_ref↔resolve_item_ref, encode↔normalize action, serialize↔deserialize shop). Locks the online serialization contract. Framework hook.
- [P3][audit:S31] `NetworkManager` God-object (30+ public methods, gdlint max-public-methods) mixes lobby/transport/config/prefs; extract a LobbyState value object from transport. Framework hook.
- [P3][audit:S31] `apply_remote_mob_move` drops the move (no-op) if the client has no sprite for that mob yet (online_event_codec.gd:560-561) → teleport-on-reveal until next snapshot; and `handle_item_dropped_world_event` logs "drops" even when the item fails to deserialize (:603-624). Polish.
- [P3][audit:S31] No mid-run online persistence — `active_run_config`/`latest_run_snapshot` are runtime-only, cleared on `close_session` (network_manager.gd:119-121); host save+reload mid-run drops the session. Document the limitation or add host-side resume. Robustness.
- [P1][audit:S32] `CharSprite` `VisualState` system is fully dead — `add_visual_state`/`remove_visual_state`/`has_visual_state` + enum + `_process_state_addition/removal` (char_sprite.gd:280-334) have zero callers; SPD drives these via `Char.updateSpriteState()` each act. Buffs show no sprite feedback (Frozen/Invisible/Burning/Chilled/Marked etc. invisible). Add a `Char.update_sprite_state()` driver on buff add/remove + per turn. (char_sprite.gd in TRUNCATED_FILES.txt — no autofix.) SPD fidelity.
- [P2][audit:S32] `MobSprite.die()` (3.0s `FADE_TIME`) and `fall()` (pit spin+shrink) overrides are dead (mob_sprite.gd:237-252) — death routes through generic `CharSprite.play_death()` (0.5s) via SceneVisualCoordinator; SPD's slow dissolve and pitfall/chasm fall animations never play. Wire mob death/fall paths to the overrides. (mob_sprite.gd truncated.) Fidelity.
- [P2][audit:S32] Animation hooks unwired — `jump()` (Heroic Leap/Pitfall/teleport arc), `play_zap()` (wands), `turn_to()`, `HeroSprite.read()` (scrolls), `sprint()` (pure `pass` stub) have no gameplay callers (char_sprite.gd:226-268, hero_sprite.gd:102-111). Special moves fall back to the generic lunge or nothing. Wire from the relevant item/mechanic paths. Fidelity.
- [P2][audit:S32] Add `Char.update_sprite_state()` framework hook — the missing plug for the dead VisualState system; view stays passive, model pushes buff→visual-state on attach/detach/turn. Framework extraction.
- [P2][audit:S32] No procedural-texture cache in Char/MobSprite — every mob spawn rebuilds its 16×16 `Image`+`ImageTexture` in `_generate_sprite()` though `MOB_VISUALS`/shape drawing is deterministic per `mob_id`; `ItemSprite` already caches by key (item_sprite.gd:19,143-149). Add a `mob_id`-keyed static cache. (Only hit on sheet-load failure.) Optimization.
- [P3][audit:S32] `CharSprite._process(delta)` polls every frame on every sprite for sleep/emote bob even when idle (char_sprite.gd:98-108); `set_process(false)` unless sleeping/emoting. Perf.
- [P3][audit:S32] No tests for pure sprite mapping math — `_cell_to_world`/`place_at` cell→pixel and `sprite_index→(col,row)` region math (item_sprite.gd:132-136, plant_sprite.gd:109-115). Framework hook.
- [P2][audit:S33] `terrain_visuals.gd` is ~180/265 lines dead — no external caller of `TerrainVisuals.*`; `TileMapManager` uses only `_terrain_to_tile` + `clear_cache()`. Orphaned: `get_texture`/`_load_tile`/`_create_fallback`/`get_palette`/`get_stitched_water_tile`/`get_water_bg_texture` + `_palettes`/`_water_region_file`/`_water_cache`. Renderer builds tiles straight from the SPD sheet. (terrain_visuals.gd in TRUNCATED_FILES.txt — no autofix.) Dead-code removal.
- [P2][audit:S33] `TileMapManager` couples to the private `TerrainVisuals._terrain_to_tile` dict (tile_map_manager.gd:242) as its only live use of that class, while TerrainVisuals' public API is entirely dead. Expose a public `terrain_tile_index()` accessor or move the map onto SPDTileset/TileMapManager. Coupling.
- [P2][audit:S33] Water-bg path table + 32→16 center-crop duplicated between `TileMapManager._get_water_bg_image_texture` (tile_map_manager.gd:166-187) and dead `TerrainVisuals.get_water_bg_texture`/`_water_region_file`. Collapse to one source. Dedup.
- [P2][audit:S33] No unit tests for the two pure deterministic funcs — `_compute_water_edge_mask` (tile_map_manager.gd:259-280) and fog state-selection in `_set_fog_pixel` (fog_of_war.gd:157-171). Cheap regression anchors before any tile-index refactor. Tests.
- [P3][audit:S33] `FogOfWar.reveal_all()` (fog_of_war.gd:103-109) is a dead duplicate — magic mapping runs through `Level.reveal_all()`; zero callers of the fog method. Dead code.
- [P3][audit:S33] `FogOfWar` never mirrors edge cells into its 1-cell UNSEEN border → visible map-boundary cells bilinear-fade toward black under LINEAR filtering (faint edge halo) (fog_of_war.gd:124-127,145-147). Clamp/replicate edge state. Cosmetic.
- [P3][audit:S33] `render_changed()` enqueues water neighbors via DIRS_4 without row-wrap guard/dedup (tile_map_manager.gd:76-80) → redundant `_update_tile` at column 0/31. Harmless but wasteful. Perf.
- [P3][audit:S33] `FogOfWar.set_dark()` runtime setter is dead — DARK feeling applied once at `setup` (game_scene.gd:439-440). Fine now; note if feeling ever mutates mid-level. Dead code.
- [P2][audit:S34] `EffectManager._cell_to_world` (effect_manager.gd:135) is a third hand-rolled cell→pixel-center copy hardcoding literal `16`, duplicating `TileMapManager.cell_to_world` (uses `TILE_SIZE`) and `CharSprite._cell_to_world`. Identical today, but a tile-size change would drift effects off sprites. Route all three through one source of truth. Coupling.
- [P2][audit:S34] `EffectManager.screen_flash` (effect_manager.gd:118) bypasses the effect pool — no `MAX_EFFECTS` check, no `_active_effects` increment. One caller today (retribution, game_scene.gd:2286); a rapid loop can stack unbounded full-viewport ColorRects. Count it like other spawns or reuse one persistent flash rect. Robustness.
- [P3][audit:S34] `LightningEffect._toggle_flash` (lightning_effect.gd:57) regenerates the whole bolt via `_generate_segments()` on every flash (2×/bolt) — redundant re-randomization/alloc. (lightning_effect.gd in TRUNCATED_FILES.txt — no autofix.) Perf.
- [P3][audit:S34] `ParticleBurst`/`ProjectileEffect` are the only two effects still on per-frame `_process`+`queue_redraw` (others moved to Tween). Fine under the 50-cap; unify to Tween-driven motion if a future polish pass wants consistency. Optimization.
- [P3][audit:S34] No SPD-parity emitters (hit `Splash`/blood, `Wound`, typed `MagicMissile` wand bolts) — port uses a generic procedural set. Functionally adequate; polish addition. SPD ref: `Emitter`/`Splash`/`MagicMissile`. Addition.
- [P3][audit:S34] No tests over effect geometry helpers (`_cell_to_world`, `_generate_segments`, projectile `t` clamping) — a tiny pure-function test would lock the cell→world contract shared with sprites/tilemap. Tests.
- [P1][audit:S35] Boss HP bar is fully dead: `boss_hp_bar.gd` (175 lines) + hud handlers (hud.gd:249-251, 403-415) subscribe `boss_fight_started/damaged/defeated` but NO emitter exists repo-wide — every boss fight shows no HP bar. Emit `boss_fight_started` on boss/arena activation, `boss_damaged` from boss `take_damage`, `boss_defeated` on death. Couples S09/S27. Correctness/fidelity.
- [P2][audit:S35] Minimap live FOV never refreshes on move: minimap.gd:244-245 reads `level.get("visible_cells")`/`level.visible_cells` but the level property is `visible` (level.gd:22, no `visible_cells`) → dead branch, `_visible_cells` frozen after level entry so currently-lit dimming + visible-gated mob dots go stale. Read `level.visible`. Correctness.
- [P2][audit:S35] `level_changed` order-dependent minimap clear: both hud._on_level_changed (hud.gd:378-391, populates via update_map) and minimap._on_level_changed (minimap.gd:274-282, blanks image + clears arrays) subscribe the same EventBus.level_changed; if minimap's clear fires last it wipes the fresh map. Pick one owner (minimap self-populates on level_changed, drop HUD's manual update_map). Coupling.
- [P2][audit:S35] BossHPBar hard-codes 1280 for centering (boss_hp_bar.gd:42) with no size_changed listener → off-center on any non-1280 viewport and after resize. Center off viewport rect + subscribe size_changed. Robustness.
- [P2][audit:S35] Buff icons render as flat ColorRects; `buffs.png` (status_pane.gd:41 BUFFS_PATH) declared but unused. SPD draws real 16×16 buff sprites indexed per buff. Atlas-slice buffs.png by buff icon index (reuse toolbar `_get_item_texture` pattern). Addition/fidelity.
- [P2][audit:S35] StatusPane `_process` (status_pane.gd:67-94) runs every frame unconditionally, re-setting portrait modulate to WHITE even when healthy. Early-out when HP ratio ≥ 0.334 and already reset; only interpolate while low. Perf.
- [P3][audit:S35] StatusPane `_update_buffs` (status_pane.gd:510-528) frees+recreates all buff icons on every `hero_stats_changed`. Diff/reuse or pool icons. Perf.
- [P3][audit:S35] HUD `_on_hero_moved` (hud.gd:398-400) is a connected `pass` no-op (minimap owns hero_moved) — hud.gd:246 still connects it; drop the subscription. Dead wiring.
- [P3][audit:S35] Dead status-pane asset consts STATUS_PANE_PATH / HERO_ICONS_PATH (status_pane.gd:39-40) referenced only at declaration — SPD status-frame/hero-icon art never wired. Wire or delete. (Left this run to not pre-empt the buff-icon/portrait work.) Dead code.
- [P3][audit:S35] No headless tests over HUD pure state math: party-row marker/format (hud.gd:471-495), online-state text (_refresh_online_state), minimap `_terrain_to_color`. Add a viewport-free test harness. Tests.
