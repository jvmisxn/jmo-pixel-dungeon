# Change Log

## 2026-07-20

- Tags: buffs, weakness, source-fidelity, tests
- Corrected the Weakness audit item instead of landing a non-source STR penalty. Upstream current Shattered Pixel Dungeon keeps `Weakness` as a flavour debuff, applies its combat effect in `Char.attack()` as `dmg *= 0.67f`, and does not subtract Weakness inside `Hero.STR()`; the port already had the outgoing-damage multiplier, so the proposed `STR_PENALTY`/equipment-encumbrance change would have stacked a divergence. Updated `Weakness` to use SPD's 20-turn duration and removed the stale strength-penalty comments. Extended `test_combat_buffs.gd` to lock the 20-turn duration and 67% outgoing-damage behavior.

- Tags: rogue, search, secrets, source-fidelity, tests
- Restored Rogue's SPD-style wider intentional search reach. Upstream Shattered Pixel Dungeon gives Rogue a base search distance of 2 tiles while other classes search 1 tile before Wide Search/Foresight modifiers; the port always searched only adjacent cells, so Rogues did not actually detect traps/secret doors more easily despite the class perk text. `Hero._do_search()` now passes a class-specific radius into `Door.search()`, and `Door.search()` scans a bounded square radius without edge wrapping while preserving the default adjacent behavior for non-Rogues. Added `test_hero_search_radius.gd` for Warrior adjacent-only and Rogue two-tile secret door/trap reveal. Local `git diff --check`, Godot import, and full headless suite passed (1411 checks, 0 failures, Godot 4.7.1).

- Tags: mobile, hud, landscape, readability, tests
- Gave the mobile landscape status strip enough vertical room for its HP/XP readout. The compact landscape pane stayed width-capped for safe-area breathing room, but its 76px height left only a 60px overlay budget for controls whose bar stack wants about 64px, making status information feel cramped or uncovered on landscape phones. `HUD` now uses an 84px landscape status height and slightly tighter mobile overlay spacing, preserving portrait and toolbar behavior. Extended `test_mobile_hud_input.gd` with an 852x393 safe-area regression that asserts the status pane stays compact while leaving readable height for HP/XP bars. Local `git diff --check`, Godot import, and full headless suite passed (1406 checks, 0 failures, Godot 4.7.1).

- Tags: hero, xp, level-cap, bless, source-fidelity, tests
- Restored SPD-style max-level XP cap behavior. `Hero.earn_xp()` now keeps normal partial XP below the cap threshold, but when a level-30 hero crosses the next XP requirement it resets `xp` to 0 and applies `Bless` as the original "surge of power" cap reward instead of serializing unbounded overflow XP. Added `test_hero_xp_cap.gd` for exact-threshold and large-overflow grants. Local full headless suite passed (1405 checks, 0 failures, Godot 4.7.1).

## 2026-07-19

- Tags: spinner, mob-ai, amok, source-fidelity, tests
- Restored part of Cave Spinner's base hunting-state parity while preserving its web/poison specialization. Fable identified Spinner AI as a higher-value source-fidelity target than another headless-only mobile HUD tweak: the custom `_act_hunting()` path ignored base Amok retargeting and chased an unseen target's live position instead of its last-known cell, letting spinners track through walls/invisibility. Spinner hunting now retargets under Amok, updates/uses `target_pos` like base `Mob._act_hunting()`, gives up when it reaches the last-known cell without sight, and still shoots webs/attacks adjacent visible targets as before. Added `test_spinner_ai.gd`. Local `git diff --check`, Godot import, and full headless suite passed (1392 checks, 0 failures, Godot 4.7.1).

- Tags: mobile, hud, toolbar, controls, readability, tests
- Improved ultra-narrow mobile toolbar readability and safe-area sizing without changing toolbar actions. The compact mobile toolbar already squeezed to fit cramped portrait/foldable widths, but Wait/Search could still become hard to read once the width fitter compressed text-heavy buttons. Narrow compact mode now uses single-letter `W`/`S` labels with full tooltips, while roomier compact and desktop labels are unchanged. Fable also removed a redundant pre-layout toolbar width update that used stale raw width before `_layout_toolbar()` applied the safe-area-adjusted width, eliminating a transient wrong-width pass on notched landscape layouts. Extended `test_mobile_hud_input.gd` across both sides of the 430px compact breakpoint. Local `git diff --check`, Godot import, and full headless suite passed (1374 checks, 0 failures, Godot 4.7.1). Fable approved the final diff as a safe mobile layout/control adaptation.

- Tags: mobile, touch, camera, zoom, controls, tests
- Restored mobile pinch zoom after adding one-finger look-around. Pinch tracking previously lived only in `GameCamera._unhandled_input()`, while the new touch router handled look-around in `GameScene._input()`, so mobile/browser event routing could let drag work while starving the camera's zoom path. `GameScene` now forwards gameplay touch presses, releases, and drags directly to `GameCamera`'s touch handlers, consumes two-finger gestures before they become look-pan or tap-to-move input, and leaves HUD/modal touches isolated. Single-finger look drags also keep camera touch tracking current so a later second finger starts pinch from the live position. Extended touch-routing and camera zoom regressions to cover scene-routed pinch in/out, no one-finger pan fallback, and no movement submission after pinching. Local `git diff --check`, Godot import, and full headless suite passed (1372 checks, 0 failures, Godot 4.7.1).

- Tags: vertigo, movement, grid, source-fidelity, tests
- Fixed Vertigo's random movement edge-wrap. `Vertigo.randomize_direction()` was adding flat `DIRS_8` offsets without guarding map columns, so a dizzy character at the left or right edge could be shoved to a wrapped cell on the opposite edge of an adjacent row. The buff now samples only true in-bounds 8-neighbours and falls back to the intended destination if none are passable. Added `test_vertigo_edge_wrap.gd`. Local full headless suite passed (1358 checks, 0 failures, Godot 4.7.1).

- Tags: turn-manager, speed, buffs, source-fidelity, tests
- Restored live speed re-querying in the turn scheduler. `TurnManager` still cached actor speed at registration, so buffs or ring passives that changed `get_speed()` after registration only affected cooldown math if some caller remembered to call `refresh_speed()`. `spend_energy()` now re-queries the actor's current speed before charging cooldown, matching SPD's live per-action timing model while preserving the existing safety floor for zero-speed actors. Extended `test_speed_modifiers.gd` with a scheduler regression for speed-up and slow-down changes after registration. Local full headless suite passed (1354 checks, 0 failures, Godot 4.7.1).

- Tags: mobile, touch, camera, zoom, controls, tests
- Added mobile dungeon look-around via one-finger drag while preserving tap-to-move. Single-finger touch drags now enter a camera-look gesture after a small movement threshold, pan the `GameCamera` with a persistent look offset, cancel auto-walk, consume the release, and avoid submitting a movement/attack tap. The next normal gameplay tap recenters the camera back to hero-follow before acting. Also added missing pinch-in coverage so mobile zoom-out remains tested alongside zoom-in. Extended `test_touch_gesture_routing.gd` and `test_game_camera_mobile_zoom.gd`. Local `git diff --check`, Godot import, and full headless suite passed (1338 checks, 0 failures, Godot 4.7.1).

- Tags: mobile, hud, buffs, controls, tests
- Made the mobile HUD buff strip update live when local hero buffs are added, refreshed, or removed. The previous compact HUD drew a separate mobile `BuffIcon` row, but only rebuilt it during broad HUD/stat refreshes, so poison/chill/haste/etc. could appear stale or invisible until another unrelated HUD update happened. `HUD` now tracks the local hero's `buff_added`/`buff_removed` signals, refreshes on `status_effect_applied`, and immediately reflows the party controls/log around the changing buff row. Fable also caught and fixed the wrapped-row case: when many active buffs spill onto a second `HFlowContainer` row, the strip now reserves the actual multi-row height instead of letting lower controls overlap it. Extended `test_mobile_hud_input.gd` with add/remove signal and wrapped-buff-row regressions. Local `git diff --check`, Godot import, and full headless suite passed (1331 checks, 0 failures, Godot 4.7.1).

- Tags: mobile, hud, buffs, source-fidelity, tests
- Restored active buff visibility on the mobile HUD. The compact HUD path was drawing HP/XP/depth through its own status overlay while keeping the full `StatusPane` hidden, so active buffs rendered by `StatusPane` were not visible on mobile. `HUD` now builds a mobile-only `BuffIcon` strip under the top status panel, refreshes it from the local hero's active buffs, and includes it in the top-control spacing so party controls, online turn labels, and the game log stay below it instead of overlapping. Extended `test_mobile_hud_input.gd` with a portrait regression covering visible buff icons and party-row reflow. Local `git diff --check`, Godot import, and full headless suite passed (1326 checks, 0 failures, Godot 4.7.1). Fable approved this as a narrow mobile layout adaptation that improves SPD status fidelity.

- Tags: mobs, demon-halls, level-generation, source-fidelity, tests
- Matched Demon Halls regular mob rotation to upstream SPD's depth-ramped `MobSpawner.standardMobRotation`: depth 21 now favors Succubi with one Evil Eye, depth 22 is even, depth 23 introduces Scorpios, and depth 24+ weights Scorpios heavily. Removed Ripper Demons from ordinary Halls room spawns; in SPD they come from Demon Spawners/Yog rather than the standard room rotation. `MobFactory.create_boss()` also now returns `null` with a warning for unknown depths instead of a statless base `Mob`, matching `create_mob()`'s unknown-id contract. Added `test_mob_factory.gd`. Local `git diff --check`, Godot import, and full headless suite passed (1324 checks, 0 failures, Godot 4.7.1). Fable final review approved the diff.

- Tags: mobile, title, layout, controls, tests
- Fixed the portrait title menu clipping shown on phone screenshots when a save exists. `TitleScene` now stacks `Continue` under `New Game` on narrow portrait layouts instead of forcing both into one split HBox row, keeps the split row in landscape/desktop, and hard-caps portrait buttons to a narrower phone-safe width so the exported web canvas cannot push them into the right edge. Extended `test_title_mobile_layout.gd` to cover the portrait stack behavior, landscape split behavior, reparenting path, and both portrait menu insets. Local `git diff --check`, Godot import, and full headless suite passed (1315 checks, 0 failures, Godot 4.7.1).

- Tags: mobile, hud, landscape, controls, tests
- Kept the mobile landscape status pane compact instead of stretching across the full safe viewport width. `HUD` now caps landscape mobile status width at 360px while preserving portrait's full-width status strip, giving the dungeon view and co-op/control rows more breathing room on notched landscape layouts. Extended `test_mobile_hud_input.gd` with an 852x393 safe-area regression. Local `git diff --check`, Godot import, and full headless suite passed (1309 checks, 0 failures, Godot 4.7.1). Fable reviewed the change as a low-risk mobile layout adaptation, not a source-gameplay divergence.

- Tags: hud, buffs, source-fidelity, tests
- Replaced the status pane's flat ColorRect buff markers with the existing `BuffIcon` component, restoring readable lettered/colored buff indicators and duration tooltips instead of anonymous color squares. `BuffIcon` now refreshes its tooltip when a buff is assigned, and `StatusPane` removes stale buff children immediately before rebuilding so same-frame refreshes don't leave old icons in the flow container. Extended `test_status_pane.gd` with a headless fixture that verifies active buffs render as `BuffIcon`s with duration text. Local `git diff --check`, Godot import, and full headless suite passed (1308 checks, 0 failures, Godot 4.7.1). Fable triage noted S25 mob-action observe coalescing remains the higher-value next fidelity/perf target; this patch is a smaller HUD readability/fidelity fix.

- Tags: traps, curses, equipment, source-fidelity, tests
- Restored Cursing Trap's live equipment-cursing path. The port was still calling a missing `Hero.get_equipped_items()` helper, so the trap could never curse gear and always fell back to `Hex`. `CursingTrap` now reads the hero's live `Belongings` slots, prioritizes weapon/armor without existing enchantment/glyphs, skips Mage's Staff like SPD, marks the selected item cursed and curse-known, and adds an existing weapon curse enchantment when a bare non-staff weapon is cursed. Added `test_cursing_trap.gd` covering bare weapon curse enchantment, preserving an existing weapon enchantment, armor curse selection, Mage's Staff exclusion, and the no-equipment Hex fallback. Local full headless suite passed (1306 checks, 0 failures, Godot 4.7.1). Remaining fidelity tail: armor curse glyphs are not implemented yet, so armor traps cannot inscribe a curse glyph.

- Tags: mobile, hud, multiplayer, controls, tests
- Kept four-hero mobile co-op party controls inside narrow portrait viewports. `HUD` now fits party focus button widths to the safe-area-adjusted mobile row instead of always using 86px buttons, so 320-360px portrait layouts no longer clip the rightmost player controls. Party-row refresh also removes old buttons from the row immediately before queue-freeing them, preventing stale controls from affecting same-frame resize/refresh layout math. Extended `test_mobile_hud_input.gd` with a 320px four-hero regression. Local full headless suite passed (1282 checks, 0 failures, Godot 4.7.1).

- Tags: mobile, hud, multiplayer, controls, tests
- Kept the online/co-op turn-state label from overlapping the toolbar on ultra-short mobile landscape layouts. `HUD` now tracks whether online state text is logically active separately from whether it currently fits on-screen, so cramped layouts can hide the label without making it sticky-hidden after rotate/resize; hidden labels also no longer reserve game-log space. Extended `test_mobile_hud_input.gd` with ultra-short hide and roomier-resize reappearance coverage. Local `git diff --check`, Godot import, and full headless suite passed (1281 checks, 0 failures, Godot 4.7.1).

- Tags: mimic, loot, level-generation, source-fidelity, tests
- Fixed regular-floor mimic generation so the item rolled for a mimic heap is stored inside the spawned `Mimic` and drops when the mimic dies, instead of being silently deleted. The spawned mimic now also runs `scale_to_depth(depth)`, matching the floor-scaled risk/reward behavior intended by SPD-style mimic chests. Added `test_mimic_loot.gd` to cover prize storage, depth scaling, level registration, and death drops. Local `git diff --check`, Godot import, and full headless suite passed (1279 checks, 0 failures, Godot 4.7.1). Fable selected this as the next narrow source-fidelity fix after confirming recent mobile HUD work left only real-device verification follow-ups.

- Tags: gladiator, subclass, combat, source-fidelity, tests
- Added a focused Gladiator combo regression after Fable flagged the stale S06 audit item as the next highest-value gameplay-fidelity target. Current source already routes landed melee attacks through `Char.attack()` -> `on_damage_dealt` and `GladiatorCombo.modify_damage()`, so the test now locks the intended simplified behavior: subclass application attaches the tracker, successive melee hits build combo, the finisher threshold boosts damage, the finisher starts a new chain, and combo state serializes. Marked the backlog item done with a follow-up noting that this remains an automatic simplified finisher rather than SPD's explicit combo-move picker. Local `git diff --check`, Godot import, and full headless suite passed (1273 checks, 0 failures, Godot 4.7.1).

- Tags: mobile, hud, toolbar, quickslots, controls, tests
- Tightened ultra-narrow mobile toolbar behavior for sub-300px safe-area widths. Compact toolbar paging now preserves the currently visible quickslot group when a resize changes the page size, drops to one quickslot per page only at ultra-narrow widths, hides Search before shrinking Bag/Menu/pager into unusable controls, and uses minimum-size fallback hit boxes after relayout so touch taps still activate visible buttons. Extended `test_mobile_hud_input.gd` with quickslot-page preservation, ultra-narrow Search hiding, and fallback hit-testing regressions. Local `git diff --check`, Godot import, and full headless suite passed (1258 checks, 0 failures, Godot 4.7.1).

## 2026-07-18

- Tags: mobs, respawn, vision, source-fidelity, tests
- Tightened regular-floor respawn placement so replacement mobs respect each hero's effective sight radius, not just the base `ConstantsData.VIEW_DISTANCE`. Offscreen mob respawn is intentional SPD-style balance, but the previous guard could allow a spawn at distance 9-10 for a Huntress or other extended-sight hero because `_is_valid_respawn_cell()` rejected only visible cells and cells within the base radius. Respawn placement now asks `hero.get_view_distance()` when available, preserving normal offscreen respawns while preventing visible/extended-vision pop-in. Extended `test_mob_spawn_positions.gd` with a Huntress sight regression and marked the stale backlog respawn item done.

- Tags: mobile, zoom, web-export, touch, tests
- Restored mobile web pinch zoom after the deploy shell patch had re-enabled browser-owned gestures. The GitHub Pages workflow was rewriting Godot's exported viewport from `user-scalable=no` to `user-scalable=yes` and setting `touch-action: auto`, so mobile browsers could consume two-finger pinches as page gestures before `GameCamera` received the touch stream. The web shell now keeps page zoom disabled and canvas touch handling claimed by Godot (`touch-action: none`), and `GameCamera._claim_web_canvas_touch_gestures()` now replaces the viewport content with one consistent no-page-zoom policy instead of appending potentially contradictory flags at runtime. Added a workflow regression to `test_game_camera_mobile_zoom.gd`.

- Tags: docs, mobile, combat-input, combat-pacing, follow-up
- Documented the remaining playtest follow-ups from the mobile attack/pacing fix in `backlog.md` and `active-context.md`: real-device verification for tap attacks/damage numbers, windowed/device tuning against SPD feel for visible mob pacing and possible action animation gating, and a separate online-only duplicate-damage-feedback investigation path if co-op still reproduces stacked numbers.

- Tags: mobile, combat-input, turn-pacing, source-fidelity, tests
- Fixed mobile tap attacks double-submitting through synthesized mouse events and restored readable visible-mob turn pacing. The touch path already suppressed generated mouse events in `_input`, but `_unhandled_input` could still receive a delayed left-click after `_handle_touch_tap()` had submitted the real attack, making a mobile tap appear to hit twice and stack feedback at the same cell. `GameScene._unhandled_input()` now applies the same synthesized-touch guard before dungeon click routing, and the suppression window is 1s to cover slower mobile/browser click synthesis. Input handled-marking is routed through a null-safe helper so bare headless scene tests stay clean under Godot 4.5. `TurnManager.MOB_ACTION_DELAY` is back to `0.1s`, matching SPD's short movement interval so visible mob moves/attacks do not all snap through instantly after the hero acts. Added a touch-routing regression for the tap-then-synthetic-click path.

- Tags: mobile, zoom, camera, combat-feedback, damage-numbers, tests
- Restored mobile settings zoom and de-duplicated scene combat feedback wiring. The settings window previously wrote `zoom_level` as an undeclared dynamic property and set `Camera2D.zoom` directly; `GameCamera`'s smoothing loop still held its old private `_target_zoom`, so the next camera update could pull the view back and make zoom-in appear broken. `GameManager` now owns and persists `zoom_level`, `GameCamera` initializes from that saved value, `WndSettings` applies zoom through `GameCamera.set_zoom_level()` so both immediate zoom and the smoothing target stay in sync, and the title settings Save button now calls the real display-settings persistence path instead of an old no-op `GameManager.save_settings()` check. Also made `GameScene._connect_signals()` idempotent for all EventBus hookups, preventing repeated setup paths from connecting damage handlers more than once and stacking multiple damage numbers on a single hit. Added regressions to `test_game_camera_mobile_zoom.gd` and `test_event_bus.gd`. Local Godot import and full headless suite passed (1246 checks, 0 failures, Godot 4.7.1).

- Tags: mobile, hud, game-log, safe-area, tests
- Fixed the remaining short-landscape mobile HUD overlap that Fable identified in the existing `LayoutHud` harness. `_layout_game_log()` only shrank the mobile game log when the vertical gap between top controls and the safe-area-adjusted toolbar was positive; on ultra-short landscape viewports, the log kept its 96px default height and could overlap the toolbar. The log now clamps to the actual available gap and hides when no usable vertical space remains, preserving the action/status controls instead of drawing a floating log over them. Extended `test_mobile_hud_input.gd` with an ultra-short landscape regression that asserts the log never crosses the toolbar and collapses cleanly when the vertical gap is gone. Local `git diff --check`, Godot import, and full headless suite passed (1230 checks, 0 failures, Godot 4.7.1). Fable triage selected the fix and final diff review approved it.

- Tags: quests, sad-ghost, rewards, enchantments, source-fidelity, tests, audit-S21
- Restored Sad Ghost enchanted reward parity. SPD generates a pending weapon enchantment and armor glyph up front, rolls the reward magic chance, stores the magic separately so the choice preview does not reveal it, then applies only the chosen item's magic after confirmation. The port already rolled and saved `reward_enchanted`, but never consumed it, so enchanted ghost rewards were always plain. `SadGhost` now stores pending `reward_weapon_enchantment` / `reward_armor_glyph`, serializes them separately from `reward_weapon` / `reward_armor`, and `_apply_enchanted_reward()` attaches the stored magic only to the claimed reward. Added `test_sad_ghost_rewards.gd` and registered it in the headless runner.

- Tags: mobile, hud, safe-area, controls, tests
- Kept the mobile landscape game log out of horizontal safe areas. The toolbar, status panel, party row, and online-state label already honored mobile landscape left/right safe-area insets, but `HUD._layout_game_log()` still anchored the floating log at raw `HUD_MARGIN`, so notched landscape/browser layouts could hide log text under the unsafe edge. The log now uses the same mobile safe-area width budget and x-offset as the rest of the HUD while preserving desktop behavior (`safe_left/right = 0`). Extended `test_mobile_hud_input.gd` to cover normal and short landscape safe-area log placement. Local `git diff --check`, Godot import, and full headless suite passed (1214 checks, 0 failures, Godot 4.7.1). Fable final diff review approved.

- Tags: persistence, combat, source-fidelity, ui, inventory, tests, audit-S03, audit-S36
- Restored the original SPD-style inheritance shape for shared character combat save state. Fable verified WS4 as the highest-value remaining fidelity fix: `Hero.serialize()` and `Mob.serialize()` were each hand-copying combat fields, and they had drifted — mobs saved `shielding`/`flying`/`invisible`/`paralysed`, while heroes silently dropped them. `Char` now owns `serialize_char()`/`deserialize_char()` for hp/hp_max/ht/shielding/str/base_speed/combat skills/damage roll/armor/alive/flying/invisible/paralysed plus buffs; `Mob` delegates its shared block through `super`, and `Hero` uses the same helper while preserving Ring of Might's base-stat override so passive bonuses are not baked into saves. Added `test_char_combat_serialization.gd` covering Hero and Mob combat round-trips. Also fixed the S36 item-window crash where dropping an equipped ring checked the nonexistent `belongings.ring`; `_action_drop()` now mirrors unequip handling with `ring_left`/`ring_right`, covered by `test_wnd_item_drop_slots.gd`. Local `git diff --check`, Godot import, and full headless suite passed (1212 checks, 0 failures, Godot 4.7.1). Web/mobile export was not run; this was a save/UI-control fix, not a browser layout change.

- Tags: buffs, persistence, source-fidelity, tests, audit-S06
- Fixed Charm/Terror source persistence. Both buffs already serialized `source_id`, but unlike `Dread` neither restored it during `deserialize()`, so a reload reset the charmer/terror source to `-1` and broke the SPD-style "cannot harm / flee from the source" relationship after loading. `Charm` and `Terror` now mirror `Dread` by calling `super.deserialize(data)` and restoring `source_id`. Added `test_status_source_serialization.gd` to cover direct buff deserialization and full `Char` buff round-trips. Local `git diff --check`, Godot import, and full headless suite passed (1180 checks, 0 failures, Godot 4.7.1).

- Tags: mobile, hud, toolbar, controls, tests
- Fixed two narrow mobile HUD/control layout failures. Fable's toolbar probe showed the compact mobile toolbar still needed 342px even after the quickslot pager work, so devices or landscape safe-area insets with only 280-320px usable width could clip the centered Bag/Menu edge controls. `Toolbar._apply_button_labels()` now computes the visible minimum width and scales compact button/quickslot/pager widths only when the toolbar would overflow, preserving the mainstream centered layout while keeping core controls tappable on foldable-width viewports. Also extracted `HUD._layout_game_log()` so short landscape mobile viewports shrink the floating game log to the space between the status/party/online labels and the safe-area-adjusted toolbar instead of overlapping either. Extended `test_mobile_hud_input.gd` with 280px/320px toolbar fit checks and an `852x320` landscape log-clearance case. Local `git diff --check`, Godot import, and full headless suite passed (1174 checks, 0 failures, Godot 4.7.1). Fable final diff review approved; web export/Playwright was not run locally because 4.7.1 web export templates are unavailable.

- Tags: mobile, hud, multiplayer, controls, tests
- Tightened mobile co-op HUD vertical layout so party focus controls and the online-state label no longer collide with the floating game log in landscape. The prior mobile layout used large fixed y-offsets from the top margin for `PartyRow` and `OnlineStateLabel`; on a short landscape viewport (`852x393` with safe-area insets), the label landed at y=196 while the log began at y=198, making turn/status text overlap the log and contributing to the "mobile HUD is broken/not visible enough" playtest bug. `HUD._apply_responsive_layout()` now anchors the party row immediately below the mobile status panel and places the online label below that row, preserving desktop placement and existing safe-area x/toolbar logic. Extended `test_mobile_hud_input.gd` to assert portrait/landscape controls sit below the status panel, remain inside safe-area width, and keep the landscape online label clear of the game log. Local `git diff --check`, Godot import, and full headless suite passed (1167 checks, 0 failures, Godot 4.7.1).

- Tags: docs, reconciliation, backlog, next-steps, system-audit
- Reconciled the stale planning docs (`system-audit/NEXT-STEPS.md`, `backlog.md`) against current `src/` + `tests/` + this change-log (docs-only; no code touched). `NEXT-STEPS.md` still carried a "Status (2026-07-15): WS2 in progress" header and a quick-wins/execution-order section that would have sent a future agent to redo already-landed work. Rewrote its Status block, quick-win status list, and TL;DR execution order to reflect source-verified reality: WS1/WS2/WS2.5/WS3/WS5/WS6/WS9/WS10/WS11 are landed, and the real remaining priorities are **WS4** (`Char.serialize_char()/deserialize_char()` combat-block serializer — still absent, the last big save-contract hole) → **WS7** (Corruption still Amok not `CorruptionBuff`, Warding spawns no sentry, no shared `Balance.normal_int_range`) → **WS8** (inert paralytic/fire trap buffs, unpooled traps, no mob respawn) → **WS12/WS13**. Also flipped 10 `backlog.md` items completed *after* the previous reconciliation from OPEN to **[DONE]**, each verified in source + its committed test: S03 bell-curve `damage_roll` (`char.gd:71-77`, `test_damage_roll_distribution.gd`) and surprise hits (`char.gd:154-157`, `test_surprise_attacks.gd`); S05 swarm split halving (`swarm.gd:41-48`, `test_swarm_split.gd`); S07 mob under-spawn loop (`regular_level.gd:398-414`, `test_mob_spawn_positions.gd`); S09 Halls trap override (`halls_level.gd:48`, `test_halls_traps.gd`); S23 targeting input-swallow (`input_coordinator.gd:55-58`, `test_targeting_input.gd`); S27 + S35 boss HP-bar signal emitters (`mob.gd:261,410,476`, `test_boss_hp_signals.gd`); S30 MessageLog reset on new-game/load (`game_manager.gd:103-104`, `save_manager.gd:99-100`); S35 minimap live FOV (`minimap.gd:245`, `test_minimap_refresh.gd`). Verified-still-OPEN quick wins left intact with notes: #2 the 7 S36 findings are still not filed into backlog under an `[audit:S36]` tag, #8 charm/terror `deserialize`, #12 client re-sanitizes host run config (`network_manager.gd:672`), #13 `item_catalog` typed-dict coercion, #14 `wnd_item._action_drop` still reads the non-existent `belongings.ring` (`:284`), #15 ghost double `mob_defeated` subscribe. `git diff --check` clean; docs-only, no game code changed.

- Tags: multiplayer, network, run-config, host-authority, source-fidelity, tests
- Fixed a host/client run-config desync in online co-op. `NetworkManager._client_receive_online_run_start(config)` re-ran `_sanitize_run_config(config)` on the client, and that helper rebuilds `party_classes`/`player_infos` from the receiver's own `lobby_players` snapshot (only falling back to the passed config when local lobby is empty). The host already sanitizes the config in `start_online_run` before broadcasting it (and `_server_register_player` resends the same `active_run_config` to late joiners), so re-sanitizing on the client discarded the host's authoritative class-to-slot mapping in favor of the client's local lobby state. Any divergence between host and client lobby snapshots at run start — ordering, an in-flight class change, or a peer whose registration hadn't fully propagated — produced a different class-to-slot mapping per client and desynced the run. `_client_receive_online_run_start` now adopts the host payload verbatim via `config.duplicate(true)` (matching the deep-copy pattern of the sibling `_client_receive_*` handlers) and no longer calls `_sanitize_run_config` on the receive path; the host stays the sole sanitizer, preserving host authority. Added `test_network_run_config_authority.gd` (registered in the runner): with a client whose local `lobby_players` deliberately diverges from the host, the emitted `online_run_start_requested` config must match the host's `party_classes`, `chosen_class`, `run_seed`, and per-slot `player_infos`, and be an isolated deep copy. The test was confirmed to fail against the old re-sanitize behavior (3 failures) and pass with the fix. Full local headless suite green (1143 checks, 0 failures, Godot 4.7.1); `git diff --check` clean.

- Tags: wands, corruption, warding, mobs, allies, ai, source-fidelity, tests
- Gave Wand of Corruption and Wand of Warding real effects backed by a shared Mob ally mechanism. Both wands were cosmetic: `WandOfCorruption.on_zap` set a bogus `alignment = "ally"` string (Mob has no `alignment` field, so the write was a silent no-op) and pinned a **permanent Amok** buff, which makes the "corrupted" mob attack the nearest character — frequently the hero — the opposite of an ally; the purpose-built `CorruptionBuff` class existed but had zero callers and its `on_attach` called a nonexistent `set_alignment`, while its `on_detach` re-entered `take_damage` (recursive death). `WandOfWarding.on_zap` only appended to a `_sentry_positions` array and printed a message — no sentry actor ever existed, so nothing zapped anything. Fix adds a gated ally mode to base `Mob` (`is_ally`/`ally_hero`), whose whole surface is behind `if is_ally` so non-ally mobs are untouched: `act()` routes allies to a new `_act_ally()` that mirrors the proven `SummonedElemental` loop (attack nearest visible hostile mob via `_find_nearest_enemy_mob()`, else follow the hero, else wander — never targeting the hero); `take_damage` returns 0 for hero-sourced friendly fire; `is_ally` is serialized and `ally_hero` re-linked from `GameManager.hero` on load. `CorruptionBuff` was rewritten to flip the target through that mechanism (`is_ally=true`, `xp_value=0`, clears Amok/Terror/Dread, no recursive detach damage), and `WandOfCorruption` now attaches `CorruptionBuff` on a successful roll and refuses bosses and already-allied targets (SPD parity). Warding gained a real `WardSentry` actor (`src/actors/mobs/special/ward_sentry.gd`): a rooted ally that each turn zaps the nearest hostile mob within `3+level` range for the wand's bolt damage, scales HP with wand level, grants no XP, is immune to the hero, and de-spawns the oldest sentry when the wand's `MAX_SENTRIES+level` cap is hit; it renders from the existing `wards.png` sheet via new `mob_sprite` entries. Removed the now-duplicate `ally_hero` declarations that `SummonedElemental`/`Bee`/`RoseGhost` inherited from Mob and marked those three `is_ally = true` so allies never target each other. Added `test_wand_of_corruption.gd` (corruption makes a real ally, grants no XP, drops Amok reliance, ally hunts hostile mobs and shrugs off hero damage, bosses immune) and `test_wand_of_warding.gd` (zap conjures exactly one live `WardSentry` on the level, tracks its position, and the sentry zaps a nearby hostile mob for damage on its turn), both registered in the runner. Full local headless suite green (1151 checks, 0 failures, Godot 4.7.1); `git diff --check` clean. Follow-ups OPEN: enemy mobs don't actively target allies (they still focus the hero, matching existing SummonedElemental behavior); partial (resisted) corruption still applies Terror+Weakness rather than SPD's scaled amok/debuff; sentries persist on-level rather than expiring when the hero leaves the depth; hero melee/movement doesn't yet swap places with allied mobs.

- Tags: shop, gold, hud, artifacts, source-fidelity, tests, audit-S26
- Routed shop sale payouts through the same canonical gold pickup path SPD uses. Upstream `WndTradeItem.sell()` pays the hero with `new Gold(item.value()).doPickUp(hero)`, so sales should update the run gold total, collected-gold stats/badges, HUD gold listeners, and gold-pickup artifact hooks together. `Shopkeeper.sell_item()` now pays `item.value()` via `GameManager.add_gold(sell_price, hero)` instead of mutating `GameManager.gold` and emitting only `gold_changed`, and `WndShop`'s fallback sale path does the same. The shop sell picker now displays `item.value()` as the sale value because the shop markup belongs to purchases, not player sale payouts. Added `test_shop_gold_events.gd` to lock payout amount, `EventBus.gold_collected`, `stats["gold_collected"]`, equipped-artifact `on_gold_pickup`, item removal, and buyback record behavior. Local Godot import passed; full headless suite green (1142 checks, 0 failures, Godot 4.7.1); `git diff --check` clean.

- Tags: wands, warding, compile, tests
- Fixed a headless parse/class-registration issue in the newly landed Wand of Warding sentry work. `wand.gd`, `ward_sentry.gd`, and `test_wand_of_warding.gd` no longer require `WardSentry` as a compile-time global type before Godot has registered it; they use the preloaded sentry script plus script/duck checks while preserving the same spawn, despawn, and zap behavior. This made the rebased full suite green again (1166 checks, 0 failures, Godot 4.7.1); `git diff --check` clean.

- Tags: mobile, hud, multiplayer, controls, tests
- Kept mobile co-op HUD controls inside the visible viewport. The shared HUD layout still used desktop-centered offsets for `PartyRow` and `OnlineStateLabel`: on a 393px portrait viewport, `party_width = _vp_size.x - 420` went negative and both controls were positioned around x=196/206, leaving multiplayer focus/status controls partly or fully offscreen. `HUD._apply_responsive_layout()` now clamps both elements to the same mobile width budget as the status strip, respects horizontal safe-area insets in landscape, and `_refresh_party_row()` uses narrower mobile buttons/separation so up to four hero focus buttons fit in a phone portrait row. Added `test_mobile_hud_input.gd` coverage for portrait party-row/online-label bounds, four-button fit, and landscape safe-area bounds. Local full headless suite green (1135 checks, 0 failures, Godot 4.7.1); `git diff --check` clean.

## 2026-07-17

- Tags: mobile, hud, web, controls, viewport, tests
- Aligned mobile web content scaling with the HUD's visible-viewport layout source. HUD already preferred `window.visualViewport` so the toolbar/status layout could avoid browser chrome and safe-area clipping, but `GameManager._get_mobile_web_viewport_size()` still sized Godot's mobile content scale from `window.innerWidth/innerHeight`. On mobile browsers those can diverge, leaving the HUD laying out against one rectangle while the canvas/content scale uses another, which makes the already-sensitive portrait/landscape toolbar geometry flaky. `GameManager` now prefers `visualViewport.width/height` and falls back to `innerWidth/innerHeight`, matching HUD behavior while leaving non-Web and desktop fallback behavior unchanged. Added parser coverage to `test_mobile_hud_input.gd`. Local Godot import passed; full headless suite green (1130 checks, 0 failures, Godot 4.7.1); `git diff --check` clean. Local web export/playwright was not runnable because this machine lacks Godot 4.7.1 Web export templates.

- Tags: mobile, hud, toolbar, quickslots, web, controls, tests
- Restored phone access to all six quickslots without widening the compact toolbar. Fable's review found that compact mobile hid quickslots 3-6 (`Toolbar._apply_button_labels`) and, unlike desktop, mobile has no keyboard fallback for `1`-`6`, so assigned items could become unreachable. `Toolbar` now keeps two quickslots visible at a time and adds a compact pager button that cycles 1-2, 3-4, and 5-6, preserving the 375px portrait width budget while making every slot tappable. Also switched web HUD viewport probing to prefer `window.visualViewport` over `innerWidth/innerHeight`, and registered web `visualViewport.resize`/`orientationchange` callbacks so toolbar/status layout refreshes immediately when mobile browser chrome or rotation changes the actually visible viewport. Extended `test_mobile_hud_input.gd` with quickslot paging and browser viewport parsing coverage. Local Godot import passed; full headless suite green (1127 checks, 0 failures, Godot 4.7.1); `git diff --check` clean.

- Tags: scenes, turn-loop, mob-refresh, current-scene, source-fidelity, tests, audit-S28
- Fixed the SceneManager/`current_scene` desync that silently killed per-action mob refreshes (S28 P1). `SceneManager._do_transition` adds every screen via `root.add_child()` and never called `set_current_scene`/`change_scene_to_*`, so Godot's `SceneTree.current_scene` stayed pinned to the boot `MainScene` for the whole run. `TurnManager._get_game_scene_cached` read `tree.current_scene` and cached it as `_cached_game_scene`, so the `has_method("on_mob_action")` guard in `_process_mobs_async` was always false and the loop's only caller of `GameScene.on_mob_action` never ran — visible mob moves/attacks during the mob phase got no per-action FOV/sprite refresh (they only "snapped" on the next full turn refresh). Two-part fix: (1) `SceneManager._finalize_transition` now calls `tree.set_current_scene(new_scene)`, guarded on `new_scene.get_parent() == tree.root` (which `_do_transition`'s deferred `root.add_child` guarantees by the time the deferred finalize runs); the tree is resolved via `get_tree()` when SceneManager is in-tree, else `Engine.get_main_loop() as SceneTree`, so it also works in bare-instance/headless contexts where autoloads aren't mounted and `get_tree()` is null. Confirmed against Godot 4.5 that `set_current_scene` applies synchronously for a node already parented to root and does NOT free the previous current_scene. (2) `TurnManager._get_game_scene_cached` now prefers `SceneManager.current_scene` (set synchronously inside `_do_transition`, so it's correct a frame before `tree.current_scene` is mirrored) and falls back to the tree; and TurnManager resets `_cached_game_scene` on `SceneManager.scene_changed` via a new `_on_scene_changed` handler. The signal hookup is deferred from `_ready` (`_connect_scene_manager.call_deferred()`) because TurnManager registers before SceneManager in the autoload order, so the `SceneManager` global is null during TurnManager's own `_ready`. Net: the engine's `current_scene` and `SceneManager.current_scene` now agree after every transition, and the turn loop resolves the live GameScene (self-healing across floor descents, since old scenes are freed → `is_instance_valid` false → re-resolve). Added `test_scene_transition_current_scene.gd` (+ `_stub.gd` GameScene stand-in exposing `on_mob_action`), registered in the runner: `_finalize_transition` mirrors a probe scene into `tree.current_scene`; TurnManager resolves the SceneManager-tracked scene and confirms it exposes `on_mob_action`; and `_on_scene_changed` clears the cached scene. Full local headless suite green (1093 checks, 0 failures, Godot 4.7.1); `git diff --check` clean. Follow-ups still OPEN under S28: MainScene leak (first-transition `queue_free` skipped, P2), no transition re-entrancy guard (P2), `scene_changed` still lacks non-TurnManager consumers e.g. per-screen audio (P2, now partial), no full `go_to` free/meta test since add_child/free run deferred and the synchronous runner can't step frames (P2, now partial), `go_to` null-guard (P3), and the meta-vs-direct-prop parameter split (P3).

- Tags: hud, minimap, fov, source-fidelity, tests, audit-S35
- Made the minimap own its `level_changed` refresh instead of racing HUD-side population against the minimap's own clear handler. `Minimap._on_level_changed()` now repopulates from `GameManager.current_level`, focused hero, current visible/visited arrays, mobs, and party positions when level state exists; it only clears to unexplored when there is no active level. `HUD._on_level_changed()` now updates HUD labels/status only, removing the duplicate minimap writer. This preserves SPD-style live floor awareness by keeping the minimap populated immediately after floor transitions regardless of signal ordering. Added `test_minimap_refresh.gd` and registered it in the headless runner. Local Godot import passed; full headless suite green (1089 checks, 0 failures, Godot 4.7.1). `gdparse` is not installed locally.

- Tags: weapons, enchantments, curses, procs, source-fidelity, tests, audit-S13
- Wired curse weapon enchantments end to end (S13 P2). Two halves of the same feature were dead: (1) `WeaponEnchantment.proc()` `match enchant_id` listed only the 13 non-curse ids, so the fully-implemented `_annoying_proc`/`_sacrificial_proc`/`_displacing_proc` bodies were never reached — any curse enchant just fell through to `return damage`; and (2) `WeaponEnchantment.random_curse()` had zero callers repo-wide, so no code path ever produced a weapon carrying a curse enchantment. Even a cursed loot weapon (`Weapon.random()`, `effect_roll < 0.3`) only set `cursed = true` and left `enchantment == null`, unlike SPD where a cursed roll also does `enchant(Enchantment.randomCurse())`. Fix is minimal and source-faithful: added the three ids that have proc bodies (`annoying`, `sacrificial`, `displacing`) to the `proc()` match so they dispatch, leaving the curse ids without implementations (dazzling/explosive/wayward/polarized/friendly) to keep falling through as no-ops; and added `enchant(WeaponEnchantment.random_curse())` to `Weapon.random()`'s cursed branch. Non-curse generation and probabilities are untouched — the good-enchant branch still fires at `effect_roll >= 0.85` via `random()`, and the cursed threshold stays `< 0.3`. Added `test_curse_weapon_enchantments.gd` (registered in the runner): `random_curse()` over 40 rolls only ever returns `is_curse` ids in `CURSE_IDS`; `create()` flags the three implemented curse ids; a cursed weapon from `Weapon.random()` now carries a curse enchantment that is itself `is_curse`; `_sacrificial_proc` dispatches (returns 1.3× damage and deals self-damage to a stub attacker via `take_damage`); `_displacing_proc` dispatches (teleports a stub defender to the stub level's `random_passable_cell`); `_annoying_proc` still emits `EventBus.enchantment_proc('annoying')`; and a non-curse proc (`shocking`) still routes and adds its bonus damage. Full local headless suite green (1086 checks, 0 failures, Godot); `git diff --check` clean. Follow-ups still OPEN under S13: the remaining five curse enchants have no proc implementations (their `create()`/color/`is_curse` metadata exists but `proc()` is a no-op for them), and the separately-tracked simplified non-curse enchant behaviors vs SPD (`_shocking_proc` chain-lightning, `_kinetic_proc` overkill storage, `_grim_proc` curve) remain open.

- Tags: armor, glyphs, buffs, haste, rooted, source-fidelity, tests, audit-S13
- Gave FlowGlyph and EntanglementGlyph real effects (S13 P2). Both glyphs hand-built a bare `Buff.new()` and stamped a `buff_id` string on it — Flow set `buff_id="Haste"`, Entanglement set `buff_id="Rooted"` — instead of instantiating the real buff classes the way the sibling `SwiftnessGlyph` already does (`Haste.new()`). Because the base `Buff.modify_speed()` returns speed unchanged, Flow's water-proc "haste" was completely inert: the wearer got a status entry that `has_buff("Haste")` could find but that never actually sped them up. Entanglement's fake "Rooted" was subtler — the hero's `_do_move` root gate keys on `has_buff("Rooted")`, so movement *was* blocked, but the buff was a NEUTRAL non-debuff with the wrong typing/description and none of the real `Rooted` class's behavior. Fix mirrors `SwiftnessGlyph` exactly: `FlowGlyph` now does `var haste: Haste = Haste.new(); haste.set_duration(2.0 + level)` and `EntanglementGlyph` does `var root: Rooted = Rooted.new(); root.set_duration(2.0 + level)` — the same `Rooted` class already used by the gripping trap and stormvine plant, so the debuff is genuine, findable by class, and duration still scales with armor level. No behavior removed: Flow still only procs on WATER terrain, Entanglement still counts adjacent grass tiles and grants `(2+level)*grass_count` shielding via `add_shielding`. Added `test_flow_entanglement_glyphs.gd` (registered in the runner): with a dummy char/level, the Flow proc on water attaches a real `Haste` (asserting `buff is Haste` and `modify_speed(1.0) == 3.0`, which an inert `Buff` fails), the Entanglement proc attaches a real `Rooted` whose `is_debuff` is true, both durations equal `2 + armor.level`, and adjacent grass still adds shielding — 9 checks. Full local headless suite green (955 checks, 0 failures, Godot); `git diff --check` clean. Follow-ups still OPEN under S13: no curse armor glyphs exist, and `has_curse_glyph()` is always false because `ArmorGlyph` has no `is_curse` property.

- Tags: armor, glyphs, loot, generator, source-fidelity, tests, audit-S13
- Made random loot armor able to spawn with glyphs (S13 P1). `Armor.random()` — called by `Generator` when spawning armor loot (`generator.gd:492`) — rolled a `+0/+1/+2` level, then a single `effect_roll`: `<0.3` cursed, `>=0.85` "good glyph". Both effect branches were stubs (`cursed = true` with a "curse glyph would go here" comment, and a bare `pass` on the good branch), so **every** piece of loot armor came out glyph-less; the only way to get a glyphed armor in the whole game was Scroll-of-Enchantment `inscribe()`. The 12 fully-implemented common glyphs (Obfuscation/Swiftness/Viscosity/Stone/Repulsion/Affection/Anti-Magic/Thorns/Potential/Brimstone/Flow/Entanglement) and `ArmorGlyph.random()` already existed and were used by `upgrade_armor(inscribe_glyph=true)` — they were simply never reached from loot generation. Fix is one line: the good branch now calls `inscribe(ArmorGlyph.random())`, exactly mirroring the already-correct `Weapon.random()` → `enchant(WeaponEnchantment.random())` sibling path (`weapon.gd:279-282`). Roll thresholds, the `+0/+1/+2` level distribution, and the curse/glyph mutual exclusivity are unchanged; the cursed branch stays `cursed = true` with **no** curse glyph because no curse-glyph set exists in the port yet (that's the separate `[P3][audit:S13]` "no curse armor glyphs" item, along with the always-false `has_curse_glyph()` and the inert Flow/Entanglement raw buffs — all left OPEN). Also refreshed the now-stale docstring/comments that described the missing "full glyph system." Added `test_armor_random_glyphs.gd` (registered in the runner): over 400 seeded `random()` rolls, some armor gains a valid non-empty-id `ArmorGlyph` and some is cursed (proving both branches are live); across another 400 rolls no cursed armor ever carries a good glyph (branches stay exclusive); `degrade()` preserves an inscribed glyph and only lowers level/str req; and an inscribed glyph round-trips through `serialize()` → `deserialize()` with its id intact. Full local headless suite green (944 checks, 0 failures, Godot 4.7.1); `git diff --check` clean. Follow-ups still OPEN under S13: curse armor glyphs don't exist, `has_curse_glyph()` is always false (no `is_curse` on `ArmorGlyph`), and FlowGlyph/EntanglementGlyph still apply inert hand-built `Buff.new()` instances instead of real `Haste`/`Rooted`.

- Tags: huntress, spirit-bow, weapons, serialization, save-load, tests, audit-S13
- Pinned the Spirit Bow save/load fix with a regression test (S13 P1). The bug itself — `SpiritBow.deserialize()` being a no-op `pass` that discarded the whole saved payload, so the Huntress's bow lost its upgrade level / augment / enchantment / curse+id flags on every load (belongings restores an equipped item via `Generator.create_item('spirit_bow')` + per-item `deserialize()`) — was already corrected in commit c45362c (WS0 quick-wins, PR #2) by replacing the override with `super.deserialize(data)`. `SpiritBow` carries no state of its own beyond the shared `Weapon`/`Item` base (its damage scales off live hero level, not a stored field), so chaining to super is the complete and correct fix; no further code change was needed this slice. Added `test_spirit_bow_serialization.gd` to lock it (registered in the runner): the core `serialize → Generator.create_item('spirit_bow') → deserialize` round-trip preserves upgrade level, `level_known`, and the resulting damage range; augment (`DAMAGE`) and enchantment (id `blazing`) survive; curse/`cursed_known`/`identified` flags survive; the equipped `spirit_bow` belongings slot round-trips level+augment through the real `Belongings.serialize/deserialize` path; and a Huntress starting-kit save/load keeps an upgraded (+5) bow. Verified the test genuinely guards the regression — 12 failures when `deserialize()` is temporarily reverted to `pass`, 0 after. Full local headless suite green (933 checks, 0 failures, Godot); `git diff --check` clean. Follow-ups still OPEN (separate from this data-loss item): the broader Spirit Bow feature parity — `shoot()` is a simplified virtual-projectile stub with no real `SpiritArrow` entity, and SPD's spirit-hunter aim/followup mechanics aren't ported.

- Tags: geometry, ballistica, pathfinder, shadow-caster, fov, tests, bugfix, audit-S22
- Backfilled the geometry core with focused headless tests and fixed a real row-edge-wrap bug it exposed (S22). Added three test cases (registered in `run_tests.gd`): **`test_ballistica.gd`** — stop-flag semantics on a hand-verified 10-wide grid (STOP_SOLID halts in the cell *before* a wall; STOP_CHARS/STOP_TARGET halt *on* the cell; MAGIC_BOLT sails through the aimed cell to the next obstacle/edge; WONT_STOP degrades collision to the last cell), diagonal `x==y` walk, and Chebyshev `distance`; **`test_pathfinder.gd`** — corner/edge neighbor sets with no column wrap, straight-path length, unreachable across a full wall column (`find_path`/`find_step`), diagonal corner-cut rejection, and the BFS `build_distance_map` flood (source 0, cardinal step 1.0, walled cells INF); **`test_shadow_caster.gd`** — origin + all 8 neighbors lit, circular radius cutoff (cardinal cell at radius visible, one past it not), and wall occlusion (wall cell visible, cell directly behind occluded, off-axis clear). The Ballistica tests surfaced a genuine bug: `cast()` traced the ray by incrementing a **flat** index and re-derived `x = cell % width` in `_inside_map`, so a horizontal (or near-horizontal) ray reaching the east/west border wrapped its index onto the adjacent row (x=width-1 → +1 → x=0, y+1) and kept tracing there instead of terminating at the map edge. Reachable in gameplay via `wand.gd:148`, where an unidentified wand fires MAGIC_BOLT through `_make_open_passable()` — an all-open grid with no border walls to STOP_SOLID against — so a bolt aimed at a map edge would wrap onto the next row. Fixed by tracking true `cur_x`/`cur_y` alongside the flat `cell` through the DDA (mirroring SPD's `Ballistica.build()`, which iterates 2D coords) and bounding the loop on those coordinates; removed the now-dead `_inside_map` helper. Regression pinned by a `test_ballistica.gd` assertion that every cell of a horizontal WONT_STOP ray stays on its origin row. Full local headless suite green (917 checks, 0 failures, Godot 4.7.1); `git diff --check` clean. Follow-ups still OPEN under S22: the entire hand-written `Pathfinder` A* remains dead code (runtime pathing uses Godot `AStar2D`), so these tests lock the *contract* of currently-unwired helpers; `build_distance_map` still uses non-uniform 1.0/√2 costs vs SPD's uniform-cost integer BFS; `Ballistica.IGNORE_SOFT_SOLID` stays inert until `avoid[]` lands; and the cosmetic `shadow_caster.gd` integer-division annotation was left untouched.

- Tags: npcs, quests, blacksmith, items, loot, source-fidelity, audit-S21
- Made the Troll Blacksmith quest completable (S21 P1). `blacksmith.gd` required 15 `dark_gold_ore` and called `Belongings.count_item`/`remove_item_quantity`/`remove_item_by_id` on that id, but repo-wide grep found `dark_gold_ore` **only** in `blacksmith.gd` — no generator branch, loot table, or terrain produced it, so the hero could never deliver and both the quest and its reforge reward were dead. The `_spawn_blacksmith` comment ("Dark gold ore drops from bats naturally — handled by bat loot tables", `quest_handler.gd:126`) described a source that didn't exist. Fix in two small pieces: (1) `Generator` now knows the item — a new `_make_stackable_misc_item()` helper builds a stackable, pre-identified, non-unique MISC material (mirroring `_make_misc_item` but stackable so pieces pool and merge), and `create_item("dark_gold_ore")` dispatches to it (`generator.gd`); (2) Caves bats carry it — `Bat.loot_table = [{"item_id": "dark_gold_ore", "chance": 0.5}]` (`bat.gd`), which flows through the existing `Mob._drop_loot()` → `Generator.create_item` → `Level.drop_item` pipeline with zero pipeline changes. Because the ore is stackable, `Belongings.add_item` merges every bat drop into one backpack stack, and the blacksmith's existing count/consume calls Just Work. Added `test_blacksmith_ore_quest.gd`: ore is a real stackable pre-identified MISC item; 15 separate pickups pool to `count_item == 15` in a single merged slot and `remove_item_quantity(15)` clears it; the bat loot table lists ore with a positive chance; a forced-chance `_drop_loot()` places exactly one ore heap at the bat's cell through the real mob pipeline; and the blacksmith's `_count_ore`/`_take_ore` count then fully consume the required 15 (via lightweight hero/level stubs, no UI). Registered in the runner. Full local headless suite green (878 checks, 0 failures, Godot 4.7.1); `git diff --check` clean. Follow-ups OPEN: this port sources ore from bat drops rather than SPD's Pickaxe + Caves mining terrain, and the 0.5 per-kill chance / 15-piece gate are gameplay-tuned, not SPD-exact; the broader Blacksmith parity gap (Pickaxe/mining, CRYSTAL/GNOLL quest variants + boss, favor system, harden/upgrade/smith reward options — currently a single reforge) remains the separate S21 P2 item.

- Made same-tier weapons mechanically distinct (S13 P1). Previously every weapon in a tier shared one profile: `delay_factor` defaulted to 1.0 and was never set in any factory branch, and damage came purely from `tier`/`level` in `Weapon.get_damage_range()`, so a Dagger and a Cudgel differed only in name and (for spear/glaive) reach. Added two shared `Weapon` fields — `damage_multiplier` (scales the whole tier/level damage range on top of the augment multiplier) and `str_req_bonus` (added to the tier-derived STR requirement) — and wired them into `get_damage_range()` and `get_str_requirement()`, alongside the already-plumbed-but-unused `delay_factor` that flows through `speed_factor()`. New saves serialize/deserialize the profile fields, while legacy saves without those fields keep the current factory archetype instead of flattening old heavy/fast weapons back to neutral. `MeleeWeapon.create` now assigns a source-fidelity archetype per weapon: **heavy** (cudgel, hand_axe, mace, battle_axe, flail, war_hammer, greataxe) = slower DLY + higher damage + `+1` STR; **light/fast** (gloves, dagger, dirk, sai, rapier, scimitar, assassins_blade) = lower DLY + lower damage + `−1`/`0` STR; **defensive** shields (round_shield, greatshield) = slower + `+1` STR; reach polearms (spear, glaive) keep the neutral profile; and the six "standard" weapons (worn_shortsword, shortsword, sword, longsword, greatsword, runic_blade) stay at the exact pre-change baseline (multiplier 1.0, delay 1.0, bonus 0) so nothing regressed for them. `MissileWeapon.create` got a light matching pass (throwing_club/throwing_stone heavier, shuriken faster) since the fields live on the shared base. Added `test_weapon_differentiation.gd`: worn_shortsword baseline unchanged (1-10 dmg, STR 10), ≥4 distinct delays / ≥4 distinct max-damage / ≥2 distinct STR reqs across the five tier-1 melee weapons, heavy>standard>light ordering through the combat-facing `get_damage_range`/`speed_factor`/`get_str_requirement` APIs, factory-cached `str_requirement` matching the live value, a serialize→deserialize round-trip of the new fields, legacy-load preservation of a heavy factory profile, and a missile club-vs-dart diff — registered in the runner. Full local headless suite green (862 checks, 0 failures, Godot 4.7.1); `git diff --check` clean. Follow-up still OPEN: per-weapon ACC factors and unique abilities (rapier lunge, sai combo, dagger surprise-damage bonus) are not yet ported — only DLY/damage/STR differentiate.

- Tags: bags, inventory, pickup-routing, magical-holster, source-fidelity, audit-S18
- Made bags actually work in play (S18), closing the two remaining bag items. **Pickup routing**: `Belongings.add_item()` now looks for a held bag whose `can_hold` accepts the incoming item (`_bag_that_accepts()`) and, if found, stores the item inside that bag — still firing `on_pickup` — before it would ever consume a top-level backpack slot (`belongings.gd`); a bag never nests inside another bag. A full bag falls back to the normal backpack path, and stackable pickups merge into an existing bag stack even when the bag is at slot capacity — achieved by splitting `Bag.can_hold`/`add_to_bag` into a category test (`accepts_category`) and a separate capacity/stacking test so a merge never needs a free slot (`bag.gd`). **Lookups stay whole**: added `Belongings._all_backpack_items()` (backpack plus every bag's contents) and routed `get_items`, `find_item`, `find_item_by_id`, `count_item`, `has_item`, `remove_item`, `remove_item_by_id`, and `remove_item_quantity` through it, with removals resolving to the correct container via `_remove_item_object()`. This matters for correctness: seeds route into the Velvet Pouch, and the wandmaker seed quest (`count_item`/`remove_item_quantity`) plus wand recharge/XP scans (`get_items`) would otherwise no longer see them. **Magical Holster**: a new serialized `accepts_missiles` flag makes `accepts_category()` admit any `MissileWeapon` alongside the `WAND` category while still rejecting melee weapons that share the WEAPON category (`bag.gd`). Added `test_bag_pickup_routing.gd` — routing into the pouch, in-bag stacking, full-bag fallback to the backpack, find/count/remove reaching bag contents (partial + final removal), and holster wand/missile acceptance with melee rejection and flag survival across serialize/deserialize — registered in the runner. Full local headless suite green (836 checks, 0 failures, Godot). Follow-ups: no inventory UI yet to open a bag or move items between bag and backpack; acquiring a bag does not retroactively sweep already-held matching items into it (SPD does).

- Tags: bags, inventory, serialization, save-load, source-fidelity, audit-S18
- Closed the latent bag data-loss bug (S18): `Bag.serialize()` already wrote its `items` array, but `Bag.deserialize()` discarded it (a stale comment claimed "items are deserialized by the item loading system" — nothing did), so every bag would silently empty on save/load the moment bags went live. `Bag.deserialize()` now reads `data["items"]`, recreates each entry via `Generator.create_item(id)`, per-item `deserialize()`s it, and appends directly to `items` (bypassing `can_hold`/`add_to_bag` so a round-trip preserves exact contents even if capacity/category rules change) — `bag.gd`. Added `test_bag_serialization.gd` (round-trip preserves two mixed items with concrete scripts, generator-backed reload mirroring how `Belongings` rebuilds a backpack bag, empty-bag round-trip, and category preservation), registered in the runner. Full local headless suite green (809 checks, 0 failures, Godot). Follow-up still OPEN: bags are not yet wired into `Belongings` (no `bags` array, pickups don't auto-route via `can_hold`), so bag containers remain non-functional in play — this slice only closes the serialize/deserialize data-loss half; `magical_holster` missile-weapon `can_hold` branch also still open.

- Tags: rings, artifacts, damage, gold, source-fidelity, audit-S16
- Revived two dead S16 effects. **Cape of Thorns**: its `on_hero_damaged` charge/absorb/reflect logic existed (`artifact.gd`) but `Char.take_damage` never called it, so the artifact never charged, absorbed, or retaliated. Wired the hook surgically from the damage pipeline — after computing applied damage, `take_damage` reads `get("belongings").get_equipped_artifact()` (heroes only; base Chars have no `belongings` and skip it) and folds the hook's return value back into the applied damage (`char.gd`). Also added the "thorns" retaliation the description promised: when the shield is active, the absorbed amount is reflected onto a living `Char` attacker (`artifact.gd`). **Ring of Wealth**: `WealthBuff` existed but nothing read `bonus()`, so it was inert. Added `Gold.wealth_adjusted_quantity(hero)`, which scales a gold pile by the wielded ring's `1.2^bonus` (queries `RingOfWealth`→`WealthBuff.ring.bonus()`, same buff-lookup pattern wands use for Ring of Energy); `Gold.on_pickup` now collects the adjusted amount and forwards the collector hero to `GameManager.add_gold` (`gold.gd`). Added `test_cape_of_thorns.gd` (charges from hits, no reduction un-activated, 50% absorb + attacker reflection when active) and `test_ring_of_wealth.gd` (no-ring/null passthrough, 1.2× and 1.2³ scaling, unequip clears, real pickup path updates gold/stat totals), registered both. Full local headless suite green (796 checks, Godot 4.7.1). Follow-ups: wealth's rare-drop / mob-loot boost is still unimplemented (`Mob._drop_loot()` has no hero context — gold-pile multiplier only); the Furor/Haste attack-vs-move split remains open.

- Tags: hero, mage, weapons, wands, source-fidelity, audit-S02
- Replaced the Mage's placeholder worn shortsword start with a real `MagesStaff` slice. `Generator.create_item("mages_staff")` now creates the Mage's unique, no-bones, zero-value staff with SPD's reduced tier-1 melee max damage, default `ZAP`, and an identified/uncursed Wand of Magic Missile imbued at the staff's +1 max-charge baseline. The hero wand-zap path now accepts an equipped non-wand item that exposes `zap()`, so the held staff can cast through the same action pipeline as backpack/equipped wands. Added `test_mages_staff.gd` covering generator construction, source-fidelity properties, equipped-staff zap charge spending, staff/belongings serialization, and the Mage starting kit. Full local headless suite green (779 checks, Godot 4.7.1). Follow-ups: staff upgrade/imbue UI/procs are still incomplete, and the staff max-charge bonus is static in this slice rather than a dynamic wrapper around future wand level recalculation.

- Tags: backlog, reconciliation, docs, audit-S01, audit-S06, audit-S14, audit-S16, audit-S18, audit-S20, audit-S24, audit-S26
- Reconciled stale audit tags in `docs/memory/backlog.md` against current `src/`+`tests/` (docs-only; no code touched). Verified each item in source and by its committed/uncommitted headless test before flipping the tag. Marked **[DONE]**: S01 atomic save write + `.bak` rotation (`save_manager._write_atomic_var`, `test_save_manager.gd`) and save migration path (`SAVE_VERSION=2`, `_migrate_save`/`_migrate_v1_to_v2`); S06 Fury single-application (`test_fury_damage`), `get_speed()` folding `modify_speed` (`test_speed_modifiers`), Doom as permanent +67% damage-taken amp (`test_combat_buffs`), Frozen one-potion loop dedent (`test_frozen`), Barkskin/ArcaneArmor interval-gated decay (`test_interval_armor_buffs`), Barrier `get_shielding`/`absorb_damage` unification (`test_barrier_shielding`); S14 wand per-turn recharge (`test_wand_recharge`), WandOfFrost `was_chilled` gate (`test_wand_of_frost`), wand use-to-id refill via `on_hero_gain_exp` (`test_wand_use_identification`), WandOfDisintegration routed through `take_damage` (`test_wand_of_disintegration`); S16 ring/artifact passive rebuild on load via `resolve_post_load` and Ring of Might non-persistent buff + clean-base serialize (`test_ring_of_might_state`); S18 ordinary LOCKED_DOOR unlocks with iron keys (`test_door_key_gating`); S20 `Level.tick_blobs()` driven per round + Fetid Rat `add_blob` arg fix (`test_blobs`); S24/S26 depth transitions consolidated onto `GameManager.descend()/ascend()` restoring the `MAX_DEPTH` cap, plus S26 `quest_flags` now persisted. Left **OPEN** (partial/unverified): S01 per-floor-transition autosave (only app-close/pause + continue autosave are wired; a fresh descend/ascend still doesn't autosave); S13 random-loot armor glyphs (`Armor.random()` still `pass`-stubs both glyph branches — only Scroll-of-Enchantment inscribes); S16 Furor/Haste split (hook is live and the two are no longer identical, but Furor still applies to movement speed rather than attack speed). All other open audit items left untouched.

- Tags: traps, disarming, source-fidelity, audit-S10
- Made Disarming Trap actually disarm the hero. `DisarmingTrap._do_effect` read the equipped weapon from `triggerer.get_weapon()` / `triggerer.weapon`, but `Hero` exposes neither — equipped weapons live on `Belongings` (`belongings.get_equipped_weapon()`). The trap therefore always hit the `weapon == null` branch and printed "You have nothing to disarm," doing nothing, despite now being live in the Halls trap pool (depth 21-24, added this session). Fix: read the weapon from `triggerer.belongings.get_equipped_weapon()` first (falling back to the old `get_weapon`/`weapon` paths for non-hero triggerers), and unequip via `belongings.unequip("weapon")` instead of the non-existent `triggerer.unequip_weapon()`, so the weapon is knocked to a nearby passable cell as a heap (SPD behavior). The earlier arg-swap P1 (`drop_item(drop_pos, weapon)`) was already fixed. Added `tests/cases/test_disarming_trap.gd` (weapon unequipped + dropped as a nearby heap; unarmed hero drops nothing) and registered it in the runner; verified it fails against the pre-fix detection (2 failures) and passes after. Full local headless suite green (754 checks, Godot 4.7.1). Remaining S10 open: cursing-trap equipment path still dead, pitfall trap doesn't descend a depth, several trap classes still unpooled outside Halls.

- Tags: plants, aoe, source-fidelity, audit-S19
- Fixed grid edge-wrap in `icecap.gd` and `firebloom.gd`: `DIRS_8` neighbours are now column-safe, so a wall-edge freeze/ignite can no longer affect a cell wrapped across the map edge. Added `test_plant_edge_wrap.gd`.

- Tags: mobile, hud, safe-area, controls
- Tightened mobile HUD safe-area layout so the game log anchors above the safe-area-adjusted toolbar instead of using a separate bottom calculation, and mobile landscape layout is detected when both dimensions fit the mobile web bounds. Extended `test_mobile_hud_input.gd` to cover portrait/landscape log clearance and bottom safe-area toolbar placement.

- Tags: wands, identification, source-fidelity, audit-S14
- Fixed wand use-based identification permanently stalling after 5 zaps (audit S14 P1). `Wand` ported SPD's two-pool scheme (`_uses_left_to_id = 10`, `_available_uses_to_id = 5`) but never ported the regeneration: `_use_for_identification()` early-returns once `_available_uses_to_id <= 0`, and nothing ever refilled it, so every wand froze at 5/10 uses and could NEVER be identified by use — a defining SPD mechanic silently dead. SPD refills the pool in `Wand.onHeroGainExp(levelPercent, hero)` (`availableUsesToID = min(USES_TO_ID/2, availableUsesToID + levelPercent*USES_TO_ID/2)`), called for every carried item from `Hero.earnExp` with `levelPercent = exp/maxExp()` computed before consuming XP. Ported faithfully: added `Wand.on_hero_gain_exp(level_percent)` (guards identified wands, caps at the half-pool), `Belongings.notify_hero_gain_exp()` iterating `get_items()` by `has_method`, and a call in `Hero.earn_xp()` using `amount/xp_to_next` before mutating XP. Added `test_wand_use_identification.gd` (up-front pool stalls without XP, uses-left frozen at half, XP refills to the cap without overfilling, identified wands stop regenerating, and an end-to-end hero-XP-interleaved zap loop that IDs the wand — the end-to-end case fails without the wiring). Full local headless suite green (744 checks, Godot 4.7.1). Remaining open S14 P1s: Corruption produces an enemy not an ally, Warding spawns no sentry.

- Tags: targeting, controls, source-fidelity, audit-S23
- Guarded targeting resolution against out-of-bounds cells before range/visibility checks or item callbacks run. This keeps SPD-style targeting/cell-selection from resolving an invalid cell after mobile/browser coordinate drift or stale input, and leaves targeting active so the player can pick again. Extended `test_targeting_input.gd` to cover invalid-cell rejection and a valid-cell callback path. Local headless suite green (732 checks, Godot 4.7.1).

- Tags: mobile, hud, toolbar, controls, web, playtesting
- Hardened mobile toolbar tap activation against nested/scaled Control coordinate drift. `Toolbar.activate_button_at_screen_position()` now resolves touches in toolbar-local space as well as global space, so HUD-forwarded mobile taps still hit the intended action buttons when browser/backing-canvas scaling or nested panel containers shift rect spaces. Extended `test_mobile_hud_input.gd` to assert a real HUD touch on the bottom toolbar emits a toolbar action. Local headless suite green (399 checks, Godot 4.7.1); no real-device/iPhone confirmation yet.

- Tags: buffs, plants, scrolls, stones, source-fidelity, audit-S15, audit-S17, audit-S19
- Replaced the remaining renamed-Paralysis sleep/freeze substitutes in Scroll of Lullaby, Stone of Deepened Sleep, Dreamfoil, and Icecap with the dedicated `SleepBuff` / `Frozen` effects. Sleep now uses the wake-on-damage contract instead of generic paralysis, and Icecap now gets Frozen's shared freeze semantics rather than an unrelated paralysis timer. Added `test_sleep_freeze_effects.gd` to cover sleep wakeup and Icecap Frozen attachment. Local headless suite green (397 checks, Godot 4.7.1).

- Tags: mobile, hud, toolbar, web, playtesting
- Fixed scaled mobile HUD touch activation after the backing-canvas layout fix. `HUD` now accepts high-DPI backing-canvas touch coordinates for hit-testing, converts them back into HUD layout coordinates before activating toolbar controls, and applies the same conversion to party-row buttons. Extended `test_mobile_hud_input.gd` with scaled toolbar hit-testing and party-button activation coverage. Local headless suite green (384 checks, Godot 4.7.1); no real-device/iPhone touch confirmation yet.

- Tags: potions, blobs, gases, source-fidelity, audit-S15, audit-S20
- Re-pointed offensive potion shatter effects at the live blob layer. Toxic Gas, Paralytic Gas, and Liquid Flame now seed `ToxicGas`, `ParalyticGas`, and `FireBlob` through `Level.add_blob()` instead of applying one-shot 3x3 buffs, and Potion of Frost now seeds a new `FreezingBlob` that freezes characters and water terrain. Updated blob and thrown-potion shatter tests to assert blob seeding, deferred tick effects, and freezing behavior. Follow-up: blob ticking still runs per hero round rather than true actor time, and freezing/fire cross-blob cancellation is not implemented yet.

- Tags: items, stack-splitting, source-fidelity, audit-S12, audit-S15, audit-S17
- Fixed stack splitting so split-off consumables keep their concrete behavior instead of becoming inert base items. Base `Item.duplicate_item()` now reconstructs clones through `Generator.create_item(item_id)` before copying shared state, so factory-backed stackables like seeds, bombs, stones, spells, food, missile weapons, torches, and dewdrops keep their scripts and subtype fields. Potion and scroll overrides now route through `Potion.create(item_id)` / `Scroll.create(item_id)` so split stacks preserve their actual `drink()`/`read_scroll()` implementations instead of downgrading to base `Potion`/`Scroll`. Added `test_item_stack_splitting.gd` to cover the affected families.

- Tags: mobile, hud, web, playtesting
- Fixed mobile web HUD layouts that used the visible browser viewport but still rendered into a larger Godot backing canvas. `HUD` now scales its `CanvasLayer` by backing-canvas size over CSS-visible layout size, so the status strip and toolbar keep their intended on-screen size when mobile browsers expose a large canvas behind a phone-sized viewport. Extended `test_mobile_hud_input.gd` with a 393x852 visible viewport / 1179x1704 backing-canvas regression that asserts the scaled toolbar remains full-width. Local headless suite green (311 checks, Godot 4.7.1); GitHub CI and Pages deploy green for commit `4fa7201`. Browser smoke: live page and `index.js`/`index.pck`/`index.wasm` returned 200, and Chromium mobile-width screenshot loaded the name prompt; no real-device/iPhone in-run touch confirmation yet.

- Tags: potions, throwing, source-fidelity, audit-S15
- Wired thrown potions into the existing shatter path. `Hero._do_throw_item()` now detects `Potion`, resolves the projectile collision cell, calls `Potion.shatter(collision_pos, level)`, identifies the potion, records potion usage, and consumes one stack item instead of falling through to generic ranged-miss logic. Added `test_thrown_potion_shatter.gd` covering Toxic Gas shatter effects, stack consumption, identification, and last-item removal. Follow-up: offensive potion shatter effects still use current one-shot 3x3 buff/terrain behavior; the next fidelity pass should re-point Toxic Gas/Paralytic Gas/Liquid Flame/Frost at real blobs.

- Tags: doors, keys, source-fidelity, audit-S11
- Fixed ordinary locked doors consuming the wrong key type. `Door.open()` now asks for `"iron"` keys instead of `"golden"` keys, activating the existing depth-scoped iron-key logic in `Hero.has_key()`/`use_key()` and preserving golden keys for golden chests. Added `test_door_key_gating.gd` to cover golden-key rejection and iron-key consumption. Follow-up: key placement for locked special rooms is still thin and should be audited against SPD's guaranteed same-floor iron-key placement.

- Tags: chasms, level-transitions, source-fidelity, audit-S11
- Replaced chasm instant-death with a first-pass SPD-style fall flow. Heroes with `Levitation` or flight now cross chasms safely; grounded heroes emit a fall transition, descend without requiring stairs, land on a passable cell of the next floor, and then take `NormalIntRange(HT/6, HT/3)` fall damage plus Cripple instead of being killed in place. `Chasm` now owns the shared crossing and fall-damage logic, `LoadingScene` understands `"fall"` arrivals, and `test_chasm_fall.gd` covers the damage formula, levitation immunity, and event handoff. Remaining follow-ups: PitfallTrap still needs to reuse the fall path, boss-arena chasm rings need encounter-specific handling, and true PitRoom landing support is not implemented yet.

- Tags: mobs, level-generation, source-fidelity, audit-S07
- Added the missing regular-floor scheduled mob respawner. `RegularLevel` now stores the generated floor's target mob population and activates a level-owned `Respawner` actor on the active floor, matching SPD's scheduler shape with a 50-tick respawn cooldown. The respawner adds depth-appropriate replacement mobs only while the living count is below that target, avoids occupied/visible/hero-adjacent cells, registers spawned mobs with the turn manager, and persists the respawn target with cached/saved level state. Extended `test_mob_spawn_positions.gd` to cover cap behavior, hidden-cell selection, serialization, and the actor cooldown path. Full local headless suite green (272 checks, Godot 4.7.1).

- Tags: mobs, level-generation, source-fidelity, audit-S07
- Fixed regular-level mob spawn placement under-filling floors. `RegularLevel.mob_spawn_positions()` used to consume one requested mob even when a room placement failed, and the no-standard-room fallback returned a single position instead of filling the requested count. Placement now counts successful cells, falls back without duplicates when room attempts stall, and has headless coverage for normal rooms, failed room placement, and fallback-only levels. Full local headless suite green (263 checks, Godot 4.7.1).

- Tags: wands, combat, source-fidelity, audit-S14
- Routed Wand of Disintegration damage through `Char.take_damage()` instead of mutating HP directly. Disintegration still bypasses physical armor because `take_damage()` receives already-raw magical damage, but it now respects shielding, Arcane Armor, wake-on-damage hooks, and the shared death path like the other wand subclasses. Added `tests/cases/test_wand_of_disintegration.gd` to cover Barrier absorption, Arcane Armor reduction, and exactly-once death handling.

- Tags: mobile, camera, zoom, web, playtesting
- Fixed mobile web runs starting heavily zoomed in after the browser-viewport scaling fixes. `GameCamera` had forced mobile web to start at `4.5x`; once the game used the actual phone-sized logical viewport, that showed only a tiny slice of the dungeon. Mobile now starts at `1.5x`, can pinch/setting zoom out to `1x`, and still allows zooming in up to `10x`. Added `tests/cases/test_game_camera_mobile_zoom.gd` to lock the mobile defaults.

- Tags: mobile, hud, toolbar, web, playtesting
- Fixed the in-run mobile HUD using the Godot canvas size when mobile browsers report a larger backing canvas than the visible viewport. `HUD` now mirrors the hero-select fix by laying out against `window.innerWidth/innerHeight` for portrait/small/mobile web contexts and polling browser viewport changes so collapsing address bars or orientation changes relayout the toolbar and status strip. Added portrait safe-area coverage to `test_mobile_hud_input.gd` to keep the toolbar above the bottom inset and the status overlay visible.

- Tags: mobile, hero-select, portrait, web, playtesting
- Fixed the character-select action row falling below the visible panel on tight portrait mobile viewports. `HeroSelectScene` now uses a tighter portrait layout when vertical space is short: smaller splash crop, reduced panel inset/gaps, compressed description/stat blocks, narrower hero buttons, and slimmer Back/Start buttons. Follow-up: mobile Web can report a desktop-sized Godot canvas while the browser viewport is portrait, so `HeroSelectScene` now lays out against `window.innerWidth/innerHeight` when the browser viewport is portrait or small and polls that size during browser chrome changes. Added `tests/cases/test_hero_select_portrait_layout.gd` to lock both the 375px portrait mobile budget and the browser-vs-canvas viewport selection.

- Tags: bosses, hud, combat, fidelity, spd-parity, audit-S27, audit-S35
- Wired the dead boss HP bar signals. `HUD` already subscribed to `boss_fight_started`, `boss_damaged`, and `boss_defeated`, but no gameplay path emitted them, so bosses fought without the original-style top HP bar. `Mob` now recognizes current boss mob IDs (plus the existing `BOSS` property), starts the bar when a boss becomes active or is first damaged, updates it on non-fatal damage, and hides it on defeat. Added `tests/cases/test_boss_hp_signals.gd` to cover non-boss silence, boss start payload, HP update payload, and fatal hide ordering. Full local headless suite green (244 checks, Godot 4.7.1).

- Tags: mobs, swarm, combat, fidelity, spd-parity, audit-S05
- Fixed Swarm of Flies split duplicating HP instead of dividing it (audit S05 P1). `Swarm._split()` set `child.hp = hp` (a full copy) and never reduced the parent, so every split roughly *doubled* total swarm HP and over-granted XP; a swarm chipped below half could balloon in effective HP. SPD's `Swarm.split()` hands half the current HP to the clone and subtracts it from the parent (HP conserved). Fix (`sewer/swarm.gd`): `child_hp = hp / 2`, child gets `child_hp` with `hp_max`/`ht` mirroring the parent cap (previously `ht` was mis-set to raw current HP), then `hp -= child_hp`. Also added an `hp >= 2` guard to the split trigger so a near-dead swarm can't spawn a 0-HP child. Added `tests/cases/test_swarm_split.gd` (division, conservation, non-duplication, cap mirroring, split-once flags) and registered it in the runner; verified it distinguishes the fixed code from the pre-fix duplication. Full local headless suite green (235 checks, Godot 4.7.1). Out-of-scope follow-up: the child is still a fresh `Swarm.new()` with base depth-1 attack/defense/damage rather than the parent's depth-scaled combat stats — separate from the flagged HP regression.

- Tags: levels, traps, halls, fidelity, spd-parity, audit-S09
- Fixed Demon Halls regular-floor trap generation falling back to the base early-game pool (WornDart/Poison/Fire/Alarm/Teleport). `HallsLevel` now has its own weighted late-game trap table based on the original SPD Halls trap weights, using the trap classes currently implemented in the port: Frost, Storm, Corrosion, Blazing, Rockfall, Flashing, Guardian, Disarming, Summoning, Warping, Cursing, Grim, and Pitfall. Added `tests/cases/test_halls_traps.gd` to lock the weighted boundaries and assert Halls never selects the early fallback traps. Full local headless suite green (225 checks, Godot 4.7.1). Remaining parity note: upstream SPD also includes Disintegration, Weakening, Distortion, Gateway, and Geyser in the Halls pool; those trap classes are not present in this port yet.

- Tags: mobile, hud, toolbar, web, playtesting
- Tightened the compact mobile toolbar for narrow portrait viewports. The previous compact sizing still needed about 380px before browser safe-area/panel padding, so a 375px-wide phone viewport could clip or hide controls even after touch routing was fixed. `Toolbar` now receives its available width from `HUD`, switches to a narrower compact layout at <=430px, keeps Bag/Wait/Find/Menu plus the first two quickslots visible, and hides only decorative separators. Added headless coverage to `test_mobile_hud_input.gd` asserting the toolbar fits a 375px portrait budget after HUD panel margins. Full local headless suite green (184 checks, Godot 4.7.1).

- Tags: mobile, hud, controls, web, playtesting
- Fixed mobile HUD taps being followed by Godot's synthesized mouse click. The scene already handled real touch release on HUD controls, but the mouse-suppression gate exempted HUD positions, so mobile web could activate a toolbar/window control and then leak a map click/hero action from the synthetic left-click. `GameScene` now suppresses synthesized touch-mouse events for the whole short post-touch window, including over-HUD positions, while preserving direct mouse/desktop input outside that window. Also routed touch release through the party-row focus buttons, which were previously included in HUD hit-testing but not activated by the custom touch path. Added `tests/cases/test_mobile_hud_input.gd` and registered it in the headless runner. Full local headless suite green (181 checks, Godot 4.7.1).

- Tags: wands, combat, fidelity, spd-parity, audit-S14
- Fixed Wand of Frost always paralysing (audit S14 P1). `WandOfFrost.on_zap` applied `Cripple` (chill) and then checked `has_buff("Cripple")` on the same target - always true because it had just been added - so `Paralysis` (freeze) fired on *every* frost hit instead of only when the target was already chilled. SPD's `Frost.freeze()` only freezes an already-chilled target. Fix: capture `was_chilled` *before* adding the new Cripple, and gate the freeze on that (`wand.gd:430-442`). Added `tests/cases/test_wand_of_frost.gd` (fresh target -> chilled but not frozen; pre-chilled target -> frozen) and registered it in the runner; verified it fails against the pre-fix code and passes after. Full headless suite green (177 checks). NOTE: the S14 wand P0 "wands never recharge" is already fixed/tested (PR #5 `belongings.recharge_wands` per hero turn) - NEXT-STEPS.md is stale on that. Remaining S14 P1s still open in `wand.gd`: Corruption makes an enemy not an ally (string alignment + Amok), Disintegration bypasses `take_damage`, Warding spawns no sentry, use-to-id stalls after 5 zaps.

## 2026-07-16

- Tags: blobs, gas, simulation, fidelity, spd-parity
- Brought the blob/gas layer to life (audit S20 vertical slice). Blobs were dead: `Blob.act()` was never driven, so every seeded gas/fire/web sat frozen at its origin, never spreading, poisoning, or decaying - the root cause of the S15/S19 one-shot-buff workarounds. Changes: (1) `Blob` now exposes `tick()` (spread -> apply effects -> decay -> prune) separate from `act()`, with `_spread()` no longer mutating `active_cells` mid-iteration and a row-wrap guard on cardinal neighbors; added `Blob.deserialize()`. (2) `Level.tick_blobs()` drives all blobs once per hero round (mirrors `tick_pending_bombs`), pruning burned-out blobs; wired into `SceneFeedbackCoordinator.on_round_completed`. (3) `Level.add_blob(blob, cell, amount)` now honors density and merges same-type blobs into one instance (SPD keeps one blob per type per level; prevents duplicate entries double-applying effects). (4) Fixed `fetid_rat.gd` toxic-gas drop, which crashed on `seed()`/`add_blob()` arg-count mismatches. Added `tests/cases/test_blobs.gd` (spread, row-wrap guard, decay/prune lifecycle, ToxicGas poison, serialize round-trip, Level merge + tick_blobs). All 65 headless checks pass.
- NEXT SLICE: blobs still tick per *hero round* rather than per game turn (approximation - fine for hero-paced gas, note if mob-time gas matters). Persistence still round-trips live Node dicts via `store_var` rather than `Blob.serialize()`/factory (`blob.deserialize()` now exists but isn't wired into save_manager). Offensive potions (ToxicGas/ParalyticGas/LiquidFlame/Frost, S15) and gas traps/plants should now be re-pointed at real blobs via `add_blob` instead of one-shot 3x3 buffs. Missing SPD blob classes: Freezing/CausticGas/StenchGas/StormCloud/Regrowth/SmokeScreen (SmokeScreen also unblocks the dead LOS branch in `level.gd:257`).

## 2026-05-09

- Tags: multiplayer, movement, sync
- Added explicit hero/mob movement events for online play. Hosts now broadcast movement events when heroes or mobs step, and non-host clients immediately animate the corresponding sprite movement instead of waiting only for the next authoritative snapshot to reposition actors.

- Tags: architecture, framework, planning
- Added `framework-extraction-roadmap.md` as the concrete planning document for turning this codebase into a reusable 2D turn-based game framework. The note defines target architecture layers, extraction phases, what should remain SPD-specific, and why the current multiplayer hardening work is also framework-seam work rather than a separate initiative.

- Tags: multiplayer, mobs, presentation
- Extended the online world-event lane to mob reveals. Hosts now broadcast explicit reveal events for cases like Mimics, and non-host clients immediately spawn the revealed mob sprite plus `Revealed!` feedback instead of waiting for the next snapshot to make the surprise interaction visible.

- Tags: multiplayer, combat, presentation
- Extended the online world-event lane to explicit combat readability events that snapshots do not convey well on their own. Hosts now broadcast attack misses and mob defeats, and non-host clients immediately show miss feedback and death-burst/audio cues instead of having to infer those moments only from later state correction.

- Tags: combat, buffs, presentation, multiplayer
- Broadened the new status-effect presentation path beyond Poison. `GameScene` now uses a shared feedback table for several high-signal debuffs including Burning, Paralysis, Blindness, Chill, Frozen, Rooted, Cripple, Weakness, and Ooze, so both local and online clients get readable burst/status cues when those effects are applied, and focused heroes also get effect-specific tick feedback when damage comes from a buff source.

- Tags: combat, buffs, multiplayer, presentation
- Added real poison feedback. Buff application now emits a status-effect event, `GameScene` now shows poison burst/status feedback when Poison is applied, poison tick damage on the focused hero now also gets a poison-specific burst/status cue, and the online world-event lane now propagates poison-application feedback to non-host clients.

- Tags: multiplayer, sync, hardening
- Hardened the new online world-event presentation path against duplicate client feedback. `GameScene` now keeps a short suppression window for pickup and door positions after explicit world events land, so the next authoritative snapshot can confirm the same state change without replaying a second `Pickup` or `Open` cue on non-host clients.

- Tags: multiplayer, plants, presentation
- Extended the online world-event lane to planting and plant activation. Hosts now broadcast explicit plant events, and non-host clients immediately update local plant state/terrain, spawn plant visuals, and play the existing planted/activation VFX instead of waiting for a later snapshot to imply that flora changed on the floor.

- Tags: multiplayer, items, presentation
- Extended the online world-event lane to explicit item drops. Hosts now broadcast dropped-item events with serialized item data, and non-host clients immediately update their local floor state, spawn the dropped item sprite with drop animation, and show `Drop` feedback instead of waiting for a later snapshot to imply that a floor mutation happened.

- Tags: multiplayer, traps, presentation
- Extended the online world-event lane to trap triggers. Hosts now broadcast explicit trap-triggered events, and non-host `GameScene` clients react immediately with trap SFX, tile/fog refresh, a `Trap!` popup, and warning text instead of waiting for the next snapshot to imply that a hidden world interaction happened.

- Tags: multiplayer, networking, presentation
- Added a lightweight host-to-client world-event lane on top of the existing snapshot model. `NetworkManager` can now broadcast explicit online world events, and `GameScene` uses that for pickups and door opens so non-host clients see those interactions immediately with item/door feedback instead of waiting only for snapshot diff presentation.

- Tags: multiplayer, sync, presentation
- Continued online snapshot polish by adding diff-based world-action feedback on clients. `GameScene` now captures lightweight level state before same-floor host snapshots, replays visible door-open and secret-door-reveal cues when terrain changes, shows remote `Pickup` feedback when floor heaps disappear under a hero, and surfaces a clearer `Ascended` / `Descended` arrival popup after host-owned floor transitions.

- Tags: multiplayer, minimap, readability
- Continued the roadmap’s co-op readability pass by making maps party-aware. `Minimap` and `WndMap` now render the full party instead of only the focused hero, with allied heroes shown as distinct cyan markers while the focused/local hero remains the white marker. This makes 3-4 player shared-floor runs easier to read at a glance.

- Tags: multiplayer, disconnect, resilience
- Hardened host-loss handling across online scenes. `LobbyScene`, `HeroSelectScene`, `LoadingScene`, and `GameScene` now all react to `Disconnected from host.` by warning the player and returning cleanly to `TitleScene` instead of leaving clients stranded in a dead lobby, setup flow, loading screen, or in-progress run.

- Tags: multiplayer, lobby, session
- Improved lobby/session transition clarity. `NetworkManager` now exposes whether an online run is already active, `LobbyScene` now distinguishes `Lobby open` from `Run in progress`, hides ready controls once a run has started, and prevents the host from trying to start again. `TitleScene` now also shows reconnect-oriented client messaging when joining an already active host session.

- Tags: multiplayer, hud, ux
- Added an explicit online-state banner to `HUD`. During online sessions the HUD now shows whether the local player is on `Your turn`, `Waiting for <player>`, `Waiting for party state...`, or `Spectating another hero`, so co-op state ownership is visible without having to infer it from snapshots or party focus alone.

- Tags: multiplayer, client, ux
- Added lightweight online client-side action preview in `GameScene`. When a non-host player submits a move, attack, throw, zap, interact, or search action, the local hero sprite now previews that intent immediately while the host still remains authoritative for the actual resolved state and later snapshot correction.

- Tags: multiplayer, sync, networking
- Improved online live-sync responsiveness in `GameScene`. Host snapshots are now coalesced through a queued sync path instead of only waiting for clean turn-boundary moments, and that queue is now triggered by input-owner changes, hero movement, item pickup, mob damage, trap activation, seed planting, round completion, and visible mob actions. This should make non-host clients catch up sooner during action-heavy moments without abandoning the existing host-authoritative snapshot model.

- Tags: multiplayer, spectator, hud
- Improved co-op death/spectator UX. `GameManager` now exposes local-owned-hero and spectating helpers, local focus cycling skips dead heroes when living options exist, `StatusPane` shows when the local player is spectating or focused on a down hero, and the party HUD row now marks dead heroes explicitly and stops offering dead-focus buttons as normal active selections.

- Tags: multiplayer, loading, startup
- Fixed a multi-hero startup crash in `LoadingScene`: party placement around the staircase no longer uses raw `DIRS_8` offset math that can wrap across rows and probe invalid cells. The spawn search is now coordinate-safe, which should stop party sizes above 1 from tripping out-of-bounds errors during level entry.

- Tags: multiplayer, hero-select, lobby
- Tightened the online class-loadout flow: `HeroSelectScene` now labels party slots and summaries with the actual connected player names, and host online run start now validates against the current lobby player count before launching. `NetworkManager` also normalizes online `party_classes` to exactly the synchronized player list size during run-start sanitization.

- Tags: multiplayer, reconnect, resync
- Added first-pass online reconnect support. `NetworkManager` now remembers the active online run config plus latest host snapshot, rebinds reconnecting players to their original hero slot by saved multiplayer name, updates host-side hero ownership to the new peer ID, and immediately resends run-start plus snapshot data to late/reconnecting clients. `LobbyScene` now also listens for online run-start while already open, so reconnecting clients can jump straight back into `LoadingScene` for resync instead of getting stranded in the lobby.

- Tags: multiplayer, lobby, identity
- Smoothed the co-op lobby flow further for `v0.1.2`: `NetworkManager` now persists the multiplayer player name, last join code, and preferred port/player-cap across sessions, `TitleScene` preloads those saved values into the host/join UI, and online `HeroSelectScene` now locks party size to the actual connected lobby player count instead of allowing extra local-only party slots.

- Tags: multiplayer, networking, authority
- Continued the online co-op bridge into a real host-authoritative prototype: online run-start config now carries a shared `run_seed`, `LoadingScene` uses that seed during `GameManager.new_game(...)`, and online clients no longer activate heroes/mobs into `TurnManager` when entering the dungeon.
- Added the first host-to-client run snapshot feed: `NetworkManager` now emits `run_snapshot_received`, `GameScene` broadcasts authoritative snapshots from the host when input ownership settles on a hero turn, and online clients now apply those snapshots to refresh level state, hero state, sprites, fog, HUD, and local input availability.
- Split `GameScene` input resolution more cleanly for online play by tracking `_network_input_slot`, so online clients now resolve the acting hero from the host-owned turn slot instead of falling back to whatever hero only happens to be locally focused.
- Extended that authority path through stairs/floor changes: the host now broadcasts an online level-transition notice when the party ascends or descends, clients freeze local input and wait for the next authoritative snapshot, and cross-depth snapshots now rebuild the client from the host's new floor instead of requiring a local deterministic level generation step.
- Continued the online authority cleanup in the UI layer: added `EventBus.request_hero_action(action)` so `HUD` quickslots and `WndItem` no longer call `hero.submit_action(...)` or `item.execute(...)` directly for use/throw/zap flows, and temporary client-side guards now block inventory-only mutations like equip/unequip/quickslot assignment/talent upgrades until those paths have real host-authoritative commands.
- Finished the first host-authoritative inventory command layer in `GameScene`: online action envelopes now encode stable `item_ref` / `target_pos` data instead of live object references, the host resolves those back into concrete items/targets before simulation, and equip/unequip/drop/quickslot/talent-upgrade/feed-seed actions now run through the same host-owned request path instead of mutating directly from the UI.
- Added first-pass co-op death rules: `GameScene` now treats hero death as a party event instead of immediate solo game-over, only transitions to death when all heroes are dead, hands local focus off to a living hero for spectating when possible, and host online runs now broadcast explicit run-end messages so clients follow party wipe / victory cleanly instead of getting stranded after the host finishes.
- Smoothed the online session UX so players do not have to start with raw IP/port entry: `TitleScene` now supports one-click `Quick Host`, join-by-code input, remembers the last join code, and auto-populates host codes; `LobbyScene` now shows the current join code and can copy it to the clipboard; and `NetworkManager` now exposes preferred local address / join-code helpers to make session sharing feel more like a real game flow than a manual network form.
- Reshaped that flow to match the intended model: `Host Co-op` in `TitleScene` now immediately creates a session and drops the player into the lobby, while `Join Co-op` is the explicit path for entering host info. `LobbyScene` now also has a `Settings` button with a small host-settings panel so player name, port, and player-cap changes can be adjusted from inside the lobby instead of forcing hosts through the title-screen network form first.

- Tags: multiplayer, ui, bootstrap
- Continued Phase 1 co-op groundwork by adding a real party bootstrap UI to `HeroSelectScene`: party size can now be set from 1-4, each active party slot can be assigned its own class, `Tab` cycles the edited slot, and Start now passes a `party_classes` payload into `LoadingScene` so local multi-hero runs can be created without debug-only metadata.
- Finished the last obvious focused-vs-input leak in `GameScene` targeting flow so targeted actions now validate range/visibility from the acting hero instead of whichever hero is only being viewed locally.
- Hardened the menu path against parser failures in `HeroSelectScene` by simplifying a few strict-syntax constructs there and switching `TitleScene`/`DeathScene` from hard preloads to runtime loading for the hero-select script.
- Continued Phase 1 persistence work by making `SaveManager` party-aware: full saves now store a `heroes` array plus `party_classes` and `local_hero_index`, load restores the whole party instead of collapsing back to one hero, and older single-hero saves still fall back through the legacy `hero` payload path.
- Wired the real continue flow onto that persistence work: `TitleScene` now prefers `SaveManager.load_full_game()` and routes successful continues through `LoadingScene` instead of jumping straight into `GameScene`, and `LoadingScene` now explicitly picks up restored `party_classes` when resuming a run.
- Added the first explicit co-op stair rule in `GameScene`: multi-hero runs now require all living party members to be standing on the same entrance/exit stairs before ascend/descend can fire, and floor-change artifact hooks now run for the whole party instead of only the acting hero.
- Continued co-op rules in moment-to-moment play: tile interactions are now hero-specific in `GameScene`, so any moving party member can trigger pickups and plant activation, gold pickup now charges the correct collector's equipped artifact, and failed inventory pickups are dropped back onto the floor instead of disappearing.
- Added the first internet-session infrastructure layer for multiplayer: new `NetworkManager` autoload owns ENet host/join/disconnect state, and `TitleScene` now exposes `Host Co-op` / `Join Co-op` entry points with a simple network panel for address, port, and player-cap setup plus live session-status feedback.
- Continued that internet layer into a real synchronized pre-game flow: `NetworkManager` now maintains a replicated lobby player list with ready state, `TitleScene` captures a local player name and transitions successful host/join sessions into a new `LobbyScene`, and `LobbyScene` displays connected players plus ready/start/leave controls as the first real shared online session screen.
- Extended the online pre-game flow into synchronized run setup: lobby start now moves every connected peer into `HeroSelectScene`, clients become waiting spectators there, and when the host presses Start the selected `party_classes` are broadcast through `NetworkManager` so all peers transition into `LoadingScene` with the same run-start configuration.
- Pushed online slot ownership into run bootstrap: `NetworkManager` now includes synchronized `player_infos` in the host's run-start config, and `LoadingScene` uses that to assign party hero names and set each client's initial focused hero index based on its connected peer ID.
- Added the first in-run online command seam: `Hero` now carries `owner_peer_id` and `hero_slot_index`, `LoadingScene` assigns those during online bootstrap, `NetworkManager` can now emit `online_action_requested(peer_id, slot_index, action)` to the host, and `GameScene` now applies host-side actions through a shared per-hero action path instead of only through local direct input.

- Tags: input, ui, qol
- Added explicit keyboard attack binds in `GameScene`: `A` and `F` now attack the first adjacent hostile target, with a warning if no adjacent enemy is available.
- Enlarged the popup map window again by letting `WndMap` use a much larger viewport fraction and reducing frame padding, so the opened map takes up more of the screen instead of remaining close to minimap size.
- Continued the fallback-item cleanup by fixing the Imp reward pool to use canonical `ring_of_sharpshooting` instead of the bad `ring_sharpshoot` identifier, and added generator aliases so older save data or stray legacy callsites still resolve to the real ring instead of a generic fallback item.
- Continued that cleanup in the Wandmaker quest by switching the rotberry request/item path from legacy `rotberry_seed` to canonical `seed_of_rotberry`, so the quest now uses the real seed ID end-to-end instead of depending on alias fallback behavior.
- Audited grass/level-generation feel and adjusted three high-signal mismatches: trampled grass now drops a real visible `Dewdrop` heap instead of instant auto-healing, standard rooms no longer spray large amounts of grass into every normal floor, and `Feeling.GRASS` now paints a more controlled mix of grass/high grass instead of turning whole standard-room interiors into blanket grass tiles.
- Continued the grass-system cleanup by replacing generic placeholder seed items with a real `SeedItem` class: generated seeds now support actual targeted planting, the item window shows a `Plant` action for seeds, and throwing seeds through the normal hero action path now plants/activates them correctly instead of treating them like inert generic items.
- Tightened the seed/plant flow further by making Wandmaker's rotberry quest item a real `SeedItem` instead of a generic item, and by removing the duplicate post-activation plant erase in `GameScene` so plant cleanup is owned consistently by `Plant.activate()`.
- Fixed a real Sad Ghost/Imp quest-tracking bug by extending `mob_defeated` to carry both `mob_name` and stable `mob_id`, then switching quest kill matching to use `mob_id` where appropriate. This fixes cases like `Gnoll Trickster` where the display name did not match the stored quest target identifier.
- Added a real plant render path: `GameScene` now tracks planted flora separately from terrain and items, and new `PlantSprite` nodes give planted seeds/plants a distinct on-map presentation instead of reading only as a `HIGH_GRASS` terrain change.
- Added plant placement/activation feedback on the effect layer: planting now shows a small burst and `Planted` status, and triggering major plants now emits plant-specific color/status VFX like `Regen`, `Armor`, `Burn!`, `Frozen`, `Poison`, `Rooted`, `Sleep`, `Warp`, `XP`, and `Haste`.
- Tightened three thinner plant mechanics: `Stormvine` now applies both `Rooted` and `Vertigo`, `Fadeleaf` now avoids obviously bad teleport destinations and emits `hero_moved` for hero teleports, and `Dreamfoil` now explicitly puts mobs back into the `SLEEPING` AI state instead of using a raw integer state write.

## 2026-05-08

- Tags: tooling, workflow, memory
- Added a lightweight repo-local memory system under `docs/memory/`.
- Added canonical files for active context, architecture map, decisions, lessons, backlog, and session-level change summaries.
- Added helper scripts to search memory quickly and append timestamped notes.
- Updated `CLAUDE.md` to point future work toward the concise memory layer before using large historical logs.
- Explicitly positioned the existing Claude-agent logs as legacy deep-reference sources beneath the new concise memory layer.
- Moved bulky historical logs from the repo root into `docs/history/` and added archive `README.md`, `INDEX.md`, and `SUMMARY.md` to preserve accessibility while reducing clutter.
- Added compact memory notes derived from the archive: `archive-backlog.md`, `system-summaries.md`, and `session-checklist.md`.
- Added focused topic notes for persistence, framework-readiness, and SPD-fidelity work.
- Started closing the original-SPD identification gap with a persistent `ItemCatalog` autoload, Journal catalog wiring, and automatic potion/scroll self-identification on use.
- Added a per-run `ItemAppearance` autoload so potions, scrolls, and rings now get deterministic unidentified appearances that persist through save/load.
- Added a persistent `DiscoveryCatalog` for enemies, traps, and plants, and expanded the Journal catalog beyond items-only.
- Extended `DiscoveryCatalog` and the Journal to track weapon enchantments and armor glyphs via proc events and identified inscribed gear.
- Discovery tracking now also records revealed enemies and interacted NPCs, so the Journal catalog is not limited to kills.
- Added groundwork for talents: `TalentData` definitions, hero-owned talent points and levels, save/load support, and read-only talent display in Hero Info.
- Added a spendable talent UI with `WndTalents`, wired it into Hero Info, and added reusable window-content refresh support so talent changes update live.
- Implemented the first live talent effects: Warrior `Hearty Meal`, Mage `Empowering Meal`, Rogue `Cached Rations`, Huntress `Nature's Bounty`, and Duelist `Adventurer's Intuition`.
- Implemented the class intuition pickup hooks: Warrior `Tested Hypothesis`, Mage `Scholar's Intuition`, Rogue `Thief's Intuition`, and Huntress `Survivalist's Intuition`.
- Fixed gold item generation so random gold piles now keep their SPD `items.png` sprite index, and updated the item detail window to use the real item icon instead of a color placeholder.
- Replaced several remaining color/text-only item previews with sheet-backed icons in the shop window, alchemy window, and generic item-selection window.
- Extended that UI art cleanup to picker-style rows as well, including the shop sell picker.
- Fixed the hero combat pipeline so invisibility-based surprise attacks resolve before breaking stealth, and hero combat now routes through equipped weapon enchantment and armor glyph procs.
- Implemented the first combat talent hooks: Rogue `Sucker Punch`, Duelist `Patient Strike`, and Mage `Backup Barrier`.
- Implemented targeted thrown-item resolution through the hero action system, added a real `Shoot` action for `Spirit Bow`, and made Huntress `Followup Strike` a live ranged-to-melee combat hook.
- Hooked Huntress click-to-attack flow into `Spirit Bow`, so clicking a visible distant enemy now auto-shoots instead of always auto-walking into melee when a clear bow shot is available.
- Hardened run-reset and scene-loading loops against stale freed hero/mob references by avoiding typed `Node` iteration over arrays that can still contain invalid instances after death/restart transitions.
- Formalized the Huntress `Spirit Bow` as a dedicated equipment slot instead of a backpack special-case, and updated inventory/status/hero-info UI to reflect the separate melee and bow loadout.
- Fixed a regression from the bow-slot formalization where ranged attacks animated but exited before resolving because `throw_item` still only accepted melee-weapon or backpack ownership, not the dedicated `spirit_bow` slot.
- Routed wand zaps through the hero action system so targeted wand use now consumes a proper turn instead of resolving instantly from the item window callback.
- Added basic use-based wand identification, with zap usage progress persisted through save/load.
- Made wands properly equippable in the `misc` slot and upgraded quickslot behavior so quickslotted wands enter targeting directly for faster SPD-style tool use.
- Rebuilt the alchemy recipe layer so seed-to-potion and stone-to-scroll recipes now use valid item IDs, and updated the alchemy window to operate on real `Recipe` results instead of broken placeholder dictionaries.
- Wired `Alchemize` to actually open `WndAlchemy` anywhere through `EventBus.show_window`, and hooked the Alchemist's Toolkit into crafting so it gains progress from completed recipes.
- Implemented the missing hero `search` action by routing toolbar search through the normal hero action pipeline and calling the existing secret-door/hidden-trap reveal logic, so searching now spends a real turn and can actually discover adjacent secrets without duplicate signal handling.
- Improved search and door parity further: bumping/clicking adjacent impassable terrain now searches instead of failing silently, adjacent doors use the shared `Door` interaction logic, and keyboard `S` now triggers search directly.
- Added explicit search feedback in the main action animation path, so using search now shows a visible status/particle effect on the hero even when no secret is found.
- Restored base dewdrop generation from trampling high grass, instead of limiting dewdrops to the Huntress talent path; `Nature's Bounty` now boosts the baseline dew chance and can still add seed finds.
- Rebuilt the Sad Ghost quest flow: first interaction now starts the quest and spawns the target mob, kill tracking completes the objective, rewards use the standard quest reward window with real generated items, and the ghost now departs cleanly through the shared quest infrastructure.
- Fixed mimic visuals in both states: revealed mimics now use the original SPD `mimic.png` mob art, and disguised mimics now render using their configured fake loot item instead of a hardcoded generic potion icon.
- Removed the artificial per-visible-mob post-action delay from the turn manager so enemies still resolve in initiative order and update visually, but no longer add a noticeable pause before control returns to the player.
- Upgraded resting from a hidden one-turn wait into a real HUD action: the toolbar now has a Rest button, resting starts continuous auto-waiting, refuses to start while enemies are visible, and interrupts correctly on damage or when enemies come into view.
- Fixed secret-room entrance generation for tunneled branch connections: secret rooms attached as `neighbors` now get real `SECRET_DOOR` endpoint tiles instead of plain carved openings, so searching can reveal their entrances properly.
- Fixed the quickslot HUD/inventory flow: quickslot capacity now matches the toolbar, assignments persist in belongings serialization, the HUD refreshes from real quickslot state, and item windows now open a slot-selection sub-window instead of always assigning to slot 1.
- Replaced text-only quickslot labels in the toolbar with real `items.png` icon rendering plus slot-number overlays, fixing clipped item names and bringing quickslot visuals in line with the rest of the item UI.
- Fixed popup centering and basic viewport responsiveness: `WndBase` windows now fit within the current viewport and center themselves properly, the HUD now switches to a more compact portrait-friendly layout, and the hero-select scene no longer forces size on a fully anchored root control.
- Fixed dynamically spawned mob visibility for cases like Swarm splits: `GameScene` now ensures any alive mob present in the level but missing a sprite gets a `MobSprite` during turn refresh, preventing invisible logical enemies after split/spawn events.
- Expanded SPD sprite-sheet coverage in `MobSprite` for many valid mob and NPC IDs that already had art assets on disk, reducing cases where alerted or newly seen entities fell back to procedural placeholder art with health bars.
- Improved death presentation: generic character death now fades and settles instead of shrinking away aggressively, hero death now has a distinct collapse animation, and the death-scene transition waits long enough for that animation to read on screen.
- Closed several remaining item/content compatibility gaps at the generator boundary by normalizing old SPD-style IDs (for example `scroll_of_identify` and `potion_of_healing`) and adding proper quest-item generation for the wandmaker request items instead of falling through to generic unknown-item placeholders.
- Improved alert AI behavior: alarm/noisemaker-style alerts now propagate a disturbance position through `Level.alert_all_mobs()`, mobs enter hunting with a meaningful `target_pos`, and alerted mobs show the alert emote instead of merely flipping state silently.
- Tightened asset fidelity around mimic disguises by switching their fake loot IDs to the actual generator-supported IDs, which keeps disguise visuals aligned with the real item art path instead of relying on alias/fallback behavior.
- Continued the same cleanup pass by converting a few remaining live legacy scroll IDs at the callsite level (hero starter loadout and Unstable Spellbook effect table), so those paths no longer depend on alias translation.
- Improved mob behavior fidelity further: alerted mobs with no visible hero now investigate the disturbance location instead of immediately dropping back to wandering when `target` is null.
- Added SPD sprite-sheet coverage for Yog's fist variants so those boss entities no longer need procedural fallback art.
- Fixed animated/golden statue weapon generation by routing them through a real tiered melee-weapon helper in `Generator` instead of the invalid `melee_weapon` ID, so statue combat and reward drops now use actual enchantable weapons rather than generic fallback items.
- Fixed the shop inventory path so the shopkeeper now stocks real generated items with canonical IDs instead of placeholder `Item` objects with bad IDs like `scroll_mirror_image` and `potion_healing`, improving both purchased-item behavior and item-art fidelity in shop UI.
- Implemented the missing sheep-summon path shared by `Stone of Flock` and `Wooly Bomb`: added a real temporary `Sheep` mob blocker and wired both consumables to spawn sheep on valid open cells instead of only printing placeholder messages.
- Replaced three remaining spell placeholders with live behavior: `Recycle` now opens item selection and transmutes consumables into others of the same broad class, `Curse Infusion` now curses and upgrades a selected item, and `Summon Elemental` now creates a temporary allied fire or frost elemental that follows the hero and attacks enemies.
- Cleaned a large launch-warning batch across gameplay, UI, and utility scripts: fixed unused parameters/locals, several local-name shadowing issues, and the integer-division warnings reported from `char.gd`, `scroll.gd`, `bomb.gd`, `wand.gd`, `item_slot.gd`, `ballistica.gd`, and related files.
- Implemented two more previously placeholder runestones: `Stone of Enchantment` now applies a random weapon enchantment to the equipped melee weapon or spirit bow, and `Stone of Augmentation` now lets the player pick a weapon/armor target and choose a real augment through a new augmentation selector window.
- Fixed missing enemy attack animations by making mobs record explicit last visible action metadata (`move` vs `attack`) and having `GameScene` animate from that data instead of trying to infer attacks from the mob's post-action `target` state.
- Extended that mob-attack animation fix to cover misses too, so visible enemy melee swings now animate whether they hit or miss instead of only when `on_attack_hit()` fired.
- Replaced the immediate-detonation bomb placeholder with real fuse timing: armed bombs now live on the `Level`, tick once per hero round via `TurnManager.round_completed`, detonate before enemy turns, and persist through `RegularLevel` save/load.
- Added visible telegraphing for armed bombs: `GameScene` now renders a separate floor sprite for any bomb in `Level.pending_bombs`, applies normal fog/visited visibility rules, and shows a small `Fuse` popup when a bomb becomes active.
- Implemented the real cast path for `Unstable Spellbook`: it now instantiates the chosen scroll ID and applies that scroll's `read_scroll()` effect directly to the hero instead of only logging the chosen effect and emitting a generic event.
- Wired equipped artifact hooks into the real hero loop: `Hero.execute_action()` now calls `artifact.on_turn(self)` each hero turn, and search actions now call `artifact.on_search()` when supported, which makes artifacts like `Talisman of Foresight` actually charge and apply their passive/turn-based behaviors in play.
- Implemented a real terrain-growth effect for `Sandals of Nature`: passive charged movement can now convert `EMPTY`/`EMBERS` into `GRASS` and upgrade grass into `HIGH_GRASS`, while activation now triggers a small burst of nearby growth instead of only printing flavor text.
- Completed the basic `Sandals of Nature` charge loop in the UI: seed items now show a `Feed` action in `WndItem` when the sandals are equipped, which consumes one seed from the stack and routes it into `SandalsOfNature.feed_seed(...)`.
- Implemented the real `Master Thieves' Armband` loop: positive gold gain now routes through `on_gold_pickup()` when the armband is equipped, and activation now performs an actual adjacent-shopkeeper theft attempt that steals a real stock item on success or forces the shopkeeper to flee on failure.
- Fixed a boss-AI timing mismatch: custom `_act_hunting()` overrides for `Goo`, `Tengu`, `DM-300`, `King of Dwarves`, and `Yog-Dzewa` now explicitly spend turn/move/attack time for their special actions, restoring fair cooldown behavior under the shared turn system.
- Replaced the placeholder thief/bandit steal behavior with real inventory theft: `Crazy Thief` and `Crazy Bandit` now remove an actual backpack item (or one unit from a stack), flee after stealing it, and drop that stolen item on death instead of only toggling a boolean placeholder.
- Implemented a real first-pass `Necromancer` AI loop: it now acquires a hero target, raises one linked `Skeleton`, keeps that skeleton focused on the hero, periodically heals or hastes it, retreats when pressured, and destroys the linked skeleton when the necromancer dies.
- Fixed the same turn-cost mismatch across several non-boss ranged mobs: `Gnoll Shaman`, `Scorpio`, `Gnoll Trickster`, and `DM-100` now explicitly spend attack/move/idle time inside their custom `_act_hunting()` logic instead of performing ranged/kiting behavior without consuming proper turn cost.
- Tightened two more special-mob behaviors: `Succubus` teleport-escape now consumes the turn instead of teleporting and then also taking a full normal action, and `Stone Golem` now emits `hero_moved` when its on-hit teleport displaces the hero so the rest of the game state/UI stays in sync.
- Improved cave/prison special behavior: `Spinner` now tracks the hero's last seen position and shoots predictive webs that also seed a real `WebBlob`, instead of only dropping `WEB` terrain on the hero's current tile, and `Prison Guard` chain-pull now consumes proper attack time.
- Implemented the missing `Timekeeper's Hourglass` scheduler effect in `TurnManager`: while the hourglass freeze is active, non-hero actors are skipped by the turn loop and still advanced through cooldown accounting, so time-freeze now actually stops enemy actions instead of only tracking local artifact state.
- Wired artifact `on_floor_change()` hooks into stair transitions in `GameScene`, so per-floor artifacts like `Dried Rose` can now reset/refresh their state when moving between dungeon levels instead of carrying stale per-floor state forever.
- Hardened the save/backtrack reconstruction path: `LevelFactory` now supports subclass instantiation without generation, `SaveManager` recreates the correct level subclass on cold load, `Level` now serializes/deserializes mobs/traps/plants/blobs/armed bombs directly, base `Mob` deserialization now restores core combat/AI state instead of being a stub, and cached-level loading in `LoadingScene` no longer stacks fresh quest/shop spawns on top of restored level state.
- Fixed Sad Ghost/NPC interaction regressions: adjacent NPC clicks in `GameScene` now route to `interact` instead of the generic adjacent-attack path, and `SadGhost` now uses its own wandering turn loop/state handling instead of inheriting the passive NPC act path that left it stationary and non-behaving.
- Fixed a death-phase turn-loop bug: `TurnManager` now aborts the async enemy-processing loop if the hero dies or the `GameScene` has already ended, and `GameScene._on_hero_died()` now explicitly drops turn-manager processing flags so the floor cannot keep resolving visible enemy turns during the death-scene delay.
- Continued save hardening: `SaveManager` no longer restores blobs twice after the level serializer absorbed them, hero/mob active buff state now persists through save/load using script-path-based buff reconstruction, and `Necromancer` now relinks its summoned skeleton after level load using saved `actor_id` references instead of dead runtime instance IDs.
- Closed a follow-on persistence hazard in actor identity: deserializing an `Actor` now bumps the global `_next_id` floor to at least `actor_id + 1`, preventing newly spawned mobs/allies/projected actors after load from colliding with restored actor IDs.
- Extended persistence into the quest/NPC layer: base `NPC` save/load now restores quest flags and re-registers restored quest NPCs with `QuestHandler`, and custom state for `Sad Ghost`, `Wandmaker`, `Blacksmith`, and `Shopkeeper` now persists instead of falling back to generic mob-only restoration.
- Repaired the Imp quest path: `Ambitious Imp` now has a coherent single-ring reward flow with real generation, reward handoff, departure, and save/load state, instead of referencing undefined reward fields/functions; also fixed `QuestHandler` to call `Wandmaker.create_quest_item(...)` instead of the non-existent `create_quest_seed(...)` when spawning the wandmaker quest item.
- Continued the quest/NPC runtime cleanup: `Wandmaker` now generates real wand rewards with canonical IDs, uses correct inventory-ID/quantity checks for handing over quest items, and only completes the quest when a reward is actually chosen; `WndQuestReward` now emits an explicit reward-selection signal so quest NPCs no longer incorrectly depart just because the window closed. `Blacksmith` now uses a real `WndReforge` flow, consumes ore through quantity-safe inventory removal, and applies the actual reforge rule/cleanup through the NPC logic instead of leaving reforging as a dead text-only branch.
- Added a real root `README.md` for the project, documenting what the port is, current `0.1.1` status, how to run it in Godot 4.5, the repository layout, current goals, and the main known rough edges.
- Closed a major quest-facing UI/runtime gap in the shop flow: `Shopkeeper.interact()` now opens the existing `WndShop` instead of dumping stock to the message log, `WndShop` now delegates purchases and sales back through the `Shopkeeper` so inventory/persistence state stays authoritative, and shop sell pricing now uses actual item value instead of a missing `price` property on inventory items.
- Implemented a real first-pass `Dried Rose`: added a summonable allied `RoseGhost` mob, wired the artifact to spawn and track that ghost instead of only toggling local flags, added cleanup on floor change, added post-load relinking from the equipped artifact back to the restored summoned ghost, and added SPD ghost-sheet art mapping for the ally.
- Implemented a real targeted first pass for `Ethereal Chains`: activation now enters the existing targeting mode, then either pulls the hero to a valid open destination with a clear chain path or yanks a visible enemy toward the hero into the nearest valid adjacent landing cell, spending charge only on successful use instead of only emitting placeholder text.
- Continued save/load hardening for special mobs: `Swarm` now persists whether it has already split, and `Mimic` now persists disguise state, fake item appearance, and stored loot contents instead of restoring as a generic/default mimic after load.
- Continued prison-mob persistence hardening: `Crazy Thief` and `Crazy Bandit` now serialize their stolen item payloads so post-load fleeing, death drops, and item recovery still work instead of losing the stolen inventory state across save/load.
- Continued summon/special-mob persistence hardening: temporary `Bee`, `Summoned Elemental`, `Sheep`, and `Animated Statue` state now survives save/load, including hero-link relinking for allied summons and weapon restoration for statues.
- Continued cooldown/phase persistence hardening for stateful mobs and bosses: `Warlock`, `DM-100`, `Spinner`, `Evil Eye`, `DM-300`, `King of Dwarves`, `Tengu`, and `Yog-Dzewa` now restore their active cooldown and phase fields instead of resuming from default encounter state after load.
- Extended that persistence hardening across the remaining encounter mobs with custom runtime state: `Shaman`, `Gnoll Trickster`, `Scorpio`, `DM-200`, `DM-201`, `Ripper Demon`, `Dwarf Monk`, and `Yog Fist` now restore their cooldown/type/combo/leap state correctly after save/load.
- Continued save/load hardening into buff state: custom counters and embedded state now persist for `Chill`, `Bleeding`, `Ooze`, `Burning`, `Regeneration`, `Combo`, and several subclass buffs including `Berserker Rage`, `Champion Dual Wield`, `Freerunner Momentum`, `Gladiator Combo`, `Monk Flurry`, `Sniper Mark`, `Warden Barkskin`, and `Assassin Preparation`.
- Fixed a real quest restore sequencing bug in `SaveManager`: saved `QuestHandler` state is now deserialized before level/NPC reconstruction, so restored quest NPCs can re-register themselves without that registration being wiped by a later quest-state reset during load.
- Fixed a live NPC interaction regression: hero `interact` actions now actually call adjacent NPC `interact(...)` handlers instead of only opening doors, and keyboard bump movement now routes into NPC interaction instead of trying to attack passive quest/shop characters.
- Added a clear message when a floor has the `DARK` feeling, so intentionally darker floors are surfaced as gameplay state rather than looking like a silent visual regression.
- Wired missing gameplay/UI keybinds in `GameScene` and `HUD`: `I` now toggles inventory, `M` toggles the minimap, `R` triggers resting, `Esc` opens settings when no popup is active, and number-row `1`-`6` now activate quickslots.
- Replaced the old map toggle behavior with a real centered map overlay: `M` and the map toolbar button now open `WndMap`, which shows an enlarged current-floor map instead of merely hiding the small corner minimap widget.
- Hardened `Minimap` for the new popup-map path by making its backing image/texture initialize lazily, fixing the null-image crash when the enlarged map window populates a fresh minimap before `_ready()` runs.
- Restored several spell behaviors that had regressed to placeholders: `Recycle` now transmutes a selected consumable into another of the same broad class, `Curse Infusion` now curses and upgrades a selected weapon/armor while enabling the infusion bonus, and `Summon Elemental` now spawns a real allied fire or frost elemental instead of only printing a message.
- Added a real `Level.spawn_mob(...)` helper and used it to close several trap-effect gaps: `Flock Trap` and `Guardian Trap` now have a working spawn path, and `Summoning Trap` now actually spawns hostile mobs instead of stopping at a placeholder comment.
- Implemented the missing blob visual layer in `GameScene`: active gas/fire/web cells from `Level.blobs` now render as visibility-aware colored overlays between the terrain and entity layers, so toxic gas, paralytic gas, confusion gas, fire, webs, and similar blob effects are finally visible on the map instead of existing only in gameplay logic.
- Tuned that new blob overlay so it only renders on currently visible cells instead of faintly tinting previously visited ones, which avoids making normal floors read darker than intended; also updated `CharSprite.play_attack()`/`play_operate()` so heroes and mobs visibly face the target of an attack or interaction even when they do not move first.
- Closed the next parity gaps: `Reclaim Trap` now creates a real serialized `TrappedItem` that can be placed later instead of only removing floor terrain, Halls floors now cap the hero's sight radius using the original depth-based rule, lingering bad loot IDs in `Necromancer` and `Scorpio` were normalized to canonical item IDs, and `Ballistica`/wand targeting now support ignoring soft solids like doors/webs when requested instead of treating every solid the same.
- Fixed a concrete duplicate-food regression: `Rat` loot was still using the invalid legacy ID `food_ration`, which produced a generic fallback item with the wrong name/art; it now uses canonical `ration`, so food drops resolve to the real SPD-backed ration item consistently.
- Enlarged the popup map window by making `WndMap` scale from the current viewport instead of using a fixed small multiplier, so the opened map now takes much more of the screen on desktop while still fitting smaller/mobile-like windows.
- Continued the legacy-item-ID cleanup pass: `Swarm` now drops canonical `healing` instead of invalid `healing_potion`, and `Skeleton` no longer uses the fake `weapon_t2` ID at all — it now rolls a real tier-2 weapon drop directly, preventing more generic fallback items and wrong art/name cases.
- Tuned early-floor presentation and keyboard flow: Sewer floors no longer roll the `DARK` feeling on depths 1-2, which avoids “first/second floor looks broken” reports, and `Enter` / numpad `Enter` now use stairs contextually so keyboard play can descend or ascend without requiring symbol keys.
- Continued the item-fallback cleanup by making `metal_shard` a real misc item in the generator instead of an unknown ID, so `DM-100`/`DM-201` shard drops no longer resolve to generic placeholder items.
- Audited and corrected a major water-generation parity gap: standard rooms no longer flood their entire interior on `Feeling.WATER`, and `StandardPainter` now paints region-style smoothed water patches for Sewers, Caves, and City using fill/smoothness values aligned with upstream SPD instead of the previous flat random scatter.
- Audited mob spawn IDs and fixed a concrete fallback-enemy path: `Sad Ghost` quest targets `fetid_rat` and `great_crab` now resolve to real mob classes in `MobFactory` instead of falling back to the base `Mob`, and a new `FetidRat` sewer mob class was added so the ghost quest can no longer produce a generic “mob” enemy with procedural fallback art/name.
- Hardened `MobFactory` so unknown explicit `mob_id` requests no longer silently instantiate the generic base `Mob`; they now return `null` with a warning, while weighted random regional spawns still fall back safely to `Rat` only if a selected table entry somehow resolves to nothing.
- Fixed the remaining Imp presentation gap: `ambitious_imp` now uses the original SPD `demon.png` sprite sheet in `MobSprite` instead of falling back to generated art.
- Improved plant presentation without inventing new assets: `PlantSprite` now reuses the original SPD seed icons from `items.png` as embedded plant blossoms where available, instead of drawing every plant entirely procedurally. This repo does not include dedicated upstream plant sprites, so the seed icons are now the closest canonical art path.
- Upgraded blob/gas/fire/web presentation in `GameScene`: the blob overlay layer no longer draws flat tinted rectangles per cell and now renders layered puff/strand shapes for gas, fire, and webs so room-filling effects read more like visible hazards and less like placeholder map tinting.
- Replaced the `WndHeroInfo` portrait placeholder with a real SPD-backed hero portrait: the info window now crops the hero's actual class sprite sheet instead of showing a flat class-color block, bringing that UI path onto the same original-art route as the status pane and hero selection screen.
- Closed a remaining summon-art gap: `Mirror Image` now records the copied hero class and `GameScene` renders it with a tinted `HeroSprite` instead of a generic `MobSprite`, so the summon now looks like a spectral copy of the player rather than falling back to generated enemy art.
- Tightened hero portrait fidelity further by switching both `StatusPane` and `WndHeroInfo` to use the real SPD `avatars.png` portrait sheet instead of cropped in-world sprite frames, so hero-facing UI now uses the original portrait art path rather than a gameplay-sprite approximation.
- Fixed a live buff regression: `Burning` no longer calls `take_damage()` with an invalid third argument, which was crashing when fire-based potion effects or other burning damage ticked on the hero.
- Fixed secret-trap clarity on activation: `Trap.activate()` now force-reveals hidden traps before they fire, and `GameScene` now refreshes the terrain/fog state when a trap triggers instead of only playing the trap sound, so poison/worn dart traps should become visibly revealed once they go off.
- Bumped the actual game-facing version to `0.1.1`: updated the title-screen version label and About text, added `config/version="0.1.1"` to `project.godot`, and set export `file_version` / `product_version` metadata so UI, project settings, and exported builds no longer disagree.
- Added a durable co-op implementation roadmap for `0.1.2` in `docs/memory/multiplayer-roadmap.md`, defining the project target as host-authoritative online co-op for up to 4 players, with phased implementation starting from single-process multi-hero support before any networking layer is added.
- Started Phase 1 co-op refactoring in code: `GameManager` now has a real party helper API (`get_primary_hero`, `get_local_hero`, `get_active_heroes`, `add_hero`, `remove_hero`, `set_local_hero_index`, `is_party_run`) plus `local_hero_index`, giving multiplayer work a first-class seam instead of only raw `hero` / `heroes` fields.
- Moved the first low-risk consumers onto that seam: `TurnManager`, `StatusPane`, and parts of `HUD` now read from party-aware hero accessors instead of hardcoding `GameManager.hero` everywhere, which begins separating “local focused hero” from “primary hero” behavior.
- Bumped the project branch target to `0.1.2`: updated title/About UI strings, `project.godot` version metadata, export file/product version metadata, and top-level `README.md` milestone text so the repo and game no longer still advertise `0.1.1`.
- Continued Phase 1 in `GameScene`: added a local-hero accessor and moved the main input, action submission, camera/FOV refresh, auto-walk, item pickup, plant activation, trap-refresh, stairs, targeting, and hero-damage/death presentation paths onto that seam instead of hardcoding `GameManager.hero` throughout the interaction loop.
- Hardened the `GameScene` launch path after the co-op refactor introduced parser brittleness: removed most custom-class type annotations from `game_scene.gd`, replaced direct `NPC`/`CharSprite` type checks with duck-typed behavior checks, and updated `MainScene`, `TitleScene`, and `HeroSelectScene` to load `game_scene.gd` dynamically instead of requiring the global `GameScene` class at parse time just to open menus or continue a run.
- Continued that parser-hardening pass into `GameScene` and `HUD`: `game_scene.gd` now instantiates its major helpers and sprites by script path instead of direct custom-class constructors, no longer depends on `Gold`/`Wand`/`MissileWeapon`/`Bomb` type checks for core flows, and `hud.gd` now dynamically instantiates its subcomponents and popup windows while using duck-typed quickslot/window handling instead of direct `Wnd*`, `Wand`, and `Belongings` class references.
- Continued `v0.1.2` co-op groundwork in the actual turn model: `TurnManager` now tracks the current input hero explicitly, `GameManager.get_local_hero()` follows that active hero automatically, and hero-round accounting now treats a round as “all currently registered heroes have acted once” instead of implicitly assuming a single hero. `Hero` now reports itself back through `hero_action_complete(self)`, and key visibility/UI readers like `FogOfWar`, `WndMap`, `WndInventory`, `WndHeroInfo`, `WndAlchemy`, `WndShop`, `WndTalents`, and `WndQuestReward` now read the focused/local hero through the new party-aware accessor instead of hardcoding `GameManager.hero`.
- Added an observable local-hero focus layer for the multiplayer groundwork: `GameManager` now exposes `local_hero_changed`, `get_hero_index(...)`, and `cycle_local_hero_focus(...)`; `TurnManager` updates `GameManager` when a different hero reaches the input phase; and both `HUD` and `GameScene` now react to focus changes so the status UI, FOV, fog, and camera follow the currently active local/input hero cleanly.
- Added hero-specific event payload groundwork for co-op: `EventBus` now exposes `hero_moved_detailed`, `hero_damaged_detailed`, and `hero_died_detailed`; the current hero movement/damage/death emitters now send detailed payloads for the actual hero involved while the legacy single-hero signals only mirror the currently focused/local hero; and `GameScene` plus `Minimap` now prefer the detailed hero signals so future multi-hero runs can distinguish local/focused hero updates from other party members cleanly.
- Added the first explicit local-party focus control: `GameScene` now uses `Tab` to cycle the locally focused hero when a party run is active, and `StatusPane` now shows a small `Focus: P#/N` indicator for the currently focused party member. The portrait path also now reads the focused hero's actual `hero_class` instead of falling back to the run-global class field.
- Added the first lightweight party HUD strip in `HUD`: party runs now show compact top-row hero buttons with class/HP summary, the currently focused hero is highlighted, and clicking a party entry changes the local hero focus through `GameManager.set_local_hero_index(...)`. The row also repositions responsively for narrower/mobile-like layouts.
- Centralized party bootstrap for future co-op runs: `GameManager` now owns `party_classes`, `set_party_classes(...)`, `get_party_classes()`, `create_party_heroes()`, and `replace_party(...)`, while `LoadingScene` now creates party heroes through that API and places the whole party around the staircase anchor instead of only a single hero. `HeroSelectScene` and `MainScene` now route new-game startup back through `LoadingScene`, so the run bootstrap path is unified instead of duplicating one-hero setup logic across scenes.
- Continued the multiplayer roadmap’s co-op readability pass by adding in-world ally identification: `CharSprite` now supports a persistent ally label separate from temporary emotes, and `GameScene` now applies party-aware labels like `P1 Alice`/`P2 Mage` to hero sprites in multi-hero runs, with color highlighting for the focused hero, the locally owned hero, and fallen heroes so party members are easier to distinguish directly on the dungeon floor.
- Continued that readability pass into the HUD: the online-state banner now uses the same `P# Name` hero identity format as the new floor labels, including `Your turn: ...`, `Waiting for ...`, and `Spectating ...` states, and the party row now marks focus, active turn, and local ownership consistently so control/ownership is readable without guessing from context.
- Continued the readability pass into the world presentation itself: `CharSprite` now supports subtle ground rings, and `GameScene` now applies them to party heroes in multi-hero runs so active turn, local ownership, focus, ally state, and fallen heroes are distinguishable at a glance even before reading labels or HUD text.
- Continued online authority polish by adding explicit host-to-client action rejection feedback: `NetworkManager` now has a dedicated `online_action_rejected` channel, host-side slot/turn validation now sends concrete rejection reasons like `You do not control that hero.` or `It is not that hero's turn.`, and `GameScene` now surfaces those failures to clients through the message log plus a `Blocked` status popup instead of silently dropping the input.
- Continued authority hardening in the client UI: `Toolbar` now supports action-only disabling, and `HUD` now dims/disables inventory, wait, rest, search, and quickslot controls whenever the local player does not currently own the active hero in an online run, while still leaving passive controls like map/settings usable. The guarded HUD entry points also now show clear warnings instead of opening action-heavy UI out of turn.
- Extended that authority clarity into the raw input loop: `GameScene` now still allows passive keys like `Tab`, `I`, `M`, and `Esc` while out of turn, but online clients now get explicit `Wait` / spectating / wrong-turn feedback for blocked action keys and map clicks instead of silent dead input when another player owns the active hero.
- Reduced online snapshot harshness on the client by changing same-floor host snapshots from full scene rebuilds to incremental entity sync in `GameScene`: heroes and mobs now keep their existing sprites and animate toward corrected positions when possible, while full teardown/rebuild is reserved for real floor/depth transitions.
- Improved remote combat presentation during online snapshot sync: `Hero` and `Mob` now serialize lightweight `last_visible_action` / `last_visible_target_pos` state, and `GameScene` uses snapshot deltas to replay fresh remote attack/interact animations plus damage/heal feedback on clients instead of only updating bars and final positions.
- Expanded that remote action replay beyond melee: heroes now record visible `throw_item` and `zap_wand` actions too, and `GameScene` now replays ranged/utility snapshot deltas with thrown-projectile arcs, wand lightning, search pings, and interact/operate animations on clients so other players’ turns read much more like real actions instead of silent state corrections.
- Tightened online movement-event presentation on non-host clients: hero_moved world events no longer only nudge a sprite, and now also drive immediate camera/FOV/fog/tile-visibility refresh when the moved hero is the currently focused hero. This uses a client-preview visibility refresh path that respects sprite preview positions instead of snapping mobs back to stale authoritative positions before the next host snapshot arrives.
- Added held-key movement repeat in GameScene: when an arrow/numpad/vi movement key is held, the acting hero now repeats movement on a fixed 0.5s cadence while input is available, instead of requiring repeated taps. The repeat path is processed for both offline play and online clients, but still respects targeting mode, auto-walk, and turn ownership.
- Refined held-key movement repeat in GameScene: repeat cadence is now 0.25s instead of 0.5s, and overlapping movement keys now fall back correctly. Holding one direction, pressing a second, then releasing the second will resume repeating the still-held original direction instead of stopping movement entirely.
- Fixed delayed hero equipment visuals: equipping or unequipping armor no longer waits for a depth transition to update the hero sprite. GameScene now reapplies armor-tier visuals to live hero sprites on equipment change events and during online snapshot hero-sync, so local and remote loadout changes refresh immediately.
- Continued multiplayer QoL in the online setup flow: NetworkManager now exposes ready-count summaries, LobbyScene now shows ready progress plus [You] ownership markers in the player list, and the host start button surfaces readiness state directly instead of looking generically disabled. HeroSelectScene now also shows clearer host/client setup messaging, including ready counts for the host and the actual host player name for waiting clients.
- Matched the co-op lobby to the title screen background treatment: LobbyScene now uses the same SPD parallax dungeon layers and the same absolute-time scroll model as TitleScene, so navigating between menu and lobby no longer resets to a different background phase. Also fixed the lobby layout by centering the main panel and changing the action buttons to a 3-column grid, which restores balanced left/right margins instead of letting the panel stretch visually to the right.
- Reworked HeroSelectScene to share the same title-style parallax dungeon background treatment as the main menu and co-op lobby, and switched the layout to a framed two-column presentation: a left info/selection column over a translucent panel and a right framed splash-art column for the selected hero. The shared background now uses absolute-time parallax scrolling so menu-to-lobby-to-hero-select navigation no longer visibly resets the backdrop phase.
- Unified the hero-select two-column layout into one framed composition: HeroSelectScene now draws a single outer panel behind both the left info column and the right splash-art column, so it reads like one box in the same style family as the co-op lobby instead of two separate floating panels. The left column was softened into an internal section, while the right splash panel remains inset within the shared frame.
- Tightened the hero-select presentation again: the framed UI now matches the co-op lobby width more closely, and the right splash-art pane was narrowed into a taller portrait-style window so the hero art is cropped toward the center instead of reading like a wide banner. Also added a real Settings button to the title screen that opens the same wnd_settings.gd popup used in-game, so music volume and other runtime settings are accessible directly from the main menu.
- Fixed the hero-select framed layout so the actual selection controls live inside the shared outer box instead of being absolute-positioned on the scene root. The left-side title, class buttons, descriptions, party controls, and action buttons are now parented under the left panel section, and the splash pane is positioned as an internal section of the same framed container.
- Fixed the hero-select frame sections to use plain `Panel` controls instead of `PanelContainer`, so the right-side splash no longer auto-expands over the whole frame and the left-side hero choice controls can render beside it as intended.
- Tightened the hero-select splash pane so it behaves like a true right-half portrait panel: widened it to occupy the right half of the shared frame, aligned it to the frame midpoint, removed the inner image margin, and enabled clipping so the splash art is center-cropped inside the pane instead of spilling past the frame.
- Centered the hero-select splash crop explicitly inside the right pane by manually sizing the image to covered-fit the panel and offsetting it to the pane center, so portraits with empty left/right margins now keep the hero centered in the cropped frame.
- Removed the local party-size controls from `HeroSelectScene`, so the standard new-game flow is now a single-hero class choice screen again while online party size remains driven by actual lobby population.
- Reworked `LobbyScene` into the same framed split layout as hero select: a left information/control column inside the shared box and a right splash-preview column. The right side now stacks 1-4 centered, clipped splash panels vertically based on the current lobby population, so two-player lobbies split the right half into top/bottom portraits and four-player lobbies split it into four equal portrait bands.
- Constrained the co-op lobby host/player action controls to the left column width by changing the lobby action grid to a 2-column layout with column-sized buttons, so the controls no longer overflow the framed left half.
- Added a real persistent `PlayerProfile` autoload, which now owns the player name used for multiplayer identity and profile summary data. TitleScene now includes a `Player Profile` section with name editing, achievement/ranking summary, and badge access, and first-time players are prompted to choose a name before using the rest of the menu flow.
- Rankings entries now store and display `player_name`, so the new profile identity carries through into persistent run history instead of only existing in multiplayer settings.
- Tightened the co-op lobby left column spacing further by shrinking the content width and button grid to leave a larger gutter from the center split.
- Removed the remaining party-loadout presentation from the normal single-player hero select flow. `HeroSelectScene` now hides party slot/loadout UI, summary, and network notice when not in online setup, so `New Game` reads as a true single-hero choice screen while the online flow can still surface coordinated class setup separately.
- Reworked the co-op lobby action layout so the important controls sit in predictable corners: `Leave` now anchors the bottom-left action slot, while the bottom-right slot is used for `Ready Up` on clients and `Start Run` for the host. The player list was also simplified from `P1 Name [Host] [You] - ...` down to cleaner `Name [Host] - ...` labels.
- Removed join-code language from the co-op join flow. The title-screen join panel now uses `Host IP` plus port directly, the lobby no longer exposes `Copy Join Code`, and host state messaging now surfaces the host IP instead of a generated join code.
- Restyled `RankingsScene` to match the newer menu pages more closely by giving it the same parallax dungeon background treatment and a centered framed panel for the list and actions instead of the older flat full-screen layout.
- Tuned `WndSettings` toward the same visual language with a roomier layout and warmer label treatment so the shared settings window reads more consistently alongside the newer menu/profile surfaces.
- Audited the main-menu submenu action language and standardized several remaining panels: the co-op lobby now uses `Back` instead of `Leave`, the join panel is reduced to a simple host-IP join flow with `Back` / `Join`, the profile panel now uses secondary links above a bottom `Back` / `Save` row, and the About panel now uses `Back` language instead of `Close`.
- Fixed the remaining main-menu consistency gaps: `HeroSelectScene` now keeps `Back` on the left and `Start` on the right in the standard new-game flow, and the title-screen settings entry no longer opens the animated in-game popup window. `TitleScene` now has its own framed `Settings` submenu panel with the same `Back` / `Save` bottom-row structure and the same static menu-page behavior as the other title submenus.
- Hardened standard floor generation against unreachable stairs. `RegularLevel` now validates that a real path exists from the entrance to `exit_pos` immediately after painting/flag-map build, and aborts generation if the stairs-down room ended up graph-disconnected. That forces a regenerate instead of allowing a softlocked floor like the sealed depth-4 case.
- Tightened the generation retry path in `LevelFactory`: floor creation now gets multiple full regeneration attempts before the emergency fallback room is used. That means a rejected unreachable layout now normally rerolls into another real floor instead of immediately collapsing to the plain single-room fallback.
- Continued multiplayer authority hardening through NPC interaction by making the shop flow owner-targeted and host-authoritative. `NetworkManager` now supports targeted online UI events, `Shopkeeper` sends remote shop-open events to the interacting hero's owner instead of opening host-local UI, `WndShop` routes online buy/sell actions through `request_hero_action`, and `GameScene` now resolves `shop_buy` / `shop_sell` on the host and pushes targeted shop refresh events back to the owning client.
- Extended that same owner-targeted UI / host-authoritative pattern to quest reward windows. `Ghost`, `Imp`, and `Wandmaker` now send targeted reward-open UI events for remote owners, `WndQuestReward` routes online selections through a new `claim_quest_reward` action instead of granting items locally, and `GameScene` now resolves the reward claim on the host and invokes the NPC completion callback there.
- Added an `Exit to Menu` button to the in-game settings window, so players can leave directly from settings without backing out through the pause menu first.
- Continued the NPC interaction authority pass with `Blacksmith`: the reforge window is now owner-targeted for remote players, `WndReforge` routes online reforges through a host-owned `blacksmith_reforge` action, and `GameScene` now resolves that action on the host with rejection feedback if the reforge is refused.
- Standardized in-game settings button order to `Back`, `Save`, `Exit to Menu`, so the settings window now follows the same primary/secondary action order the rest of the menu flow uses.
- Tightened the multiplayer feedback path inside `GameScene`: host-authoritative inventory/reward actions now use a hero-targeted message helper for cursed-item removal failures, full-inventory failures, drops, and quest reward receipt, so remote players see those action results on their own client instead of only the host receiving the message.
- Continued multiplayer authority cleanup for item/spell subwindows: `Scroll of Transmutation` now opens a targeted transmutation window for remote owners, `WndTransmute` now routes confirmed transmutes back through a host-owned `transmute_item` action, `Alchemize` now opens a targeted alchemy window for remote owners, and `WndAlchemy` now routes brewing through a host-owned `alchemy_brew` action. `GameScene` now normalizes those item-ref payloads, resolves both actions on the host, and sends the resulting success/failure feedback back to the owning player instead of letting those systems mutate locally on clients.
- Extended the same selector-window multiplayer pattern to more remaining item/spell flows: `WndItemSelect` now supports host-owned online selections, `Scroll of Identify` and `Scroll of Upgrade` now open targeted remote selectors instead of only host-local windows, and `Recycle` / `Curse Infusion` now do the same. `GameScene` now resolves `identify_item`, `upgrade_item`, `recycle_item`, and `curse_infusion_item` on the host and sends the resulting feedback back to the owning player instead of leaving those actions on old single-player callbacks.
- Simplified the main menu structure in `TitleScene`: `Host Co-op` / `Join Co-op` are now collapsed into one top-level `Co-op` entry, and the top action row now puts `New Game` and `Continue` on the same line with a wider `New Game` button and a narrower `Continue` button when a save exists, while `New Game` expands to full width if no save is present.
- Continued multiplayer selector-window cleanup for stones: `Stone of Enchantment` now uses the same targeted remote selector flow as other host-owned item picks, and `Stone of Augmentation` now supports a full two-step remote flow with targeted item selection followed by a targeted augment-choice window. `WndAugmentSelect` now supports host-owned online selections, and `GameScene` now resolves the resulting stone enchantment and augmentation actions on the host instead of leaving those flows local-only.
- Continued multiplayer NPC authority hardening by adding targeted `npc_message` delivery: NPC base class now routes dialogue and feedback to the interacting hero’s owning peer when the host is handling a remote interaction, `GameScene` now logs those targeted UI events on the client, and the core shop/ghost/imp/wandmaker/blacksmith interaction and reward/reforge messages now follow the player who triggered them instead of only appearing in the host log.
- Removed the redundant top-level Rankings button from TitleScene, so rankings are now accessed only through the Player Profile area instead of appearing in two separate title-menu locations.
- Folded co-op into the `New Game` flow as a default-off toggle under the main start row, removed the separate `Co-op` and `About` title-menu entries, and made `New Game` open the co-op host/join panel only when that toggle is enabled.
- Continued multiplayer direct-effect hardening by routing a larger set of scroll and runestone result messages through owner-targeted UI delivery, so remote players now see success/failure feedback for effects like remove curse, magic mapping, teleportation, recharging, intuition, blink, clairvoyance, sleep/disarm, flock, and shock on their own client instead of those results only appearing in the host log.
- Moved co-op entry out of the title screen and into `HeroSelectScene`: `TitleScene` no longer has a co-op toggle or About entry, `New Game` always opens hero select, and hero select now exposes `Host` / `Join` controls above the normal `Back` / `Start` row.
- Added a small inline join panel to `HeroSelectScene` for host IP + port entry, and wired hero-select online session start/connect signals to transition straight into the co-op lobby after hosting or joining.
- Refined `HeroSelectScene` layout to give co-op its own framed subsection inside the left column, with a titled host/join area and the inline join panel nested inside it. This reduces the visual jump from single-player hero select to the hosted lobby and gives the co-op controls clearer separation from the normal start/back actions.
- Reworked the multiplayer entry flow again: `TitleScene` now owns a dedicated `Multiplayer` submenu under `New Game`, with `Open Lobby` as the primary action and `Join Lobby` using lobby IP + port. This keeps hosting as a backend detail while leaving hero select focused on class/loadout instead of network session setup.
- Removed the temporary host/join controls and inline join panel from `HeroSelectScene`. Hero select is back to its normal single-player layout unless it is entered from an already-active online session, where it only shows the online party/loadout state.
- Expanded `PlayerProfile` into a real profile identity shell: it now persists a selected profile icon plus unlocked profile icons, defaults to the Warrior icon, and unlocks additional class icons from badge progression milestones over time instead of treating profile identity as only a plain text name.
- Reworked the title-screen Player Profile panel to use a proper profile header instead of an always-editable text box. The player name is now shown as a large display label with a small edit button that reuses the profile-name prompt, and a new profile-icon panel sits beside it with its own small edit control for cycling currently unlocked icons.
- Moved online class choice into `LobbyScene` itself. The co-op lobby now has a top-row class selector matching the new-game hero buttons, class choice is synchronized through `NetworkManager` lobby state, right-side lobby splash previews now reflect each connected player's chosen class, and host start now launches the online run directly from the lobby using those synchronized class selections.
- Added a first-pass controller scheme for the title screen, hero select, and co-op lobby. D-pad navigation and face-button accept/cancel now work on those menu surfaces, and the lobby also supports quick class cycling plus a controller shortcut for opening lobby settings.
- Simplified multiplayer entry again so pressing `Multiplayer` on the title screen now immediately creates the local player's own hosted lobby instead of opening a separate host/join setup panel. Joining another session is now handled from inside `LobbyScene` with a bottom-row `Join Lobby` action and an inline join overlay for host IP + port.
- Extended synchronized lobby identity to include the player's selected profile icon as well as class choice. `NetworkManager` now carries profile icon IDs in lobby state, class selection persists across hosted/joined lobbies through saved local lobby preferences, and the co-op lobby player list now shows actual profile icons beside each player's row instead of only plain text.
- Closed another multiplayer setup gap by removing the extra online hero-select step from the main host flow. The lobby now owns the core pre-run multiplayer state directly: host opens lobby, players pick classes there, and host starts the run from there.
- Reworked the co-op lobby action layout into a real 2x2 grid: `Settings` top-left, `Join` top-right, `Back` bottom-left, and one shared bottom-right action button for ready/start flow. Hosts now use that single bottom-right button as `Ready Up` first and then `Start Run` once they are marked ready, with the start state greying out until the rest of the party is ready.
- Split lobby settings into `General` and host-only `Co-op` tabs. Normal player settings stay under `General`, while host session settings like port and player cap now live under the dedicated `Co-op` tab instead of being mixed into the same panel for everyone.
- Mirrored Shattered-style class unlock progression so Warrior is the only guaranteed starting class again. `PlayerProfile` now persists unlocked hero classes and tracks Mage/Rogue/Huntress/Duelist unlock progress, `Hero` now emits the missing surprise-attack and thrown-weapon progress events, solo hero select now previews locked classes but disables `Start` and shows each unlock requirement, and the co-op lobby now clamps the local class selector to classes the player has actually unlocked.
- Expanded the Player Profile summary so the new progression is visible: it now reports unlocked classes, lists the currently available class names, shows icon progress, and surfaces the remaining class unlock requirements directly in the profile panel instead of leaving the new class-gating system hidden behind the hero-select screen.
- Tightened the Player Profile layout again so progression data no longer pushes the page below the visible frame. The profile summary is now a fixed-height snapshot with compact class/icon progress, one current unlock target, and condensed run stats instead of a long multiline report.
- Simplified the Player Profile summary back into a cleaner stats card and moved progression detail into the Achievements popup. The profile panel now focuses on visually compact account/run stats, while `WndBadges` now has separate `Achievements` and `Unlocks` tabs so class/icon unlock requirements are visible without overflowing the profile or requiring horizontal scrolling.
- Replaced the profile icon edit cycle with a proper popup picker. The new `WndProfileIconPicker` shows all profile icons in a grid, keeps locked ones visible but greyed out, marks the current icon, and only allows choosing icons the player has actually unlocked.
- Expanded the profile icon system beyond hero portraits. The picker is now taller so the current icon set fits without scrolling, title/profile previews now use circular portrait crops with a tighter zoom, enemy trophy icons (`rat`, `gnoll`, `crab`, `skeleton`, `goo`) were added, and `PlayerProfile` now unlocks those icons from lifetime kill thresholds while the lobby and Achievements `Unlocks` tab both understand and display the new icon set.
- Fixed the new circular profile icon rendering path and simplified the picker presentation. Circular icon previews now render their textures correctly again, the icon picker tiles are less text-heavy, and locked icons now read through a small corner badge instead of extra explanatory copy inside each tile.
- Reworked profile icon texture extraction to use real cropped textures instead of atlas textures before applying the circular portrait treatment. This should restore visible art in the profile header, picker, and lobby list, and the picker’s lock badge is now an actual drawn corner lock anchored at the lower-right of the icon frame instead of a centered text placeholder.
- Tuned the circular crop framing for hero portrait icons specifically: class portraits are now framed slightly lower-left and a touch less zoomed-in than the default enemy-trophy crop, while the lock badge remains anchored at the lower-right corner of the icon area.
- Updated the co-op lobby player list to use the real selected profile icons from each synchronized lobby slot through the same circular profile-icon renderer and crop rules used by the profile UI and icon picker, instead of drawing them as tiny square textures.
- Ran a broader menu-shell polish pass to unify submenu layout and design language. `TitleScene` profile/settings/multiplayer panels now use one shared submenu size, consistent internal padding, and aligned bottom action rows; the square frame around the profile icon header was removed in favor of a cleaner centered circular portrait; the co-op lobby player list dropped its old square icon box and now uses bare circular profile icons; lobby buttons and modal actions were restyled to match the rest of the menu chrome; and `RankingsScene` was resized and relaid out to the same overall shell dimensions and bottom-button pattern as the other submenu pages.
- Fixed the remaining hero-select layout drift by moving `HeroSelectScene`'s `Back` / `Start` row into the shared bottom action zone instead of leaving it at the old mid-panel coordinates, and removed the obsolete online-only Y-offset that was still making that submenu feel misaligned compared to the rest of the menu family.
- Tightened the co-op lobby stack so its 2x2 action grid sits higher and with more bottom breathing room, bringing the multiplayer submenu closer to the same bottom-button band and padding rhythm used by the other menu pages.
- Removed the extra explanatory lobby text block under the co-op action grid so the multiplayer submenu reads cleaner and stays closer to the same minimal action-first layout as the other submenu pages.
- Matched `HeroSelectScene`'s left-column padding to the co-op lobby by moving its title, class row, description, stats, party/loadout rows, notices, and action row onto the same 24px inset system. This removes the last obvious offset mismatch between the `New Game` and multiplayer submenu layouts.
- Corrected `HeroSelectScene`'s left-column content width so the right-side inset inside the left pane now matches the left-side inset, instead of leaving an overly narrow inner column with visibly uneven padding against the center split.
- Rebuilt `HeroSelectScene`'s left pane around the same constrained `VBoxContainer` stack pattern used by the co-op lobby, instead of continuing to position each label/row manually. The hero-select title, class label, class row, description, stats, optional party rows, notices, and bottom action row now flow through the same structural layout model as the multiplayer left pane, so the two screens should finally format alike instead of only sharing approximate spacing values.
- Tightened that hero-select alignment further by matching the lobby's actual effective content widths and button sizing rhythm instead of only its inset origin. `HeroSelectScene` now uses the same narrow stacked content column and 145px action-button widths as the co-op lobby, which should remove the remaining visual mismatch in the left-pane formatting.
- Fixed the Playwright web-smoke regressions found in the live build: `StatusPane` now uses `update_minimum_size()` instead of calling the `minimum_size_changed` signal like a method, exported audio loading now uses explicit SFX/music manifests instead of runtime `res://` directory scans, the compile smoke now loads every `src/**/*.gd` script, and the deploy workflow now runs a mobile Playwright smoke that fails on critical script/audio console regressions.
- Restored room-door generation for tunneled regular-level neighbor links. `StandardPainter` now places a door at each valid tunnel mouth, preserving secret doors for secret rooms, locked doors for vault/armory rooms, and crystal doors for crystal vaults; the tunnel pair tracker now uses stable string keys instead of an overflowing numeric hash.
- Restored the boss-floor skeleton-key loop closer to SPD: boss mobs now drop a canonical, depth-tagged `skeleton_key`, and descending from boss depths consumes a current-depth skeleton key before transitioning. The check is co-op-aware and can consume the key from any active party hero while rejecting wrong-depth keys.
- Fixed mobile modal close controls by making `WndBase`'s title-bar `X` consume direct screen-touch down/up events and close on the first touch release. Removed the duplicate content-level `Close` buttons from inventory and item detail windows so each popup has a single close affordance again.
- Widened compact mobile toolbar fallback hit targets by 8px without changing desktop behavior, making safe-area/scaled near-edge taps less likely to miss or fall through to world input. Covered by `test_mobile_hud_input.gd`.
